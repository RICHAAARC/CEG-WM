# Geometry-V6 R0

`geometry_v6_training_free_final_latent_roundtrip_sync` is a clean-identity carrier-existence diagnostic. It is not an RST/similarity result, a robustness result, a content decision, or scientific evidence; `science_denominator=0`.

The normal SD3.5 20-step path preserves the existing content-adaptive callback unchanged at zero-based step 18. The V6 callback then applies one fixed-amplitude, frozen-VAE `E(D(z))` adjoint update only at zero-based step 19, after that step's scheduler update and before the pipeline's final decode. Its Fourier energy is strictly within `0.24 < normalized radius < 0.58`.

The blind geometry detector accepts only final/attacked ordinary RGB, a geometry key (or explicit no-key), the frozen public image processor/VAE, and method parameters. It uses posterior mode and never accepts prompt, clean RGB, truth, residual, or embedding latent. Geometry roots are distinct from the content root and derive independent `k_search`, `k_fit`, and `k_validate` subkeys. Geometry reports only raw matching quantities and cannot change content LF/HF/weighted-joint output or make a content positive.

The user-run Colab diagnostic records, under the same prompt/seed, `content_only`, `content_geometry`, `geometry_only`, and `unwatermarked` arms. Each arm records frozen content LF/HF/weighted-joint raws and distinct correct/wrong/no-key geometry calls. It deliberately reports content judgement, carrier window, and compatibility as `NOT_ADJUDICATED` unless a separately authorized frozen roster/threshold supplies those quantities. No retry, per-image amplitude, content-score feedback, threshold change, or fallback exists.

The notebook's first executable cell is the required independent two-line Drive mount. It clones, detached-checks out an externally supplied approved exact, runs the engine in a fresh subprocess, and creates one small JSON at `MyDrive/CEG-WM/Geometry-V6/R0`; it never reads, overwrites, or deletes prior Drive evidence.
