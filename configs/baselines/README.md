# Baseline Configurations

此目录保存已登记外部对比方法的固定 implementation revision、参数和实验适配配置。每份配置必须对应 `docs/reference/baseline_registry.md` 中的条目，并在 comparison protocol 中以内容 digest 固定。

不得使用浮动依赖、未登记的实现偏差、项目方法专属调参预算或只对 baseline 生效的隐含失败规则。当前 CEG-WM 尚未选择或配置外部 baseline。
