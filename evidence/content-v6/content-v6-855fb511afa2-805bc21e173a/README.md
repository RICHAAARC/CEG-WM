# Content V6 current-roster scalar evidence

This directory preserves the exact `receipt.json` and `result.json` members of
the authenticated terminal artifact for
`content-v6-855fb511afa2-805bc21e173a`.

The package contains the complete 8-unit/16-record current-roster result with
51 LF/HF/joint scores per record. It contains no image, prompt text, raw key,
token, latent, route, mask, tensor, or private embed state. The original ZIP
remains external; its exact hash, sidecar binding, sizes, and member hashes are
recorded in `artifact_manifest.json`.
