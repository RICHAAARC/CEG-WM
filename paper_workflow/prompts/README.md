# paper_workflow prompts

This folder contains optional small prompt examples for PW00 smoke checks.

Do not replace official experiment prompt files in prompts/.
PW00 accepts prompt_file as an explicit parameter and can point to any existing prompt file.
