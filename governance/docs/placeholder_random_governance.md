# Placeholder And Random Trace Governance

- Placeholder 字段必须以 `_placeholder` 结尾，登记替换条件，不得支撑 claim、方法完成状态或实验就绪状态。
- 随机轨迹字段必须以 `_random` 或 `_digest_random` 结尾，并提供 seed、digest 或可复现说明。
- `seed` 等随机控制字段使用直接语义名，不等同于随机轨迹字段。

字段实例及等级见 `docs/reference/field_registry.md`。
