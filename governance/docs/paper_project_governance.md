# Research Progress Guard

外层护栏只用于防止构建路径偏移，不是研究项目的目标或运行组件。

- 方法、runtime、实验或 baseline 任务必须产生对应业务层的实质实现；只改 docs、policy、skills 或 tests 不能宣告完成。
- 阶段门禁只判断是否具备继续推进的条件，不把审计通过解释为方法有效。
- 空目录、placeholder、dry-run、弱化实现和结构测试不能代替设计要求的机制。
- 样本范围、阈值选择、排除条件、不确定性和失败记录属于研究协议与 records，不属于 harness 产物。
