[pytest]
testpaths = tests
pythonpath = ..
addopts = -m "(constraint or unit or quick) and not integration and not smoke and not slow and not formal"
markers =
    unit: governance pure behavior tests
    constraint: governance contract tests
    quick: lightweight governance functional tests
    integration: excluded integration tests
    smoke: excluded smoke tests
    slow: excluded expensive tests
    formal: excluded formal tests
