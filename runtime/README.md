# Runtime Layer

`runtime/` owns SD3.5 execution, content write/measurement, and Q/K observation behind the `main` public surface. It does not decide watermark positives, thresholds, or promotion. The retired runtime-qualification delivery family is preserved only through its producer revision in Git ancestry and is not a current runtime authority.
