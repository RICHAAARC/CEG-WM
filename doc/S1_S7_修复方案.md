# S1–S7 静态审计差距修复方案

> 生成时间：2026-03-01（基于真实代码读取校准，最终合并版）  
> 适用版本：当前 main 分支  
> 约束锚：frozen_contracts.yaml / runtime_whitelist.yaml / injection_scope_manifest.yaml / records_schema_extensions.yaml  
> 校准依据：逐项读取 `main/` 与 `configs/` 真实实现，非仅依据方案文本

---

## 总览

| 编号 | 级别 | 差距摘要 | 修改文件 | 预期工作量 |
|------|------|----------|----------|-----------|
| S1 | CRITICAL | 语义掩码 v2 无神经模型 | semantic_mask_provider.py | 中 |
| S2 | CRITICAL | Self-Attention 提取空壳 | infer_runtime.py, hooks.py | 高 |
| S3 | CRITICAL | ECC/LDPC/BP 未实现 | low_freq_coder.py, (新) ldpc_codec.py | 高 |
| S4 | HIGH | Fusion rescue 不检查证据等级 | decision.py | 低 |
| S5 | HIGH | 无图像质量指标 | metrics.py, (新) image_quality.py | 中 |
| S6 | HIGH | 无 AUC/ROC 曲线 | metrics.py | 低–中 |
| S7 | HIGH | 几何同步无 embed 侧注入 | latent_sync_template.py | 中–高 |

### 复审收敛说明（2026-03-01）

- S1：统一使用 `model_artifact_anchor` 作为模型锚点语义容器，避免新增并行字段导致口径分叉。
- S2：`AttentionAnchorMapRelation` 非空壳，已可消费 `attention_maps` 并给出 `authentic/proxy`；主改造点聚焦 `infer_runtime.py + hooks.py` 的 runtime 真实采集链路。
- S3：不再要求“先全量把 `ecc` 改为 int”作为硬前置；先在代码层兼容 `ecc: "sparse_ldpc"` 与 int 双语义，再分阶段迁移配置。
- S4：rescue 门控保持基于现有主字段 `status/geo_score`，`sync_status` 必须先对齐现有可读取来源字段后再参与规则。

### 本轮强制修正点（遗漏补齐）

- S3 不能只改 `LFCoderPRC`：必须同步处理 detect 侧仍在使用旧 LF 路径的问题，否则会出现“双实现口径并存”。
- S2 主改造重心固定为 runtime 采集链路（`infer_runtime.py + hooks.py`），`AttentionAnchorMapRelation` 仅做最小适配。
- S1 模型锚点语义固定为 `model_artifact_anchor`，禁止新增并行容器（如 `semantic_model_anchor`）造成审计口径分叉。
- 方案依赖的 schema/registry 与 plan-digest 路径追加在落地前为阻塞项；当前需先完成：`injection_scope_manifest.yaml:62`、`records_schema_extensions.yaml`、`frozen_contracts.yaml:394`。

---

## S1：语义掩码 v2 无神经网络显著性模型

### 1.1 当前问题

`build_semantic_saliency_mask_v2()`（L958）直接调用 `build_semantic_saliency_mask_v1()` 产出梯度启发式掩码（0.45×gradient + 0.35×center + 0.20×gray），论文核心创新"语义显著性自适应"未实现。`_probe_model_v2_availability()`（L744）仅检查路径存在性，无模型加载逻辑。

### 1.2 修复目标

- `saliency_source == "model_v2"` 时，加载真实语义显著性模型（U2-Net / BASNet / InSPyReNet 均可），在 GPU/CPU 上推理，输出归一化显著性图 `[0,1]`
- `saliency_source == "auto_fallback"` 时，尝试 model_v2 → 失败回退 proxy_v1，`mask_stats.fallback_used = True`
- 所有 digest（`mask_digest`, `routing_digest`）必须反映新路径

### 1.3 需要新增/修改的函数

| 函数 | 操作 | 位置 |
|------|------|------|
| `_load_saliency_model(params) → nn.Module` | **新增** | semantic_mask_provider.py L744 附近 |
| `_run_saliency_inference(model, image_tensor, params) → np.ndarray` | **新增** | semantic_mask_provider.py L764 附近 |
| `_probe_model_v2_availability(params) → bool` | **修改** | L744 — 加入 `torch.load()` 快速校验 |
| `build_semantic_saliency_mask_v2(...)` | **修改** | L958 — 删除 v1 调用，插入模型推理主流程 |
| `_build_model_artifact_anchor(params)` | **修改** | L764 — 增加 `weights_sha256` 字段 |

### 1.4 实施步骤

**(1)** 新建 `_load_saliency_model(params)`：
- 读取 `params["semantic_model_path"]`，使用 `torch.load(..., map_location="cpu")` 加载权重
- 如 `params["semantic_model_source"] == "u2net"`，实例化 U2-Net 轻量版（U²-Net-lite，~4.7M 参数）
- 计算模型权重 SHA256 写入 `weights_sha256` 字段（审计可追溯）
- 超时保护：`params["mask_inference_timeout"]` 秒内未完成则抛出，上层 `select_saliency_source` 处理 fallback

**(2)** 新建 `_run_saliency_inference(model, image_tensor, params)`：
- 输入 `image_tensor`: `[1, 3, H, W]` float32 归一化到 `[0, 1]`
- 预处理策略由 `params["semantic_preprocess"]` 控制（`"rgb_normalized"` / `"bgr_imagenet"`）
- 输出 `saliency_map: np.ndarray [H, W]` 范围 `[0, 1]`
- 阈值化策略由 `params["semantic_thresholding"]` 控制（`"quantile"` → 使用 `saliency_threshold_quantile`；`"otsu"` → 大津法）

**(3)** 修改 `build_semantic_saliency_mask_v2()`：

```python
# 伪代码结构
def build_semantic_saliency_mask_v2(image, image_shape, cfg, params):
    model = _load_saliency_model(params)  # 进程级缓存，避免逐样本重复加载
    image_tensor = _preprocess_for_saliency(image, image_shape, params)
    saliency_map = _run_saliency_inference(model, image_tensor, params)
    threshold = float(np.quantile(saliency_map, float(params["saliency_threshold_quantile"])))
    mask = saliency_map >= threshold
    # 形态学后处理（与 v1 共享 open_iters / close_iters 逻辑）
    # ...
    model_path = params.get("semantic_model_path", "")
    mask_stats = {
        "area_ratio": ...,
        "saliency_source_selected": "model_v2",
        "model_artifact_anchor": {
            # ⚠ 路径脱敏：禁止写入绝对路径，仅记录 path_name + weights_sha256
            "model_source": params["semantic_model_source"],
            "model_version": params["semantic_model_version"],
            "weights_id": params["semantic_weights_id"],
            "model_path_name": Path(model_path).name if model_path else "<absent>",
            "weights_sha256": _compute_weights_sha256(model_path),  # 不写原始路径
            "preprocess": params["semantic_preprocess"],
            "thresholding": params["semantic_thresholding"],
        },
        # ⚠ 必须保持 saliency_provenance 字段完整性
        "saliency_provenance": {
            "source_selected": "model_v2",
            "source_attempted": "model_v2",
            "fallback_used": False,
            "fallback_reason": None,
        }
    }
    binding = _bind_resolution(image_shape, cfg)
    return mask, saliency_map, mask_stats, binding
```

> **路径脱敏要求（基于真实代码校准）**：当前 `build_semantic_saliency_mask_v2()` 在 v2 统计中仍写入原始路径字段。修复时应统一写入 `mask_stats["model_artifact_anchor"]`（沿用既有语义容器），仅保留 `model_path_name`（文件名）并新增 `weights_sha256`，避免引入 `semantic_model_anchor` / `model_artifact_anchor` 双容器口径分叉。

**(4)** 修改 `_probe_model_v2_availability()`：加入实际 `torch.load` 验证（捕获异常返回 False）。

**(5)** 进程级模型缓存：在 `_load_saliency_model()` 中实现 `_SALIENCY_MODEL_CACHE: Dict[str, nn.Module]`，以 `semantic_model_path` 为 key，避免批量处理时逐样本重复加载导致吞吐崩溃。

**(6)** fallback 路径完整性：当 model_v2 加载失败时，`saliency_provenance` 必须记录 `fallback_used=True`、`fallback_reason=<具体原因>`、`source_attempted="model_v2"`、`source_selected="proxy_v1"`，与现有 `select_saliency_source()` 输出口径保持一致。

### 1.5 冻结约束绑定

| 约束 | 说明 |
|------|------|
| `runtime_whitelist.yaml/enums/mask_impl_id/allowed` | `semantic_saliency_v2` 已在白名单，**无需修改** |
| `runtime_whitelist.yaml/enums/saliency_source/allowed` | `model_v2` 已在白名单，**无需修改** |
| `records_schema_extensions.yaml` | `content_evidence.mask_stats.model_artifact_anchor` 已注册；需 **append** `weights_sha256` 子字段；`model_path` 字段**必须改为** `model_path_name`（路径脱敏） |
| `injection_scope_manifest.yaml/plan_digest_include_paths` | `watermark.mask.semantic_model_path` 需 **append**（模型路径变化 → plan_digest 变化）；经确认，该路径当前**未注册** |
| `frozen_contracts.yaml/field_paths_registry` | 需 **append** `content_evidence.mask_stats.model_artifact_anchor.weights_sha256` |

### 1.6 不可修改区域

- `SaliencySourceDecision` 的 frozen dataclass 字段定义
- `SEMANTIC_MASK_PROVIDER_ID`, `SEMANTIC_SALIENCY_V2_IMPL_ID` 常量值
- `ALLOWED_MASK_FAILURE_REASONS` — 只允许 append
- `compute_mask_digest()`, `compute_routing_digest()` 的哈希算法不变（仍委托 `digests.canonical_sha256`）

### 1.7 测试验证

- **单元测试**：`test_semantic_mask_v2_loads_model` — mock torch.load，验证返回 saliency map 而非 gradient heuristic
- **单元测试**：`test_semantic_mask_v2_fallback_records_reason` — model_v2 加载失败时，验证 fallback 到 proxy_v1 且 `mask_stats.fallback_used == True`
- **Digest 测试**：`test_mask_digest_changes_when_saliency_source_changes` — 切换 proxy_v1 ↔ model_v2 时 mask_digest 必须不同
- **白名单合规**：现有 `test_align_impl_whitelist_enforcement` 应自动通过

### 1.8 预期成果

- `saliency_source == "model_v2"` 时产出真实神经网络语义显著性图
- 论文核心创新"语义显著性自适应流形"在代码层面闭环
- 审计链完整：`model_artifact_anchor.weights_sha256` → `mask_digest` → `plan_digest`

---

## S2：SD3 Self-Attention Map 提取空壳

### 2.1 当前问题

`_extract_runtime_self_attention_maps()`（infer_runtime.py L418）会探测 output / pipeline 上多个属性名（`self_attention_maps`, `attention_maps`, `runtime_self_attention_maps` 等），但缺少稳定生产该字段的 runtime capture 机制；在当前主路径下通常返回 `(None, "<absent>")`。detect 链已支持通过 `__runtime_self_attention_maps__` 注入（orchestrator.py `_resolve_runtime_self_attention_maps`），但没有 hook 能产出该字段。hooks.py 只有注入回调，无 `register_forward_hook` 抓取 attention。几何锚点全链路为 latent correlation proxy（`anchor_evidence_level="proxy"`）。

### 2.2 修复目标

- 在 SD3 Transformer 的 self-attention Q/K 投影层（`to_q` / `to_k`）注册 `register_forward_hook`，捕获投影并在推理后重建 attention weights（不直接依赖 forward output）
- 将捕获的 attention maps 传递给 `AttentionAnchorMapRelation.extract()` 用于真实几何锚点
- `anchor_evidence_level` 从 `"proxy"` 提升到 `"authentic"`（代码中 `authentic` 表示真实 attention 数据，区别于 `proxy`）

### 2.3 需要新增/修改的函数

| 函数 | 操作 | 文件 |
|------|------|------|
| `class SelfAttentionCaptureHook` | **新增** | hooks.py |
| `register_attention_hooks(pipeline, cfg) → SelfAttentionCaptureHook` | **新增** | hooks.py |
| `remove_attention_hooks(hook_handle) → None` | **新增** | hooks.py |
| `_extract_runtime_self_attention_maps(...)` | **修改** | infer_runtime.py L418 — 优先从 hook 缓存读取 |
| `run_sd3_inference(...)` | **修改** | infer_runtime.py L28 — 新增 `attention_capture` 参数 |
| `AttentionAnchorMapRelation.extract(...)` | **可选微调** | attention_anchor_extractor.py — 仅在需要补充 source 标签时最小改动 |

### 2.4 实施步骤

**(1)** hooks.py 新增 `SelfAttentionCaptureHook` 类：

> **重要技术说明**：diffusers 的 `Attention` 模块（`AttnProcessor2_0` 等）内部调用 `F.scaled_dot_product_attention`，其 `forward()` 仅返回 `hidden_states`（单 tensor），**不返回 attention weights**。因此不能用简单的 `register_forward_hook` 捕获 output 来获取 attention map。
>
> **本轮收敛实现（唯一主路径）**：在 Q/K 线性投影层（`to_q`, `to_k`）上注册 hook 捕获投影输出，然后在推理结束后外部计算 `softmax(Q @ K^T / sqrt(d))` 得到 attention weights。
>
> **不纳入本轮**：替换 attention processor 为自定义版本仅作为后续优化选项，本轮不并行落地，以避免双路径维护与审计口径分叉。

```python
class SelfAttentionCaptureHook:
    """
    功能：捕获 SD3 Transformer self-attention Q/K 投影，按模块名+step 维度缓存配对，事后计算 attention weights。

    Capture Q/K projections from SD3 Transformer Attention layers,
    keyed by (module_name, step_index) to avoid cross-layer/cross-step mismatch.
    Only captures self-attention (.attn1) layers; cross-attention (.attn2) is excluded.
    Supports optional layer sampling to limit memory and latency.

    Args:
        max_layers: Maximum number of attention layers to hook (None = all self-attn layers).
        sample_steps: Set of denoising step indices to capture (None = all steps).
    """
    def __init__(self, max_layers=None, sample_steps=None):
        self._captures = {}        # Dict[(module_name, step_idx), {"q": Tensor, "k": Tensor}]
        self._hook_handles = []    # List[RemovableHandle]
        self._step_index = 0       # 当前 denoising step 索引
        self._max_layers = max_layers
        self._sample_steps = sample_steps  # 可选：仅采集指定 step

    def _make_hook_fn(self, module_name, projection):
        """生成闭包 hook，绑定 module_name 和 projection 类型。"""
        def hook_fn(module, input, output):
            # 可选：仅在指定 step 采集
            if self._sample_steps is not None and self._step_index not in self._sample_steps:
                return
            key = (module_name, self._step_index)
            if key not in self._captures:
                self._captures[key] = {}
            self._captures[key][projection] = output.detach().cpu()
        return hook_fn

    def advance_step(self):
        """推进 denoising step 计数器（在每个 scheduler step 后调用）。"""
        self._step_index += 1

    def register(self, pipeline, cfg):
        """
        在 transformer 的 self-attention 子模块的 to_q / to_k 上注册 hook。
        仅针对 self-attention（通常 '.attn1.' 或 '.attn.' 且不含 '.attn2.'），
        跳过 cross-attention（'.attn2.'）以避免拿到错误的 attention map。
        """
        transformer = getattr(pipeline, "transformer", None)
        if transformer is None:
            return
        # SD3Transformer2DModel → transformer_blocks[*].attn.to_q / .to_k
        # 注意：Self-attn 通常在 .attn 或 .attn1，cross-attn 在 .attn2
        hooked_layers = 0
        for name, module in transformer.named_modules():
            # 跳过 cross-attention 层
            if ".attn2." in name:
                continue
            # 限制采样层数
            if self._max_layers is not None and hooked_layers >= self._max_layers:
                break
            if name.endswith(".to_q"):
                # 提取父层名（如 transformer_blocks.0.attn）作为 module_name
                parent_name = name.rsplit(".", 1)[0]
                handle = module.register_forward_hook(self._make_hook_fn(parent_name, "q"))
                self._hook_handles.append(handle)
            elif name.endswith(".to_k"):
                parent_name = name.rsplit(".", 1)[0]
                handle = module.register_forward_hook(self._make_hook_fn(parent_name, "k"))
                self._hook_handles.append(handle)
                hooked_layers += 1  # 按 K hook 计数（一对 Q/K = 一层）

    def compute_attention_maps(self):
        """从收集的 (Q, K) 对计算 attention weights。仅处理 Q/K 都存在的条目。"""
        import torch
        maps = []
        for key in sorted(self._captures.keys()):
            pair = self._captures[key]
            q, k = pair.get("q"), pair.get("k")
            if q is None or k is None:
                # Q/K 不完整，跳过（不会产出错误配对）
                continue
            d_k = q.shape[-1]
            attn_weights = torch.softmax(q @ k.transpose(-2, -1) / (d_k ** 0.5), dim=-1)
            maps.append({"module": key[0], "step": key[1], "attn": attn_weights})
        return maps

    def collect(self):
        """收集 attention maps 并清空缓存。
        
        ⚠ 接口契约（与 AttentionAnchorMapRelation 对齐）：
        `_build_relation_graph()` 期望输入为 tensor 或 ndarray，不接受 list[dict] 结构。
        因此 collect() 必须输出如下格式之一：
          - 单个 stacked tensor/ndarray： shape [N_layers, H, W] 或 [N, heads, H, W]
          - 或付有 metadata 的 dict，但其中 "attn" 字段必须是 tensor/ndarray
        禁止直接返回原始 list[dict(attn=Tensor, ...)] 而不做类型收敛。
        """
        maps = self.compute_attention_maps()
        self._captures.clear()
        self._step_index = 0
        if not maps:
            return None
        # 将 list[dict] 收敛为 stacked tensor，供 _build_relation_graph 直接使用
        import torch
        attn_tensors = [m["attn"] for m in maps if m.get("attn") is not None]
        if not attn_tensors:
            return None
        return torch.stack(attn_tensors, dim=0)  # [N_captured, ...]

    def remove(self):
        """移除所有 hook handles。"""
        for h in self._hook_handles:
            h.remove()
        self._hook_handles.clear()
```

> **关键设计决策说明**：
> - **按 (module_name, step_index) 维度缓存**：避免多层/多 step 下全局 `_current_q/_current_k` 串层配对错误。
> - **跳过 `.attn2.`（cross-attention）**：仅捕获 self-attention，避免拿到 text-image cross-attention map。
> - **`max_layers` 默认值 ≤ 8**：SD3 有 24 层 transformer block，推荐**默认 4–8 层**（单元测试须明确声明该值），全采集内存与时延不可控。
> - **`sample_steps` 推荐默认 `{10, 20, 30}`**：50 step 推理中仅采集中间段，须在文档中声明，R1 运行后依实测调整。
> - **`advance_step()` 必须在 on_step_end 回调中调用**，以保持 step 计数同步。
> - **`collect()` 输出格式契约**（基于真实代码校准）：`AttentionAnchorMapRelation._build_relation_graph()` 仅接受 tensor 或 ndarray 作为 `attention_maps` 参数，遇到其他类型直接 `raise ValueError`。因此 `collect()` 必须输出 stacked tensor `[N_captured, heads, H, W]`，禁止返回未收敛的 `list[dict]`。
>
> **后续可选优化（不纳入本轮）**：若项目后续选择替换 AttnProcessor，可实现 `CaptureAttnProcessor` 在 `__call__` 中调用 `torch.nn.functional.scaled_dot_product_attention` 的同时保存 attention weights 到外部列表。该路径需在单独迭代中引入，不与本轮主路径并行。
>
> **R1 运行时验证必须确认**：SD3Transformer2DModel 的 `named_modules()` 中 self-attention 与 cross-attention 的确切命名模式（如 `.attn.to_q` vs `.attn2.to_q`），Q/K tensor shape 是否含 multi-head 维度，以及合理的 `max_layers` / `sample_steps` 默认值。

**(2)** infer_runtime.py 修改 `run_sd3_inference()`：
- 新增参数 `capture_attention: bool = False`
- 当 `capture_attention=True` 时，在推理前调用 `register_attention_hooks()`，推理后 `collect()` + `remove()`
- 将 collected maps 存入 `result["runtime_self_attention_maps"]`

**(3)** 修改 `_extract_runtime_self_attention_maps()`：
- 优先检查 `output.get("runtime_self_attention_maps")` — 这是 hook 写入的位置
- 若存在则返回 `(maps, "hook_capture")`
- 保留既有 fallback 路径（属性探测），最终返回 `(None, "<absent>")`

**(4)** attention_anchor_extractor.py（可选最小改动）：
- 当前 `extract()` 已支持 `attention_maps` 输入，并根据 `attention_maps_source` 产出 `anchor_evidence_level = "authentic" / "proxy"`
- 默认不作为主改造目标，仅在需要补充 `attention_capture_source` 等审计标签时做 append-only 字段补充
- S2 主改造重心保持在 runtime 供给链（`infer_runtime.py + hooks.py`）

### 2.5 冻结约束绑定

| 约束 | 说明 |
|------|------|
| `runtime_whitelist.yaml/impl_id` | `attention_anchor_map_relation_v1` 已在白名单，**无需修改** |
| `records_schema_extensions.yaml` | `geometry_evidence.anchor_evidence_level` **已注册**（L403），无需修改 |
| `records_schema_extensions.yaml` | 需 **append** `geometry_evidence.attention_capture_source`（layer: diagnostic, type: string） |
| `frozen_contracts.yaml/field_paths_registry` | `anchor_evidence_level` **已注册**（L485），仅需 append `attention_capture_source` |
| `frozen_contracts.yaml/status_enum` | **不可修改** — status 仍在 `{ok, failed, mismatch, absent}` 内 |

### 2.6 不可修改区域

- `AnchorResult` frozen dataclass 字段（可 append 方法，不可修改已有字段）
- `SyncResult`, `SyncRuntimeContext` frozen dataclass
- hooks.py 已有的 `define_on_step_end_callback`, `collect_step_evidence`, `define_composed_callback` 不可修改签名
- `run_sd3_inference()` 现有返回 dict 的 key 不可删除

### 2.7 测试验证

- **单元测试**：`test_attention_hook_captures_mock_attn_weights` — 用 mock transformer module 验证 hook 注册与收集
- **单元测试**：`test_extract_attention_maps_prefers_hook_over_probe` — 验证 hook 结果优先于属性探测
- **集成测试**：`test_anchor_evidence_level_authentic_when_maps_provided` — 传入 attention maps → `anchor_evidence_level == "authentic"`
- **回归测试**：现有 `test_anchor_gate_by_sync_status` 不应受影响

### 2.8 预期成果

- `anchor_evidence_level` 从永久 `"proxy"` 变为条件 `"authentic"` / `"proxy"`
- 几何锚点基于真实 self-attention 关系矩阵而非 latent correlation
- 论文创新"Attention Map 的相对关系绑定"在代码层面闭环
- **注意**：SD3 transformer 的 attention layer 结构需要在 R1（真实 SD 运行）中验证

---

## S3：ECC/LDPC/BP 解码未实现

### 3.1 当前问题

- `LFCoderPRC.extract()`的内部分数计算为后验加权和 `(posterior_score * 0.8 + confidence * 0.2) * strength`，非基于 H 矩阵的 LDPC BP 解码；`embed_apply()` 的 codeword 生成为伪随机 0/1 序列，没有经过 LDPC 系统编码
- **代码已在内部使用 `block_length` 作为 latent 截取刱0：`embed_apply()` L278 和 `extract()` L554 均使用 `block_length` 不是 `message_length`。真正需要修复的是算法本身**
- config `ecc: "sparse_ldpc"`（string 类型）与 `LowFreqCoder` 的 `isinstance(ecc, int)` 校验冲突，会直接触发 ValueError
- `ecc_sparsity` 仅用于 digest 绑定，未参与实际矩阵构造；且该参数尚未在任何代码中定义语义
- detect 侧仍存在旧 LF 路径消费口径（未与新 LDPC 分支统一），若仅改 embed 或仅改 `LFCoderPRC`，会产生“同一配置、两套 LF 语义”的并存风险

### 3.2 修复目标

- 实现真实 sparse LDPC 编码/解码
- BP 在因子图上迭代（sum-product 或 min-sum）
- codeword 为 LDPC 编码后的合法码字
- 可配置参数：`message_length`, `ecc_sparsity` (校验矩阵列重), `bp_iterations`

### 3.3 需要新增/修改的函数

| 函数 | 操作 | 文件 |
|------|------|------|
| `class SparseLDPC` | **新增** | main/watermarking/content_chain/ldpc_codec.py（新文件） |
| `SparseLDPC.build_parity_check_matrix(n, k, col_weight) → H` | **新增** | ldpc_codec.py |
| `SparseLDPC.encode(message_bits) → codeword` | **新增** | ldpc_codec.py |
| `SparseLDPC.decode_bp(llr_input, max_iter) → (decoded_bits, converged)` | **新增** | ldpc_codec.py |
| `LFCoderPRC.embed_apply(...)` | **修改** | low_freq_coder.py（现有 embed 入口）——替换 codeword 生成逻辑 |
| `LFCoderPRC.extract(...)` | **修改** | low_freq_coder.py（现有检测分数入口）——替换为 LDPC BP 解码 |
| `detect 侧 LF 路径适配函数（现有入口）` | **修改** | 现有 detect 流程中 LF 读取/评分入口——统一到同一 LDPC 语义分支 |
| `recover_posteriors_erf(...)` | 保留 | 作为 LLR 初始化的输入不变 |

### 3.4 实施步骤

**(0)** 前置步骤：`ecc` 双语义兼容与迁移（兼容优先）

当前仓库同时存在 `ecc` 为 string（`"sparse_ldpc"`）和 int 两种路径，`LowFreqCoder` 的 `isinstance(ecc, int)` 校验会导致 string 路径直接 ValueError。建议先在代码层做兼容：
- `ecc` 为 `"sparse_ldpc"`：走 LDPC 分支（列重由 `ecc_sparsity` 承载）
- `ecc` 为 int：兼容旧分支并给出 deprecation 提示

在兼容路径稳定后，再分阶段将配置迁移为统一语义，避免现有流程因硬切换立即中断。

**(0.1)** detect 侧同步改造（阻塞级，必须先定口径）

- 明确 detect 侧 LF 分数计算入口只能走与 embed 同一分支判定（`ecc == "sparse_ldpc"` 或 `ecc=int`）。
- 禁止“embed 走 LDPC、detect 走旧后验加权”的混用。
- 若 detect 侧暂无法一步切换，必须提供显式 adapter，并在 records 中记录 `lf_impl_binding` 的 `fallback_used/fallback_reason`。

**(1)** 新建 `main/watermarking/content_chain/ldpc_codec.py`：

```python
LDPC_CODEC_ID = "ldpc_codec_v1"

class SparseLDPC:
    """
    功能：稀疏 LDPC 编码器/解码器（Gallager 随机构造）。

    Sparse Low-Density Parity-Check codec with BP decoding on factor graph.

    Args:
        n: Codeword length (block_length).
        k: Message length.
        col_weight: Column weight of H matrix (ecc_sparsity).
        seed: Deterministic seed for H matrix construction.
    """
    def __init__(self, n, k, col_weight, seed):
        # (1) Gallager 随机构造：生成 (n-k)×n 的稀疏 H 矩阵
        # 每列恰好 col_weight 个 1
        self.h_matrix = self._build_gallager_h(n, k, col_weight, seed)
        self.n = n
        self.k = k

    def _build_gallager_h(self, n, k, col_weight, seed):
        """构造 Gallager 随机 LDPC 校验矩阵 H。"""
        # 确定性构造：使用固定 seed 保证可复现
        ...

    def encode(self, message_bits):
        """系统编码：message || parity。"""
        # 从 H → G 矩阵（GF(2) 上），编码
        ...

    def decode_bp(self, llr_input, max_iter):
        """Sum-product BP 解码。
        
        Args:
            llr_input: LLR 初始值（来自 recover_posteriors_erf 的后验）。
            max_iter: 最大 BP 迭代次数。
        
        Returns:
            (decoded_bits, converged, iteration_count)
        """
        # (1) 初始化：变量节点 → 校验节点消息 = llr_input
        # (2) 校验节点更新：tanh 域乘积
        # (3) 变量节点更新：LLR 累加
        # (4) 检查是否 H @ decoded == 0 (GF2)
        # (5) 未收敛则回到 (2)
        ...
```

**(2)** 修改 `LFCoderPRC.embed_apply()`：

```python
# 替换当前的伪随机 codeword 生成：
from main.watermarking.content_chain.ldpc_codec import SparseLDPC

# 注意：当前代码已正确使用 block_length 作为 latent 截取维度，
# 修复核心是：用 LDPC 编码的码字替换伪随机 codeword。
block_length = int(lf_cfg.get("block_length", 8))
ecc_sparsity = int(lf_cfg.get("ecc_sparsity", 3))   # H 矩阵列重
ldpc = SparseLDPC(
    n=block_length,
    k=message_length,
    col_weight=ecc_sparsity,
    seed=seed   # 已从 plan_digest 派生
)
# (1) 生成确定性 message bits（仍用 plan_digest 派生种子）
message_bits = [1 if rng.random() < 0.5 else 0 for _ in range(message_length)]
# (2) LDPC 编码 → 得到 n-bit codeword（bipolar: ±1）
codeword_binary = ldpc.encode(message_bits)
codeword = [1 if b == 1 else -1 for b in codeword_binary]  # bipolar
# (3) 用 codeword 做 latent sign flipping——范围必须是 range(block_length)
for i in range(block_length):
    embedded_latents[i] = flat_latents[i] * codeword[i] * variance
```

**(3)** 修改 `LFCoderPRC.detect_score()`：

```python
# 替换当前的 1D 邻居 BP：
posteriors = recover_posteriors_erf(flat_latents[:block_length], variance)

ldpc = SparseLDPC(n=block_length, k=message_length, col_weight=ecc_sparsity, seed=seed)
# 将后验转换为 LLR 输入
llr_input = [2.0 * p / variance for p in posteriors]
decoded_bits, converged, iter_count = ldpc.decode_bp(llr_input, bp_iterations)

# 计算检测分数：decoded message 与 expected message 的相关性
expected_message = [1 if rng.random() < 0.5 else 0 for _ in range(message_length)]
match_count = sum(1 for a, b in zip(decoded_bits[:message_length], expected_message) if a == b)
lf_score = match_count / message_length  # 归一化到 [0, 1]
```

**(4)** trace 字段更新：
- `parity_check_digest` 必须包含 H 矩阵结构摘要（`digests.canonical_sha256(H.tolist())`）
- 新增 `bp_converged: bool`, `bp_iteration_count: int` 字段

### 3.5 冻结约束绑定

| 约束 | 说明 |
|------|------|
| `runtime_whitelist.yaml/impl_id` | `lf_coder_prc_v1` 已在白名单，**无需修改**（内部升级，impl_version 递增） |
| `injection_scope_manifest.yaml/plan_digest_include_paths` | `watermark.lf.ecc` 已在路径中（L77），`watermark.lf.block_length` 已在路径中（L80），但 `watermark.lf.ecc_sparsity` **未注册**，需 **append** 到 `plan_digest_include_paths`（ecc_sparsity 影响 H 矩阵结构 → 必须影响 plan_digest） |
| `records_schema_extensions.yaml` | 需 **append**：`content_evidence.bp_converged`（diagnostic, boolean_flag）、`content_evidence.bp_iteration_count`（diagnostic, integer） |
| `frozen_contracts.yaml/field_paths_registry` | append 上述两项 |
| `frozen_contracts.yaml/digest_canonicalization` | H 矩阵摘要必须使用 `digests.canonical_sha256`，**禁止自行 json.dumps + sha256** |

### 3.6 不可修改区域

- `LF_CODER_PRC_ID = "lf_coder_prc_v1"` 常量值
- `ALLOWED_LF_CODER_FAILURE_REASONS` — 只允许 append
- `embed_apply()` / `detect_score()` 的签名不可修改（`cfg, latent_features, plan_digest, cfg_digest`）
- `recover_posteriors_erf()` 和 `erf()` 函数不可修改
- `_flatten_to_list()` 不可修改
- codeword/message 种子必须仍从 `plan_digest` 派生（冻结确定性要求）

### 3.7 测试验证

- **单元测试**：`test_ldpc_encode_decode_roundtrip` — 随机消息编码 → 无噪声 BP 解码 → 100% 恢复
- **单元测试**：`test_ldpc_bp_corrects_errors` — 加入可纠错范围内的错误位 → 解码成功
- **单元测试**：`test_lf_coder_prc_embed_uses_ldpc_codeword` — 验证 embedded latents 长度 == block_length（非 message_length）
- **单元测试（新增，阻塞）**：`test_detect_lf_path_matches_embed_branch_selection` — 同一 cfg 下 embed/detect 必须选择同一 LF 分支
- **单元测试（新增，阻塞）**：`test_ecc_sparse_ldpc_and_int_branches_are_explicitly_recorded` — 双语义分支均需记录到 `lf_impl_binding`
- **Digest 回归**：`test_parity_check_digest_includes_h_matrix` — 验证 `parity_check_digest` 非空且随 ecc_sparsity 变化
- **已有回归**：`test_attack_digest_stable` 不应受影响（攻击 digest 与 LF 编码无关）

### 3.8 预期成果

- BP 解码基于真实因子图，错误纠正能力与理论一致
- 论文"PRC-Watermark 编码约束策略"在代码层面闭环
- parity_check_digest 可审计，包含 H 矩阵指纹

---

## S4：Fusion Rescue 不检查 Anchor Evidence Level / Sync Status

### 4.1 当前问题

`NeumanPearsonFusionRule.fuse()`（decision.py L132）的 rescue band 逻辑（步骤 7）仅检查 `geometry_status == "ok"` + `isinstance(geometry_score, (int, float))`。不检查：
- `anchor_evidence_level`（可能为 `"proxy"`）
- `sync_status`（可能为 `"absent"` 或 `"failed"`）
- 结果：proxy 级别的几何证据可触发 rescue，翻转 NP 主决策

### 4.2 修复目标

- Rescue band 额外要求：`anchor_evidence_level != "proxy"` **或** `sync_status_normalized == "ok"`
- 若 proxy 几何触发了 rescue 候选但被门控拒绝，记录 `rescue_blocked_reason = "proxy_geometry_not_trusted"`
- 单侧策略不变：仅允许 False → True

### 4.3 需要修改的函数

| 函数 | 操作 | 位置 |
|------|------|------|
| `NeumanPearsonFusionRule.fuse(...)` | **修改** | decision.py 步骤 (7) — 增加 evidence level 门控 |
| `_check_geo_gate(...)` | **不变** | 签名和逻辑不变，evidence level 检查在 `fuse()` 调用 `_check_geo_gate` 之前完成 |

### 4.4 实施步骤

**(1)** 修改 `fuse()` 步骤 (7)：

```python
# (7) 几何辅助（rescue band）：单侧救回策略
rescue_band_spec = _build_rescue_band_spec(cfg)
rescue_blocked_reason = None

if (geometry_status == "ok" and
    isinstance(geometry_score, (int, float)) and
    not content_decision):

    # 新增：检查几何证据可信度
    anchor_level = geometry_evidence.get("anchor_evidence_level", "proxy")

    # 优先对齐当前真实可读字段来源：
    # 1) geometry_evidence["sync"]["status"]
    # 2) geometry_evidence["sync_status"] / ["sync_status_detail"]（兼容）
    # 3) 回退 absent
    sync_node = geometry_evidence.get("sync") if isinstance(geometry_evidence.get("sync"), dict) else {}
    sync_status_raw = (
        sync_node.get("status")
        or geometry_evidence.get("sync_status")
        or geometry_evidence.get("sync_status_detail")
        or "absent"
    )
    sync_status_normalized = "ok" if sync_status_raw in {"ok", "synced"} else sync_status_raw
    
    # ⚠ 融合规则主判定字段仍以 status/geo_score 为主。
    #   sync_status 仅作为 proxy rescue 的可信度门控补充信息。
    #   其中 synced/ok 口径需做归一化。
    #   anchor_evidence_level 枚举值为 "authentic"/"proxy"（来自 attention_anchor_extractor）。
    #   AnchorResult.as_geometry_evidence() 不含 sync_status 键，默认 "absent"。
    geo_trusted = (anchor_level != "proxy") or (sync_status_normalized == "ok")
    
    if not geo_trusted:
        # proxy 几何不可信，记录拦截原因但不执行 rescue
        rescue_blocked_reason = "proxy_geometry_not_trusted"
    elif _is_rescue_candidate(content_score, np_threshold, rescue_band_spec):
        geo_gate_applied = _check_geo_gate(content_score, geometry_score, rescue_band_spec)
        if geo_gate_applied:
            rescue_band_applied = True
            content_decision = True
```

**(2)** 审计字段扩展 — 在 audit dict 中增加：

```python
"rescue_blocked_reason": rescue_blocked_reason,
"rescue_anchor_evidence_level": geometry_evidence.get("anchor_evidence_level"),
"rescue_sync_status": sync_status_raw,
"rescue_sync_status_normalized": sync_status_normalized,
```

### 4.5 冻结约束绑定

| 约束 | 说明 |
|------|------|
| `frozen_contracts.yaml/status_enum` | **不可修改** — 新增的字段不是 status |
| `records_schema_extensions.yaml` | 需 **append**：`decision.rescue_blocked_reason`（layer: diagnostic, type: reason_string）、`decision.rescue_anchor_evidence_level`（diagnostic, string）、`decision.rescue_sync_status`（diagnostic, string）、`decision.rescue_sync_status_normalized`（diagnostic, string） |
| `frozen_contracts.yaml/field_paths_registry` | append 上述三项 |
| `fusion_rule_digest` 计算 | `rescue_blocked_reason` **必须**加入 `fusion_rule_payload`，导致 digest 变化（正确行为）；经真实代码确认，当前 `fusion_rule_payload` 构造在步骤 (8)，需在步骤 (7) 完成后将 `rescue_blocked_reason` 传入 |

### 4.6 不可修改区域

- `FusionDecision` dataclass 定义
- `FUSION_RULE_ID`, `FUSION_RULE_VERSION`, `RESCUE_BAND_VERSION` 常量
- `validate_fusion_inputs()` 逻辑
- `_is_rescue_candidate()` 逻辑
- `get_np_threshold()` / `select_np_threshold_from_artifact()` 签名

### 4.7 测试验证

- **单元测试**：`test_rescue_blocked_when_anchor_is_proxy` — `anchor_evidence_level="proxy"` + `sync_status="absent"` → rescue 不触发，`rescue_blocked_reason == "proxy_geometry_not_trusted"`
- **单元测试**：`test_rescue_allowed_when_anchor_is_authentic` — `anchor_evidence_level="authentic"` → rescue 正常触发
- **单元测试**：`test_rescue_allowed_when_sync_status_ok` — `anchor_evidence_level="proxy"` 但 `sync_status="ok"` → rescue 正常触发
- **回归测试**：现有 `test_align_fail_geo_score_absent` 应自动通过（geometry absent 时 rescue 本就不触发）

### 4.8 预期成果

- 消除 proxy 几何证据翻转 NP 主决策的风险
- 审计链可追溯：每次 rescue 拦截都有 `rescue_blocked_reason`
- 论文"辅锚点仅在主同步路径成功后参与"在策略层闭环

---

## S5：无 PSNR/SSIM/FID 图像质量指标

### 5.1 当前问题

metrics.py 仅计算 TPR、FPR、混淆矩阵、reject_rate 等检测指标。无 PSNR、SSIM、FID、LPIPS 等图像质量指标。论文需要证明"保持高生成质量"。

### 5.2 修复目标

- 新增 PSNR、SSIM 计算（纯 NumPy 实现；SSIM 的高斯窗口卷积可选使用 `scipy.ndimage` 加速，若 scipy 不可用则走纯 NumPy 降级路径）
- FID/LPIPS 作为可选扩展（需 torch + 预训练模型），标记为 `optional_quality_metrics`
- 图像质量指标独立于检测指标，不影响现有的 `compute_overall_metrics`

### 5.3 需要新增/修改的函数

| 函数 | 操作 | 文件 |
|------|------|------|
| `compute_psnr(img_original, img_watermarked) → float` | **新增** | main/evaluation/image_quality.py（新文件） |
| `compute_ssim(img_original, img_watermarked, win_size=7) → float` | **新增** | image_quality.py |
| `compute_quality_metrics_batch(pairs) → Dict` | **新增** | image_quality.py |
| `aggregate_metrics(...)` | **修改** | metrics.py — 增加 `quality_metrics` 段 |

### 5.4 实施步骤

**(1)** 新建 `main/evaluation/image_quality.py`：

```python
"""
File purpose: 图像质量指标计算（PSNR / SSIM / 可选 FID）。
Module type: General module
"""

def compute_psnr(img_original, img_watermarked, max_val=255.0):
    """
    功能：计算 Peak Signal-to-Noise Ratio。

    Compute PSNR between original and watermarked image.

    Args:
        img_original: Original image as numpy array, shape [H, W, C] or [H, W].
        img_watermarked: Watermarked image, same shape.
        max_val: Maximum pixel value (255.0 for uint8).

    Returns:
        PSNR value in dB. Returns float('inf') if images are identical.

    Raises:
        ValueError: If shapes do not match.
    """
    ...

def compute_ssim(img_original, img_watermarked, win_size=7, data_range=255.0):
    """
    功能：计算 Structural Similarity Index。

    Compute SSIM using sliding window approach (Wang et al., 2004).

    Args:
        img_original: Original image, shape [H, W] or [H, W, C].
        img_watermarked: Watermarked image, same shape.
        win_size: Gaussian window size (odd integer).
        data_range: Dynamic range of pixel values.

    Returns:
        Mean SSIM value in [0, 1].

    Raises:
        ValueError: If shapes mismatch or win_size is even.
    """
    ...

def compute_quality_metrics_batch(image_pairs):
    """
    功能：批量计算图像对的质量指标。

    Compute PSNR and SSIM for a batch of (original, watermarked) image pairs.

    Args:
        image_pairs: List of (original_array, watermarked_array) tuples.

    Returns:
        Dict with keys: mean_psnr, mean_ssim, per_image (list of individual scores).
    """
    ...
```

**(2)** SSIM 实现要点：
- 高斯窗口：`_gaussian_kernel(win_size, sigma=1.5)`
- 局部均值/方差优先使用 `scipy.ndimage.uniform_filter`（若 scipy 可用），否则降级为纯 NumPy 手动卷积
- 公式：`SSIM(x,y) = (2*μx*μy + C1)(2*σxy + C2) / ((μx²+μy² + C1)(σx²+σy² + C2))`
- 多通道：逐通道计算取均值

**(3)** metrics.py 修改 `aggregate_metrics()`：
- 若 `records_manifest` 包含 `image_pairs` 字段，调用 `compute_quality_metrics_batch`
- 将结果放入返回 dict 的 `"quality_metrics"` key 下
- 不影响 `metrics_overall` 和 `metrics_by_attack_condition`

### 5.5 冻结约束绑定

| 约束 | 说明 |
|------|------|
| `frozen_contracts.yaml` | image_quality.py 不涉及 IO write、digest、impl_id — **无冻结约束** |
| `records_schema_extensions.yaml` | 可选 **append**：`quality_metrics.mean_psnr`、`quality_metrics.mean_ssim`（layer: diagnostic, type: score_float） |
| metrics.py 既有逻辑 | `compute_overall_metrics()` 和 `compute_attack_group_metrics()` 签名和返回结构 **不可修改** |

### 5.6 不可修改区域

- metrics.py 的 `compute_overall_metrics()` 函数签名、返回 dict 的已有 key
- metrics.py 的 `compute_attack_group_metrics()` 函数签名和分组逻辑
- `reject_count_by_reason` dict 的已有 key

### 5.7 测试验证

- **单元测试**：`test_psnr_identical_images_returns_inf` — 相同图像 → PSNR == inf
- **单元测试**：`test_psnr_known_value` — 已知 MSE → 验证 PSNR 精度
- **单元测试**：`test_ssim_identical_images_returns_one` — 相同图像 → SSIM ≈ 1.0
- **单元测试**：`test_ssim_shape_mismatch_raises` — 形状不匹配 → ValueError
- **回归测试**：`test_aggregate_report_has_all_anchors` 不应受影响

### 5.8 预期成果

- 论文"保持高生成质量"可在评测阶段量化
- PSNR/SSIM 为纯 NumPy 实现（可选 scipy 加速），无强制外部依赖
- 不影响现有检测指标系统

---

## S6：无 AUC/ROC 曲线

### 6.1 当前问题

metrics.py 仅在单一 NP 阈值下计算 TPR/FPR。无多阈值扫描、无 ROC 曲线、无 AUC 值。论文需要标准化的 ROC/AUC 报告。

### 6.2 修复目标

- 新增 `compute_roc_curve(records) → (fpr_list, tpr_list, thresholds)`
- 新增 `compute_auc(fpr_list, tpr_list) → float`
- 可在 `aggregate_metrics` 中可选输出 ROC/AUC

### 6.3 需要新增/修改的函数

| 函数 | 操作 | 文件 |
|------|------|------|
| `compute_roc_curve(records) → Tuple` | **新增** | metrics.py |
| `compute_auc(fpr_list, tpr_list) → float` | **新增** | metrics.py |
| `aggregate_metrics(...)` | **修改** | metrics.py — 增加 `roc_auc` 段 |

### 6.4 实施步骤

**(1)** 新增 `compute_roc_curve()`：

```python
def compute_roc_curve(records):
    """
    功能：计算 ROC 曲线（多阈值扫描）。

    Compute ROC curve by sweeping thresholds over all score values.

    Args:
        records: List of detection record dicts.

    Returns:
        Tuple of (fpr_list, tpr_list, thresholds) where:
        - fpr_list: List of FPR values (descending threshold → ascending FPR).
        - tpr_list: List of TPR values.
        - thresholds: Sorted unique threshold values used.

    Raises:
        TypeError: If records is not a list.
    """
    # (1) 提取所有有效 (score, label) 对
    # (2) 对 score 去重排序作为阈值候选
    # (3) 每个阈值计算 TPR/FPR
    # (4) 返回三元组
```

**(2)** 新增 `compute_auc()`：

```python
def compute_auc(fpr_list, tpr_list):
    """
    功能：使用梯形法计算 AUC。

    Compute Area Under ROC Curve using trapezoidal rule.

    Args:
        fpr_list: FPR values (must be sorted ascending or matched with tpr_list).
        tpr_list: TPR values.

    Returns:
        AUC value in [0, 1].

    Raises:
        ValueError: If lists have different lengths or are empty.
    """
    # 梯形积分：AUC = Σ (fpr[i+1] - fpr[i]) * (tpr[i+1] + tpr[i]) / 2
```

**(3)** 修改 `aggregate_metrics()`：增加可选 ROC/AUC 计算：

```python
# 在返回 dict 中增加
roc_fpr, roc_tpr, roc_thresholds = compute_roc_curve(records)
auc_value = compute_auc(roc_fpr, roc_tpr) if roc_fpr else None
result["roc_auc"] = {
    "auc": auc_value,
    "roc_curve_points": len(roc_fpr),
    # ROC 原始数据可选输出（点数可能很大，按需截断）
}
```

### 6.5 冻结约束绑定

| 约束 | 说明 |
|------|------|
| `frozen_contracts.yaml` | ROC/AUC 为纯计算，不涉及 IO write/digest — **无冻结约束** |
| `aggregate_metrics()` 返回 dict | 已有 key（`metrics_overall`, `breakdown`, `metrics_by_attack_condition`）**不可删除**，`roc_auc` 为新增 key |

### 6.6 不可修改区域

- `compute_overall_metrics()` 签名和返回结构
- `compute_attack_group_metrics()` 签名和逻辑
- `aggregate_metrics()` 已有返回 key

### 6.7 测试验证

- **单元测试**：`test_roc_curve_perfect_classifier` — 完美分类器 → AUC == 1.0
- **单元测试**：`test_roc_curve_random_classifier` — 随机分类器 → AUC ≈ 0.5
- **单元测试**：`test_auc_trapezoid_known_value` — 已知 FPR/TPR 序列 → 验证梯形积分精度
- **单元测试**：`test_roc_empty_records_returns_empty` — 空记录 → 空列表
- **回归测试**：`test_aggregate_report_has_all_anchors` 不应受影响

### 6.8 预期成果

- 论文可报告标准 AUC 值和 ROC 曲线
- 不同攻击条件下的 TPR-FPR 权衡可视化
- 与 NP 单阈值指标互补

---

## S7：几何同步模板无 Embed 侧注入

### 7.1 当前问题

`LatentSyncTemplate` 为 detect-only FFT 提取器。embed orchestrator 设置 `geometry_chain_status="absent"`。没有 `embed_inject()` 或 `inject_template()` 方法。论文描述"在 latent 空间引入轻量级空间同步模块"需要 embed 侧注入。

### 7.2 修复目标

- 新增 `LatentSyncTemplate.embed_inject(latents, cfg) → (modified_latents, inject_trace)`
- embed 侧在 latent 空间特定频率 bin 注入可检测同步信号
- detect 侧 `extract_sync()` 检测该信号完成空间同步
- embed orchestrator 从 `geometry_chain_status="absent"` 变为可选 `"ok"`

### 7.3 需要新增/修改的函数

| 函数 | 操作 | 文件 |
|------|------|------|
| `LatentSyncTemplate.embed_inject(self, latents, cfg, seed) → Tuple` | **新增** | latent_sync_template.py |
| `LatentSyncTemplate._build_sync_template(self, shape, cfg, seed) → np.ndarray` | **新增** | latent_sync_template.py |
| `LatentSyncTemplate.extract_sync(...)` | **修改** | 增加对 embed 注入信号的检测匹配 |
| Embed orchestrator 调用点 | **修改** | 上层 orchestrator 需要调用 `embed_inject` |
| `resolve_enable_latent_sync(cfg)` | 保留 | 已支持 embed.geometry.enable_latent_sync |

### 7.4 实施步骤

**(1)** 新增 `embed_inject()` 方法：

```python
def embed_inject(self, latents, cfg, seed):
    """
    功能：在 latent 空间注入 FFT 域同步模板。

    Inject sync template into latent frequency domain for geometric sync.

    Args:
        latents: Input latent tensor/array, shape [B, C, H, W] or [C, H, W].
        cfg: Configuration mapping.
        seed: Deterministic seed for template generation.

    Returns:
        Tuple of (modified_latents, inject_trace).

    Raises:
        TypeError: If inputs are invalid.
        ValueError: If latent shape is unsupported.
    """
    # (1) 验证输入与开关
    if not resolve_enable_latent_sync(cfg):
        return latents, {"status": "absent", "reason": "embed_sync_disabled"}
    
    # (2) 构建同步模板（FFT 域）
    template = self._build_sync_template(latents.shape, cfg, seed)
    
    # (3) 将 latents 变换到频率域
    latents_fft = np.fft.fft2(latents_np)
    
    # (4) 在指定 bin 注入模板（加性叠加，强度由 cfg 控制）
    sync_strength = cfg.get("embed", {}).get("geometry", {}).get("sync_strength", 0.01)
    latents_fft += template * sync_strength
    
    # (5) IFFT 恢复
    modified_latents = np.real(np.fft.ifft2(latents_fft))
    
    # (6) 构建 trace
    inject_trace = {
        "status": "ok",
        "sync_inject_strength": sync_strength,
        "template_digest": digests.canonical_sha256({"shape": list(template.shape), "seed": seed}),
        "sync_config_digest": digests.canonical_sha256(self._build_sync_config_domain(cfg)),
    }
    return modified_latents, inject_trace
```

**(2)** 新增 `_build_sync_template()`：

```python
def _build_sync_template(self, shape, cfg, seed):
    """
    功能：生成确定性 FFT 域同步模板。

    Generate deterministic sync template in FFT domain.

    Args:
        shape: Target latent shape.
        cfg: Config for bin counts.
        seed: RNG seed.

    Returns:
        Complex template array, same spatial shape as latent.
    """
    # 使用固定 seed 在特定频率 bin 放置标记信号
    # bin 选择：sync_fft_bins（默认 16）个位置
    # 每个 bin 的相位由 seed 确定 → detect 侧用相同 seed 重建匹配
    fft_bins = self._resolve_fft_bins(cfg)
    rng = np.random.RandomState(seed)
    template = np.zeros(shape[-2:], dtype=complex)
    # 在对称位置放置已知相位信号
    for _ in range(fft_bins):
        row = rng.randint(1, shape[-2] // 2)
        col = rng.randint(1, shape[-1] // 2)
        phase = rng.uniform(0, 2 * np.pi)
        template[row, col] = np.exp(1j * phase)
        template[-row, -col] = np.exp(-1j * phase)  # 共轭对称
    return template
```

**(3)** 修改 `extract_sync()`：
- 在 FFT 提取后，计算检测到的频率 bin 与 embed-side template 的相关性
- `sync_quality` 基于相关性强度判定

**(4)** 修改上层 embed orchestrator（`main/watermarking/embed/orchestrator.py`）——两处必须同时修改：

- **调用点**：在 `_run_sync_module()` 之后，若 `resolve_enable_latent_sync(cfg)` 为真，调用 `latent_sync_template.embed_inject(latents, cfg, seed)`，并将返回的 `inject_trace` 合并到 `sync_result`
- **写回点**：`execution_report["geometry_chain_status"]` 目前硬编码为 `"absent"`（L274），需改为依据 `sync_result` 的 status 动态赋值：
  ```python
  # 当前：硬编码
  "geometry_chain_status": "absent",
  # 修改为：
  "geometry_chain_status": sync_result.get("status", "absent") if isinstance(sync_result, dict) else "absent",
  ```
- **不可修改 `sync_result` 字段语义**：`SyncResult` frozen dataclass 字段不变，仅修改 `_run_sync_module()` 返回 dict 的内容

> **注意**：仅新增 `embed_inject()` 方法而不修改 orchestrator 的写回路径，`geometry_chain_status` 将仍然永久为 `"absent"`，修复无法生效。

### 7.5 冻结约束绑定

| 约束 | 说明 |
|------|------|
| `runtime_whitelist.yaml/impl_id` | `geometry_latent_sync_sd3_v2` 已在白名单（content_extractor、geometry_extractor、sync_module 三域），**无需修改** |
| `injection_scope_manifest.yaml` | 需 **append** `embed.geometry.sync_strength` 到 `plan_digest_include_paths`（注入强度变化 → plan_digest 变化） |
| `records_schema_extensions.yaml` | 需 **append**：`geometry_evidence.sync_inject_status`（diagnostic, string）、`geometry_evidence.sync_inject_strength`（diagnostic, score_float）、`geometry_evidence.template_digest`（anchor, digest_hex64） |
| `frozen_contracts.yaml/field_paths_registry` | append 上述三项 |
| `policy_path_semantics.yaml` | `content_with_geometry_gate` 的 `required_chains` 已包含 geometry — embed 侧 geometry ok 后不影响路由 |

### 7.6 不可修改区域

- `SyncResult`, `SyncRuntimeContext` frozen dataclass 字段
- `LatentSyncTemplate.__init__()` 签名
- `extract_sync()` 签名（`pipeline, latents, *, cfg, rng`）
- `resolve_enable_latent_sync()` 逻辑（已支持 embed 侧）
- `compute_sync_digest()` 算法

### 7.7 测试验证

- **单元测试**：`test_embed_inject_modifies_latents` — 注入后 latents != 原始 latents
- **单元测试**：`test_embed_inject_disabled_returns_original` — 开关关闭时返回原始 latents + absent status
- **单元测试**：`test_sync_template_is_deterministic` — 相同 seed → 相同 template_digest
- **单元测试**：`test_embed_detect_roundtrip_correlation` — embed 注入 → extract_sync 检测 → 相关性 > 阈值
- **回归测试**：现有 `test_anchor_gate_by_sync_status` 不应受影响（sync_status 语义不变）

### 7.8 预期成果

- embed 侧可在 latent 空间注入 FFT 同步模板
- detect 侧通过频率域相关性验证几何同步
- 论文"在 Latent 空间中实现轻量级空间同步模块"在代码层面闭环
- `geometry_chain_status` 从永久 `"absent"` 变为条件 `"ok"` / `"absent"`

---

## 配置文件变更汇总

以下为所有 S1–S7 修复所需的配置文件 **append-only** 变更。**禁止修改已有条目。**

### records_schema_extensions.yaml — 新增字段

```yaml
# S1
- path: "content_evidence.mask_stats.model_artifact_anchor.weights_sha256"
  layer: "anchor"
  type: digest_hex64

# S2（anchor_evidence_level 已注册于 L403，无需重复追加）
- path: "geometry_evidence.attention_capture_source"
  layer: "diagnostic"
  type: string

# S3
- path: "content_evidence.bp_converged"
  layer: "diagnostic"
  type: boolean_flag
- path: "content_evidence.bp_iteration_count"
  layer: "diagnostic"
  type: integer

# S4
- path: "decision.rescue_blocked_reason"
  layer: "diagnostic"
  type: reason_string
- path: "decision.rescue_anchor_evidence_level"
  layer: "diagnostic"
  type: string
- path: "decision.rescue_sync_status"
  layer: "diagnostic"
  type: string
- path: "decision.rescue_sync_status_normalized"
    layer: "diagnostic"
    type: string

# S7
- path: "geometry_evidence.sync_inject_status"
  layer: "diagnostic"
  type: string
- path: "geometry_evidence.sync_inject_strength"
  layer: "diagnostic"
  type: score_float
- path: "geometry_evidence.template_digest"
  layer: "anchor"
  type: digest_hex64
```

### injection_scope_manifest.yaml — plan_digest_include_paths 追加

```yaml
# S1
- watermark.mask.semantic_model_path

# S3（ecc_sparsity 影响 H 矩阵结构，必须纳入 plan_digest）
- watermark.lf.ecc_sparsity

# S7
- embed.geometry.sync_strength
```

### frozen_contracts.yaml — field_paths_registry 追加

与 records_schema_extensions.yaml 新增字段保持一致（同步 append）。

### runtime_whitelist.yaml

**无需修改** — 所有既有 impl_id 和枚举已覆盖。

---

## 新文件清单

| 文件 | 说明 | S 编号 |
|------|------|--------|
| `main/watermarking/content_chain/ldpc_codec.py` | LDPC 编解码器 | S3 |
| `main/evaluation/image_quality.py` | 图像质量指标（PSNR/SSIM） | S5 |
| `tests/test_ldpc_codec_roundtrip.py` | LDPC 编解码单元测试 | S3 |
| `tests/test_image_quality_metrics.py` | 图像质量指标测试 | S5 |
| `tests/test_roc_auc_computation.py` | ROC/AUC 测试 | S6 |
| `tests/test_rescue_evidence_level_gate.py` | Rescue 证据等级门控测试 | S4 |
| `tests/test_semantic_mask_v2_model.py` | 语义掩码 v2 模型测试 | S1 |
| `tests/test_attention_hook_capture.py` | Attention hook 捕获测试 | S2 |
| `tests/test_sync_embed_inject.py` | 几何同步 embed 注入测试 | S7 |

---

## 优先级与依赖

```
S4 (LOW, 无依赖) ─── 可立即实施
S5 (MED, 无依赖) ─── 可立即实施
S6 (LOW, 无依赖) ─── 可立即实施

S1 (MED, 需 torch) ── 需确定 saliency 模型选型
S3 (HIGH, 无外部依赖) ── 纯算法实现
S7 (MED-HIGH, 依赖 S2 部分) ── embed 注入独立于 attention hook

S2 (HIGH, 需 SD3 transformer 结构知识) ── R1 运行时验证后再固化
```

**推荐实施顺序**：S4 → S5 → S6 → S3 → S1 → S7 → S2

理由：
1. S4/S5/S6 为低风险外科手术，不改变核心数据流，可快速提交
2. S3 为纯算法实现，可独立验证；但须先完成前置步骤：统一 `ecc` 参数类型
3. S1 模型选择优先 BASNet，保留 InSPyReNet 模块接口以备扩展
4. S7 的 embed 注入与 detect 提取是对称结构，可独立开发；**但必须先明确 embed orchestrator 写回路径（`geometry_chain_status` 动态赋值 + `sync_result` 同步）后再编码**
5. S2 依赖 SD3 transformer 的实际 attention layer 命名，建议 R1 运行验证后再固化 hook 层名；**S2 必须先完成 attention 输出契约定义（collect() 输出 stacked tensor + extractor 适配）后再提交**

**实施前必须执行的全局前置条件**：
- 确认 `injection_scope_manifest.yaml` 已 append `watermark.mask.semantic_model_path`、`watermark.lf.ecc_sparsity`、`embed.geometry.sync_strength`
- 确认 `records_schema_extensions.yaml` 和 `frozen_contracts.yaml` 的 `field_paths_registry` 已同步 append 本方案所有新增字段
- 所有新 status 字面量不得超出 `{ok, failed, mismatch, absent}` 枚举范围

**基于 2026-03-01 代码实读的落地提醒（必须先完成）**：
- 当前仓库尚未看到 `injection_scope_manifest.yaml` 中上述 3 条 `plan_digest_include_paths` 追加项；请先补齐，否则后续实现无法满足 digest 绑定闭环（定位：`injection_scope_manifest.yaml:62`）。
- 当前仓库尚未看到 `records_schema_extensions.yaml` 与 `frozen_contracts.yaml/field_paths_registry` 对本方案新增字段的同步追加；请先补齐 schema 注册再改代码，避免 freeze gate 拦截（定位：`frozen_contracts.yaml:394`，`records_schema_extensions.yaml` 对应新增段）。

---

## 新增风险与退出准则（补充）

本方案在修复既有差距的同时，会引入可控的实施风险。为避免“修复后产生新隐患”，新增以下风险登记与退出准则。

### R1：S2 运行时捕获带来的资源与稳定性风险

**风险描述**：Q/K 捕获与 attention 重建可能增加显存占用、推理时延，且存在 step 对齐错误风险。  
**控制措施**：
- 默认启用采样策略（`max_layers`、`sample_steps`），禁止全层全步默认采集。
- `collect()` 仅输出收敛后的 stacked tensor，禁止未收敛结构进入 extractor。
- 推理异常时必须降级为 `proxy` 路径，不得中断主流程。

**退出准则（满足全部才可关闭）**：
1. 在固定输入与 seed 下，`anchor_evidence_level` 可稳定输出 `authentic` 或受控回退为 `proxy`，无随机漂移。
2. S2 新增路径不引入 `status` 新字面量，仍严格在 `{ok, failed, mismatch, absent}` 内。
3. 关键回归（content-only 与 content+geometry）均可通过，不出现 freeze gate 新拦截。

### R2：S1/S2/S7 新字段引入后的审计漏绑定风险

**风险描述**：新增字段若未完成 schema 注册与 digest 绑定，可能出现“实现变化不可审计”。  
**控制措施**：
- 先 append `records_schema_extensions.yaml` 与 `frozen_contracts.yaml/field_paths_registry`，再落代码。
- 新参数必须先 append 到 `injection_scope_manifest.yaml/plan_digest_include_paths`，再进入运行路径。

**退出准则（满足全部才可关闭）**：
1. 新增字段在 schema 与 frozen registry 中一一对应，无孤儿字段。
2. 变更新增参数时，`plan_digest` 与相关 evidence digest 能同步变化。
3. 白名单与冻结检查链路全绿（含既有审计脚本）。

### R3：S3 双语义兼容期的一致性风险

**风险描述**：`ecc`（string/int）过渡期若边界不清，会造成 embed/detect 分支行为不一致。  
**控制措施**：
- 明确分支规则：`ecc == "sparse_ldpc"` 走 LDPC；`ecc` 为 int 走兼容旧分支并标记 deprecation。
- 在同一配置下，embed 与 detect 必须使用同一分支判定结果。

**退出准则（满足全部才可关闭）**：
1. `ecc` 两种输入语义均有覆盖测试，并能稳定产出预期分支。
2. 兼容分支存在明确退场条件（版本里程碑 + 配置迁移完成标准）。
3. 未出现“同配置不同路径”的 digest 不一致问题。

### R4：S4/S5/S6 指标与决策口径耦合风险

**风险描述**：新增评估指标（PSNR/SSIM/ROC/AUC）若误入决策链，可能改变 Neyman-Pearson 主控语义。  
**控制措施**：
- 明确“报告指标”与“决策指标”边界：新增指标仅用于评估输出，不直接参与最终判决阈值。
- `decision.conditional_fpr_notes` 同步记录口径，避免文档与实现偏离。

**退出准则（满足全部才可关闭）**：
1. 融合判决仅依赖既定主控分数与门控字段，不读取新增评估指标。
2. ROC/AUC 结果可复算，但不改变在线判决行为。
3. 评估报告字段与冻结语义一致，无口径漂移。

### 新增差距项登记规则（治理补充）

若出现以下任一情形，登记为“新增治理差距项”（不与 S1-S7 核心算法差距混记）：
- 新增字段未完成 append-only 注册；
- 新增参数未纳入 digest include paths；
- 过渡分支缺少退场条件；
- 默认采样策略未给出可复现实验基线。

建议编号为 `G1/G2/...` 并单独跟踪，以免污染 S1-S7 的核心修复闭环。

### R1-R4 检查点映射（现有 tests / scripts）

以下映射用于“立即可执行”的回归与审计收口；其中“缺口”列表示当前仓库尚未覆盖、需按本方案补充的最小项。

| 风险项 | 现有测试（可直接复用） | 现有脚本/审计（可直接复用） | 当前缺口（需补充） |
|------|------|------|------|
| R1（S2 运行时捕获稳定性） | `tests/test_anchor_gate_by_sync_status.py`；`tests/test_attention_anchor_digest_discipline.py`；`tests/test_attention_anchor_extractor_no_bypass_write.py`；`tests/test_attention_consistency_only_after_align_success.py` | `scripts/run_all_audits.py`（含 `audits/audit_runtime_impl_smoke_new_saliency_and_subspace.py`）；`scripts/run_freeze_signoff.py` | 缺少针对“Q/K 捕获 + 重建”主路径的专用回归（如 `test_attention_hook_capture.py`） |
| R2（字段注册与 digest 绑定） | `tests/test_records_schema_append_only_fields.py`；`tests/test_new_params_must_be_in_digest_or_fail.py`；`tests/test_plan_digest_append_only_and_recomputable.py`；`tests/test_align_impl_whitelist_enforcement.py` | `scripts/audits/audit_records_fields_append_only.py`；`scripts/audits/audit_injection_scope_manifest_binding.py`；`scripts/verify_whitelist_config.py` | 缺少对本次新增字段集合的“逐字段一致性”专用断言（建议新增 schema 对照测试） |
| R3（S3 双语义兼容一致性） | `tests/test_lf_coder_plan_digest_binding.py`；`tests/test_lf_coder_plan_digest_mismatch.py`；`tests/test_lf_coder_disabled_returns_absent.py` | `scripts/run_all_audits.py`（通过总体门禁回归） | 缺少 `ecc=\"sparse_ldpc\"` 与 `ecc=int` 双路径同配置一致性测试；缺少兼容分支退场条件测试 |
| R4（评估指标与决策口径解耦） | `tests/test_threshold_source_and_rescue_reason_semantics.py`；`tests/test_fusion_rescue_audit_trail.py`；`tests/test_metrics_group_key_canonicalized.py` | `scripts/audits/audit_evaluation_report_schema.py`；`scripts/run_freeze_signoff.py` | 缺少 PSNR/SSIM 与 ROC/AUC 专用测试（方案中的 `test_image_quality_metrics.py`、`test_roc_auc_computation.py`） |

### 最小执行清单（对应 R1-R4）

1. 先跑审计聚合：`python scripts/run_all_audits.py --repo-root . --output audit_report.json`  
2. 再跑冻结签署前检查：`python scripts/run_freeze_signoff.py --repo-root . --profile paper --strict`  
3. 最后跑定向测试集（R1-R4 相关）：`pytest tests/ -k "attention or sync_status or digest or whitelist or rescue or metrics" -q`

若步骤 1-3 全部通过，再进入本方案新增测试（S2/S3/S5/S6）的补齐阶段。

---

> 本方案已基于真实代码读取完成校准合并（2026-03-01），各 S 节已直接融入所有校准约束，不再设立补充节。
