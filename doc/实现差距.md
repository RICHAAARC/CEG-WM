结论：**目前仍不符合你的研究预期（未完全达成论文目标）**。

已确认的进展：
- 多协议矩阵已能跑完：`outputs/experiment_matrix/artifacts/grid_summary.json`（`8/8 ok`）。
- `detect->calibrate` 前置门禁已接入：`main/evaluation/experiment_matrix.py:667`，并保持 calibrate 严格样本要求：`main/watermarking/detect/orchestrator.py:2513`。
- 冻结/审计有提升：`scripts/audits/audit_records_fields_append_only.py` 当前结果为 `PASS`。
- ablation 新开关已进入 whitelist：`configs/runtime_whitelist.yaml:159`。

关键差距（按严重性）：
1. `BLOCK` 几何链在实跑中整体失效  
- 8/8 样本 `geometry_evidence_payload.status=failed`，错误为 `relation_graph_failed: ... tuple`。  
- 代码根因对应：`main/watermarking/geometry_chain/attention_anchor_extractor.py:710`（`top_k_edges` 用 tuple），失败抛出处：`main/watermarking/geometry_chain/attention_anchor_extractor.py:612`。  
- 结果侧证据：`outputs/experiment_matrix/experiments/item_0000_c797eab7d856/artifacts/evaluate_inputs/detect_record_with_attack.json`。

2. `BLOCK` 当前量化并非真实模型主链，属于 fallback/shell 运行  
- `detect_runtime_mode` 全部是 `fallback_identity_v0`：`main/watermarking/detect/orchestrator.py:523`。  
- 产物显示 `synthetic_pipeline=true`、`shell_mode_without_real_model`：`main/diffusion/sd3/pipeline_factory.py:380`。  
- 这意味着“保持高生成质量+几何鲁棒提升”的论文主结论目前证据不足。

3. `HIGH` T2SMark 对比仍是代理对比，不是独立同口径基线实验  
- 对比定义是 `hf_score_as_t2smark_proxy`：`main/evaluation/experiment_matrix.py:1046`，提取逻辑：`main/evaluation/experiment_matrix.py:259`。  
- `t2smark_comparison_table.csv` 中 8 个 attack 的 `content_score/hf_proxy` 完全相同，统计显著性很弱。  
- `grid_summary` 每项 `n_total=1`、`geo_available_rate=0.0`。

4. `HIGH` 自适应流形主证据在“当前跑通结果”里仍未形成  
- 代码里已有 real JVP 路径接口：`main/watermarking/content_chain/subspace/subspace_planner_impl.py:1690`。  
- 但仍存在 surrogate 语义路径：`main/watermarking/content_chain/subspace/subspace_planner_impl.py:2643`。  
- 当前产物中 `subspace_plan`/`plan_digest` 证据链不完整（如 `embed_record` 顶层 `plan_digest=None`）。

5. `HIGH` “真实 Self-Attention 锚点”未在当前运行中落地  
- detect 侧仍会回退到 `latent_proxy`：`main/watermarking/detect/orchestrator.py:3702`；真实路径仅在提供预置 attention 时才标记：`main/watermarking/detect/orchestrator.py:3696`。  
- 即使代码支持 `self_attention_authentic` 字段，当前实验并未形成真实性锚点证据。

6. `MEDIUM` HF/LF 仍以“等价映射声明”而非完全独立实现闭环  
- 目前是 `equivalence_mode` 标注（PRC/T2S 到 channel 映射）：`main/diffusion/sd3/infer_runtime.py:536`, `main/diffusion/sd3/infer_runtime.py:560`。  
- 这比纯 fallback 强，但仍不等价于“主链路完全由论文目标实现直接执行”。

我本次额外复核通过的测试：
- `tests/test_paper_impl_binding_consistency.py`（4 passed）
- `tests/test_sync_module_registry_latent_sync.py`（8 passed）
- `tests/test_latent_impl_binding_audit_fields.py`（3 passed）

所以当前状态是：**工程合规性与可运行性明显提升，但论文主张（真实流形证据、真实注意力锚点、几何鲁棒主证据、超越 T2SMark 的有效量化）仍未闭环。**




你现在执行“论文目标闭环实现（强制验收版）”。必须读取真实仓库代码、修改代码、自动测试、自动审计、自动跑全量实验；禁止只输出方案。

【0. 强制前提（不满足即直接失败并停止）】
1. 如果需要进行真实模型 + 真实 CUDA 跑全量 workflow，你需要明确告知我，我将在 colab 进行  workflow 后将结果文件交给你。
2. 若任一记录出现 `pipeline_runtime_meta.synthetic_pipeline=true`，本轮结果无效。
3. 若任一 detect 记录出现 `detect_runtime_mode != "real"`，本轮结果无效。
4. 若几何链主实验 `geo_available_rate == 0`，本轮结果无效。
5. 若仍使用 `hf_score_as_t2smark_proxy` 作为最终“超越 T2SMark”证据，本轮结果无效。

【1. 先读代码，再输出计划（未完成不得改代码）】
先读取：
- `configs/`（冻结约束与白名单）
- `main/`（主实现）
- `tests/`（回归与门禁）
- `scripts/audits/`（审计脚本）

然后输出四个清单：
1. 必改文件清单（路径级）
2. 需同步改动清单（实现/配置/schema/tests/audits）
3. 禁止改动清单
4. 风险点与硬约束清单

【2. 必改范围（先核对后执行）】
- `main/watermarking/geometry_chain/attention_anchor_extractor.py`
- `main/watermarking/geometry_chain/sync/latent_sync_template.py`
- `main/watermarking/detect/orchestrator.py`
- `main/watermarking/content_chain/subspace/subspace_planner_impl.py`
- `main/watermarking/content_chain/latent_modifier.py`
- `main/diffusion/sd3/infer_runtime.py`
- `main/diffusion/sd3/pipeline_factory.py`
- `main/evaluation/experiment_matrix.py`
- `main/core/config_loader.py`
- `main/registries/runtime_resolver.py`
- `configs/runtime_whitelist.yaml`
- `configs/frozen_contracts.yaml`（若新增字段）
- 对应 `tests/` 与 `scripts/audits/` 同步更新

【3. 禁止改动】
- `outputs/` 既有历史产物（可新建 run_root，不可覆盖历史结论）
- 与本任务无关模块
- 通过放松门禁、硬编码分数、关闭校验来“伪通过”

【4. 模块职责与必须实现的方法/函数】
1. 几何锚点模块：
- 在 `AttentionAnchorMapRelation._build_relation_graph()` 修复序列化失败（tuple->JSON-safe list/dict）。
- 新增/完善 JSON-safe 辅助函数（命名风格保持 snake_case）。
- `extract()` 必须在真实 attention 输入下输出 `self_attention_authentic=true` 的证据链。
2. detect 几何链：
- 在 `detect/orchestrator.py` 的几何 runtime_inputs 构建路径中，优先接入真实 self-attention maps。
- latent_proxy 仅允许 fallback，且不能进入论文主结果统计。
3. 子空间 planner：
- 在 `subspace_planner_impl.py` 中确保主路径可落到 `real_unet` / `runtime_operator` JVP 证据。
- surrogate 仅 fallback，必须写入 `sample_semantics`/`surrogate_reason` 且在主结果排除。
4. HF/LF 主链路：
- `latent_modifier` + `infer_runtime` 中实现 HF/LF 主调用闭环，保留 fallback 但严格审计。
- `lf_impl_binding` / `hf_impl_binding` 必须可区分 `primary` / `primary_equivalent` / `adapter_fallback`。
5. 实验编排与对比：
- `experiment_matrix.py` 中保留 detect->calibrate 有效样本门禁。
- 新增“真实 T2SMark 基线运行与对比”路径，禁止最终仅用 proxy 表。

【5. 冻结边界与门禁绑定（必须逐条满足）】
1. append-only：
- 新增 records 字段必须登记 schema 与冻结注册表；审计脚本必须 PASS。
2. whitelist：
- 新增 override/arg_name 必须登记 `runtime_whitelist.yaml` 且与代码一致。
3. digest：
- 保持 `impl_set_capabilities_digest` 兼容；
- 并行维护 `impl_set_capabilities_v2_digest`，新能力进入 v2。
4. ablation：
- `enable_sync/enable_anchor/enable_attention_proxy/enable_image_sidecar` 全部纳入 normalized 与审计。
5. calibrate 输入门禁：
- 仅接受 `status=ok` 且数值 `content_score` 样本，禁止放宽规则。

【6. 高风险硬约束】
1. 禁止把 surrogate/proxy 伪装成论文主证据。
2. 禁止硬编码 score/threshold。
3. 禁止通过关闭 `paper_faithfulness` 作为最终交付方案。
4. 任何接口变更必须同步改：实现、配置、注册、测试、审计。

【7. 四阶段执行（每阶段后必须审计）】
阶段1：运行闭环与门禁稳定
- 修复几何链序列化失败、detect/calibrate 门禁、真实运行链路激活。
- 审计输出：通过项/失败项/阻塞项。

阶段2：子空间真实证据闭环
- 完成 real JVP 主路径与证据级别分类，surrogate 降级为 fallback。
- 审计输出：主证据占比、fallback 占比、回归测试结果。

阶段3：HF/LF 与几何主链强化
- 完成 HF/LF 主调用闭环；真实 attention 锚点 + sync 质量指标闭环。
- 审计输出：impl 绑定分布、geometry 可用率、失败语义统计。

阶段4：全量量化与封版
- 真实 CUDA 下跑全量 workflow 与 attack families。
- 输出 `grid_summary`、`aggregate_report`、真实 T2SMark 对比表。
- 跑全部相关 tests + audits，给最终“达成/未达成”结论。

【8. 最终强制验收标准（全部满足才可宣称达成论文目标）】
1. `success_count == experiment_count`。
2. 全部主结果记录：`synthetic_pipeline=false` 且 `detect_runtime_mode="real"`。
3. 几何链可用率显著大于 0，且旋转/缩放/高斯噪声三类有可解释收益。
4. 与 T2SMark 是独立基线同口径对比，不是 proxy 替代。
5. append-only/whitelist/门禁测试全部 PASS。
6. 输出可复现实验命令、关键指标、误差与风险说明。

【9. 输出格式要求】
1. 改动文件清单（必改/同步改/禁改确认）。
2. 关键函数与方法变更说明（函数名级别）。
3. 测试结果与审计结果（PASS/FAIL + 证据路径）。
4. 全量实验结果与对比结论（含是否达成论文目标）。
5. 若未达成：阻塞点、根因、下一轮最小闭环计划。