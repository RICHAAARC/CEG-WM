结论：**目前仍不符合你的研究预期（未完全达成论文目标）**。

已确认的进展：
- 多协议矩阵已能跑完：`outputs/experiment_matrix/artifacts/grid_summary.json`（`8/8 ok`）。
- `detect->calibrate` 前置门禁已接入：`main/evaluation/experiment_matrix.py:667`，并保持 calibrate 严格样本要求：`main/watermarking/detect/orchestrator.py:2513`。
- 冻结/审计有提升：`scripts/audits/audit_records_fields_append_only.py` 当前结果为 `PASS`。
- ablation 新开关已进入 whitelist：`configs/runtime_whitelist.yaml:159`。

关键差距（按严重性）：
1. `BLOCK` 几何链在实跑中整体失效  
- 8/8 样本 `geometry_evidence_payload.status=failed`，错误为 `relation_graph_failed: ... tuple`。  
- 代码根因对应：`main/watermarking/geometry_chain/attention_anchor_extractor.py:710`（`top_k_edges` 用 tuple），失败抛出处：`main/watermarking/geometry_chain/attention_anchor_extractor.py:612`。  
- 结果侧证据：`outputs/experiment_matrix/experiments/item_0000_c797eab7d856/artifacts/evaluate_inputs/detect_record_with_attack.json`。

2. `BLOCK` 当前量化并非真实模型主链，属于 fallback/shell 运行  
- `detect_runtime_mode` 全部是 `fallback_identity_v0`：`main/watermarking/detect/orchestrator.py:523`。  
- 产物显示 `synthetic_pipeline=true`、`shell_mode_without_real_model`：`main/diffusion/sd3/pipeline_factory.py:380`。  
- 这意味着“保持高生成质量+几何鲁棒提升”的论文主结论目前证据不足。

3. `HIGH` T2SMark 对比仍是代理对比，不是独立同口径基线实验  
- 对比定义是 `hf_score_as_t2smark_proxy`：`main/evaluation/experiment_matrix.py:1046`，提取逻辑：`main/evaluation/experiment_matrix.py:259`。  
- `t2smark_comparison_table.csv` 中 8 个 attack 的 `content_score/hf_proxy` 完全相同，统计显著性很弱。  
- `grid_summary` 每项 `n_total=1`、`geo_available_rate=0.0`。

4. `HIGH` 自适应流形主证据在“当前跑通结果”里仍未形成  
- 代码里已有 real JVP 路径接口：`main/watermarking/content_chain/subspace/subspace_planner_impl.py:1690`。  
- 但仍存在 surrogate 语义路径：`main/watermarking/content_chain/subspace/subspace_planner_impl.py:2643`。  
- 当前产物中 `subspace_plan`/`plan_digest` 证据链不完整（如 `embed_record` 顶层 `plan_digest=None`）。

5. `HIGH` “真实 Self-Attention 锚点”未在当前运行中落地  
- detect 侧仍会回退到 `latent_proxy`：`main/watermarking/detect/orchestrator.py:3702`；真实路径仅在提供预置 attention 时才标记：`main/watermarking/detect/orchestrator.py:3696`。  
- 即使代码支持 `self_attention_authentic` 字段，当前实验并未形成真实性锚点证据。

6. `MEDIUM` HF/LF 仍以“等价映射声明”而非完全独立实现闭环  
- 目前是 `equivalence_mode` 标注（PRC/T2S 到 channel 映射）：`main/diffusion/sd3/infer_runtime.py:536`, `main/diffusion/sd3/infer_runtime.py:560`。  
- 这比纯 fallback 强，但仍不等价于“主链路完全由论文目标实现直接执行”。

我本次额外复核通过的测试：
- `tests/test_paper_impl_binding_consistency.py`（4 passed）
- `tests/test_sync_module_registry_latent_sync.py`（8 passed）
- `tests/test_latent_impl_binding_audit_fields.py`（3 passed）

所以当前状态是：**工程合规性与可运行性明显提升，但论文主张（真实流形证据、真实注意力锚点、几何鲁棒主证据、超越 T2SMark 的有效量化）仍未闭环。**