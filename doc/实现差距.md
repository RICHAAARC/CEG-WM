**关键差距（按严重性）**

1. 几何同步 v2 在 detect 主路径里仍然无法真正工作（核心阻塞）
   - detect 侧确实改成了 `sync -> geometry extractor`（`main/watermarking/detect/orchestrator.py:3645`）。
   - 但 `_run_sync_module_for_detect()` 调 `sync_with_context()` 时只传 `SyncRuntimeContext`，没有把 `runtime_inputs`（含 `relation_digest`）传给同步模块（`main/watermarking/detect/orchestrator.py:3574`）。
   - `SyncGeometryLatentSyncSD3V2.sync_with_context()` 内部又调用 `self._extractor.extract(..., inputs=None, sync_ctx=context)`（`main/registries/geometry_registry.py:384`）。
   - 而 `GeometryLatentSyncSD3V2.extract()` 明确要求 `inputs["relation_digest"]`，缺失就返回 `relation_digest_missing_for_v2`（`main/watermarking/geometry_chain/sync/latent_sync_template.py:686`, `main/watermarking/geometry_chain/sync/latent_sync_template.py:694`）。
   - 结果：你现在的 v2 同步模块大概率只能输出 `mismatch/absent`，无法成为论文里的几何同步主证据链。

2. 几何链“attention 锚点 + sync”依赖顺序仍然不闭环
   - `attention_anchor_map_relation_v1` 才会生成 `relation_digest`（`main/watermarking/geometry_chain/attention_anchor_extractor.py:611`）。
   - 但 detect 编排是先 sync 再 extractor（`main/watermarking/detect/orchestrator.py:3645`）。
   - 当前设计下 sync 先跑时拿不到 `relation_digest`，随后 extractor 才生成 `relation_digest`，但 sync 不会重跑。
   - 这与论文目标中的“显式同步并绑定锚点关系”在工程上仍不一致。

3. 子空间 planner 虽已接入运行时 pipeline/latents 输入，但核心采样仍是代理伪状态，不是实际 latent/UNet 轨迹
   - embed/detect 编排都已把运行时 `pipeline/latents` 塞进 planner inputs（`main/watermarking/embed/orchestrator.py:572`, `main/watermarking/detect/orchestrator.py:1853`）。
   - planner 也会优先走 `pipeline` 路径（`main/watermarking/content_chain/subspace/subspace_planner_impl.py:2337`）。
   - 但 `_sample_from_diffusion_pipeline()` 内部仍是“随机伪状态 + 时间调制”，注释也写了代理状态（`main/watermarking/content_chain/subspace/subspace_planner_impl.py:2418`）。
   - 这意味着“自适应流形区域”目前更接近“可审计代理轨迹子空间”，还不是你论文叙事里的真实扩散流形/Jacobian 主路径。

4. 语义自适应子空间有实质升级，但仍未达到“流形区域”级别
   - 这是进步：你已经从“列裁剪”升级为按 `region_strengths` 加权调制（`main/watermarking/content_chain/subspace/subspace_planner_impl.py:857`, `main/watermarking/content_chain/subspace/subspace_planner_impl.py:918`）。
   - 仍有差距：mask 到特征维的映射仍基于 `downsample_grid_true_indices % feature_dim`（`main/watermarking/content_chain/subspace/subspace_planner_impl.py:838`），本质还是特征域投影的近似映射，不是 latent manifold 局部区域的直接建模。

5. HF/LF 实现 ID 已进入 runtime resolver，但 embed 主路径仍未真正使用这些实现对象（影响论文真实性与消融）
   - 已完成：`ImplIdentity` 和 `BuiltImplSet` 已纳入 `hf_embedder_id` / `lf_coder_id`（`main/registries/runtime_resolver.py:47`, `main/registries/runtime_resolver.py:203`, `main/registries/runtime_resolver.py:314`）。
   - detect 侧 HF 证据已经会使用 `impl_set.hf_embedder`（`main/watermarking/detect/orchestrator.py:776`）。
   - 但 embed 侧真实嵌入流水线仍直接调用 `encode_low_freq_dct()` / `embed_high_freq_pattern()`（`main/watermarking/embed/orchestrator.py:721`, `main/watermarking/embed/orchestrator.py:748`），并未经 `impl_set.lf_coder` / `impl_set.hf_embedder`。
   - 这会让“HF/LF 实现消融”在 embed 侧不干净。

6. 消融实验支持已有框架，但仍存在关键“开关无效/粒度不足/摘要不全”问题
   - 正向进展：
   - embed/detect 已支持 `ablation.normalized` 并有显式 `absent` 语义（`main/watermarking/embed/orchestrator.py:102`, `main/watermarking/detect/orchestrator.py:102`）。
   - detect/evaluate 已写入 `ablation_digest`（`main/watermarking/detect/orchestrator.py:2286`, `main/watermarking/detect/orchestrator.py:2347`, `main/watermarking/detect/orchestrator.py:3745`）。
   - 仍有问题：
   - `embed` 里读取了 `enable_subspace`（`main/watermarking/embed/orchestrator.py:105`），但后面仍无条件执行 planner（`main/watermarking/embed/orchestrator.py:165`），这个开关目前基本无效。
   - 当前 ablation 粒度偏粗，只有 `content/geometry/subspace` 级别（`main/watermarking/detect/orchestrator.py:104`, `main/watermarking/detect/orchestrator.py:105`；`main/watermarking/embed/orchestrator.py:104`, `main/watermarking/embed/orchestrator.py:105`），还不够做你论文需要的 HF/LF、sync/anchor、attention-proxy/real-attn、sidecar 等细粒度消融。
   - `ablation_digest` 只哈希 `ablation.normalized`；像 `detect_runtime.image_domain_sidecar_enabled` 这种关键实验开关不在该摘要里（sidecar 开关逻辑在 `main/watermarking/detect/orchestrator.py:3469`，配置在 `configs/paper_full_cuda.yaml:89`）。
   - `impl_set_capabilities_digest` 仍按历史口径只绑定五个核心域（不含 hf/lf impl 变化）（`main/registries/runtime_resolver.py:398`）。

7. 几何锚点目前是“latent 相关性代理 attention”，与论文“Self-Attention Map 锚点”仍有方法差距
   - detect 侧已补了 latent -> attention proxy（`main/watermarking/detect/orchestrator.py:3493`），这是工程可运行性上的进步。
   - 但 `attention_anchor_map_relation_v1` 内部关系图仍是简化相关矩阵构图（`main/watermarking/geometry_chain/attention_anchor_extractor.py:668`）。
   - 这更像“attention-like proxy anchor”，还不能等价宣称是真实 Self-Attention 几何锚点。

8. 几何同步质量评分仍偏启发式，占位性质较强
   - `relation_alignment` 仍用 `relation_digest` 的字节和构造归一化量（`main/watermarking/geometry_chain/sync/latent_sync_template.py:809`）。
   - 这还不是论文里“空间同步 + 低阶几何反演”的强实现。


实现方法指令

请作为本仓库的“高约束合规修复代理”，针对我已确认的 1–8 条主要差距，执行“真实代码读取 → 阶段化计划 → 分阶段实现 → 每阶段测试门禁 → 阶段提交 → 最终合规审计”的端到端修复。

你的首要目标不是快速改动，而是在冻结约束、门禁语义、审计可追溯与消融实验纯净性前提下，产出可用于论文主实验与消融实验的合规代码。

你必须严格按以下流程执行：禁止跳步，禁止先改后读，禁止基于假设虚构接口，禁止通过隐式 fallback 掩盖问题。

一、总目标（必须同时满足）
1. 修复并闭环 1–8 条主要差距（几何同步链、planner runtime 真实性、HF/LF embed 接线、消融开关与摘要、代理 attention 语义、同步质量评分语义等）。
2. 保持与现有仓库命名风格、接口风格、失败语义风格一致。
3. 满足冻结约束与门禁绑定点，不破坏历史字段语义与冻结口径。
4. 支持论文主实验与消融实验（真实生效、可审计、可复现、无隐式污染）。
5. 自动运行测试与必要扫描，并报告风险、失败项、残余风险。

二、必须先做的事情（禁止跳过）
1. 先读取真实仓库代码，优先读取 `configs/` 与 `main/`：
- `configs/`：冻结约束/白名单/策略语义/论文配置（先读）
- `main/`：主实现（先读后改）
2. 读取后先输出“阶段 0：读取与约束提取”和“阶段 1：实施计划”，不得先改代码。
3. 在我未要求暂停的前提下，直接进入代码修改与测试，不要停留在方案层。

三、分阶段提交规则（硬约束）
你必须按阶段实施，并在每阶段完成后执行对应测试门禁。只有在门禁通过（或明确记录失败原因与风险并得到继续条件）后，才能进入下一阶段。

阶段提交要求：
1. 每阶段结束后给出：
- 本阶段改动文件清单
- 关键函数/方法变更
- 对应修复差距编号（1–8）
- 测试命令与结果
- 风险与回归点
2. 若用户要求你提交 commit，则使用非交互命令按阶段提交（不要 amend）。
3. 即使未提交 git commit，也必须输出“阶段交付摘要”，格式可映射为 commit message。
4. 未通过本阶段测试门禁，不得进入下一阶段（除非你明确标注阻塞并给出最小合规替代方案）。

建议阶段划分（必须映射 1–8）：
- 阶段 0：读取与约束提取（只读）
- 阶段 1：几何链闭环修复（差距 1、2）
- 阶段 2：embed/detect 接线与 HF/LF runtime impl 接入（差距 5，连带 6）
- 阶段 3：planner runtime 真实性增强 + 子空间语义映射增强（差距 3、4）
- 阶段 4：消融实验框架强化（差距 6）
- 阶段 5：几何锚点代理语义纠偏 + sync 评分语义增强（差距 7、8）
- 阶段 6：回归测试、合规审计总结、残余风险报告

四、硬性工作原则（必须遵守）
1. 以真实代码为准，不得假设接口存在。
2. 先搜索调用链，再修改被调方；先读注册表/调用方，再改实现。
3. 命名风格必须延续仓库习惯：
- `snake_case`
- `_build_* / _run_* / _resolve_* / _extract_* / _bind_*`
4. 保持 fail-fast 风格：
- 参数类型检查
- 显式异常
- 显式 `status` / `*_failure_reason` / `*_absent_reason`
5. 禁止隐式降级、隐式跨链调用、隐式 fallback 污染消融实验结果。
6. 冻结字段采用 append-only 原则：
- 优先新增字段，不改旧字段语义
- 若需要扩展摘要口径，优先新增并行字段（如 `*_v2`）
7. 不得使用破坏性 git 命令回滚用户改动。
8. 若某项无法完整实现，必须做“最小合规实现”，并显式标注 `proxy` / `heuristic` / `surrogate` 语义，禁止过度宣称。

五、必须先读取并核查的文件（最小集合）
A. 先读（默认只读，除非你论证必须同步改）
- `configs/runtime_whitelist.yaml`
- `configs/policy_path_semantics.yaml`
- `configs/frozen_contracts.yaml`（若存在）
- `configs/paper_full_cuda.yaml`
- `configs/` 下与 impl / policy / contract / whitelist / frozen 相关文件（按真实仓库存在情况补充）

B. 重点改动候选（先读后改）
- `main/watermarking/detect/orchestrator.py`
- `main/watermarking/embed/orchestrator.py`
- `main/watermarking/content_chain/subspace/subspace_planner_impl.py`
- `main/registries/runtime_resolver.py`
- `main/registries/geometry_registry.py`
- `main/watermarking/geometry_chain/sync/latent_sync_template.py`
- `main/watermarking/geometry_chain/attention_anchor_extractor.py`

C. 按需要同步改动（读后决定）
- HF/LF coder/embedder 接口与实现文件
- fusion / neyman_pearson / decision 相关文件（仅当字段与状态语义受影响）
- tests 中覆盖上述模块的单测/集成测试
- 记录/契约/authority 校验相关脚本（若仓库已有）

六、默认禁止改动（除非在阶段计划中明确论证）
1. 冻结契约与语义文件（如以下文件）：
- `configs/frozen_contracts.yaml`
- `configs/policy_path_semantics.yaml`
- `configs/runtime_whitelist.yaml`
2. 与本轮修复无关的模块和配置。
3. 历史 digest 字段的既有语义与口径（除非新增并行字段而非覆盖）。
4. 任何仅为绕过测试/门禁而进行的字段语义变更。
如确需改动上述文件，必须先在计划中说明：
- 为什么不能用兼容层/新增字段解决
- 风险是什么
- 与冻结约束的关系是什么

七、本轮修复 1–8 条差距的具体实现要求（必须逐条映射）
1. 差距 1：几何同步 v2 `relation_digest` 传递修复（核心阻塞）
- 修复 detect 几何链中 `sync_module` 与 `GeometryLatentSyncSD3V2.extract()` 的接口断裂。
- 使 sync 路径在 detect 主流程可获得 `relation_digest`（或等价可审计绑定输入）。
- 保持旧 sync 模块兼容，不破坏 baseline sync。

2. 差距 2：attention 锚点 + sync 顺序闭环
- 重构 detect 几何编排为显式闭环：
  - 允许先 anchor 再 sync，或两阶段运行（但必须显式）
  - 必须保证 `relation_digest` 先生成再消费
- 几何证据中保留 anchor/sync 状态、失败原因、绑定摘要，禁止隐式覆盖。

3. 差距 3：planner runtime 真实性增强
- 优先使用 runtime `pipeline/latents/trajectory`
- 不得继续将代理伪状态路径当作“真实扩散轨迹”
- 若暂无法完整实现 Jacobian/真实 UNet 轨迹采样，必须实现“真实 runtime 驱动的可审计近似路径”，并在 `samples_anchor / plan / audit` 中明确代理语义

4. 差距 4：语义自适应子空间映射增强
- 改进 mask→feature 映射（不能只靠 `% feature_dim`）
- 新增可复算映射摘要（如 `mapping_mode / mapping_digest / mapping_stats`）
- 保持 `region_spec_digest` 稳定、可复算

5. 差距 5：embed 主路径接入 runtime resolver 的 HF/LF 实现对象
- embed 真实嵌入流程优先使用 `impl_set.lf_coder` / `impl_set.hf_embedder`
- 如接口不一致，新增适配层，不要破坏现有公共接口
- 允许保留显式 fallback，但必须在 trace/audit 中标记 fallback 使用情况，避免消融污染

6. 差距 6：消融实验框架强化（真实生效 + 粒度 + 摘要）
- 修复 `embed` 侧 `enable_subspace` 无效问题
- 增加细粒度开关（至少覆盖 HF/LF、sync/anchor、attention proxy、image sidecar；命名按仓库风格）
- 扩展 `ablation_digest` 覆盖会影响实验结论的关键开关
- 开关关闭时输出 `absent` + 清晰 `absent_reason`
- 禁止隐藏 fallback 使被关闭链路继续生效
- 若历史 `impl_set_capabilities_digest` 口径冻结，新增并行字段（例如 `impl_set_capabilities_digest_v2`）而不是覆盖旧字段语义

7. 差距 7：几何锚点代理语义纠偏
- 若仍使用 latent 相关性 proxy attention，字段/审计必须显式标注 `proxy` / `attention_like`
- 禁止将 proxy 命名为真实 self-attention
- 为未来真实 attention 输入预留兼容入口，但本轮优先保证语义诚实、可审计

8. 差距 8：几何同步质量评分语义增强
- 若当前评分仍是启发式，必须显式标注 `heuristic`，记录版本与来源
- 若增强实现，必须可复算、失败可解释、与融合门禁语义一致
- 不得让不可信评分在融合层被当作可信几何主证据而无状态标记

八、模块职责划分（必须保持清晰边界）
1. `detect/embed orchestrator`
- 负责编排、开关路由、runtime inputs 构造、证据合并、门禁衔接、审计字段绑定
- 不负责重写底层算法实现（除非适配层）

2. `runtime_resolver / geometry_registry`
- 负责实现对象构建、包装与接口兼容
- 不偷渡业务判决逻辑，不做隐式降级

3. `geometry_chain/*`
- 负责锚点/同步/质量指标/失败语义的结构化输出
- 必须显式字段化，不依赖隐式上下文

4. `subspace_planner_impl`
- 负责子空间规划、采样锚点、映射摘要、代理/真实路径语义区分
- 必须输出可审计锚点与稳定 digest

5. HF/LF coder/embedder
- 负责具体嵌入/检测算法与 trace 结构
- orchestrator 仅做编排与适配，不复制算法逻辑

九、冻结边界与门禁绑定点（必须逐项核对，并在最终报告逐项说明）
1. 冻结契约绑定点
- 不删除/重命名/改写既有冻结字段语义
- 新增字段必须 append-only，说明用途与兼容性
- `status` / `failure_reason` / `absent_reason` / `audit` 风格保持一致

2. policy path / 失败语义绑定点
- `ok / failed / absent / mismatch` 使用需与策略语义配置一致
- 证据不可用时返回显式失败或缺失，不得伪装为低分

3. runtime whitelist / impl 选择绑定点
- 新增或变更 impl 路径前核查 whitelist 与 registry 能力门禁
- 不引入未允许的运行时实现路径（除非同时合规更新并说明）

4. digest 冻结口径绑定点
- 历史 digest 字段语义尽量不变
- 需要扩大摘要覆盖时新增并行字段优先
- 新增 digest 必须可复算、字段来源稳定、命名明确

5. 消融实验纯净性绑定点
- ablation 关闭必须真实生效、可审计、无隐式 fallback
- `ablation_digest` 必须绑定影响实验结论的关键开关
- 与 `detect_runtime.image_domain_sidecar_enabled` 等开关的优先级/冲突关系必须明确

6. 测试与兼容性绑定点
- 不只改实现，必须同步改测试/新增测试
- baseline 路径不得破坏
- 兼容层优先于破坏式改接口

十、阶段门禁（每阶段必须执行）
阶段 0（读取与约束提取）门禁：
- 输出已读取文件清单
- 输出可改/禁止改/需同步改文件清单
- 输出冻结边界与门禁绑定点清单
- 输出高风险点与硬约束清单
未完成不得进入阶段 1

阶段 1（几何链闭环：差距 1、2）门禁：
- detect 主路径能显式闭环处理 anchor→sync（或等价两阶段闭环）
- `relation_digest` 能被 sync v2 消费（有代码路径证据）
- 相关单测/最小集成测试通过（至少覆盖 sync v2 + geometry orchestration）
- 无破坏 baseline sync 的回归

阶段 2（HF/LF embed 接线：差距 5，连带 6）门禁：
- embed 主路径优先使用 `impl_set.lf_coder` / `impl_set.hf_embedder` 或显式适配层
- fallback 使用有 trace/audit 标记
- 相关测试通过（embed orchestrator / runtime_resolver / HF/LF 接线）

阶段 3（planner runtime + 子空间映射：差距 3、4）门禁：
- planner 真实/代理路径语义可区分，审计字段齐全
- mask→feature 映射增强具备可复算摘要
- 子空间/plan digest 不因不稳定字段产生明显漂移（需最小验证）
- 相关测试通过

阶段 4（消融框架强化：差距 6）门禁：
- `enable_subspace` 等关键开关真实生效
- 新增细粒度开关具备显式 absent 语义
- `ablation_digest` 覆盖关键实验开关（或新增 v2 字段并保留旧口径）
- 相关测试/评测构建路径校验通过

阶段 5（几何语义纠偏：差距 7、8）门禁：
- proxy/heuristic 语义字段落地，不再误导性命名
- 与融合/门禁状态语义兼容
- 相关测试通过

阶段 6（总回归与合规审计）门禁：
- 改动相关测试集通过（或明确列出失败与阻塞）
- 输出完整合规审计总结（映射 1–8 + 冻结绑定点）
- 输出残余风险与下一步建议

十一、测试与扫描要求（必须执行）
1. 每阶段结束必须运行该阶段相关 pytest 测试（最小必要集合）。
2. 阶段 6 必须至少运行一次改动路径的回归测试集合。
3. 若改动涉及记录字段 / digest / 写入路径，必须做一次最小静态检查（优先用仓库已有脚本）。
4. 最终报告必须明确：
- 已运行测试命令
- 通过/失败情况
- 未运行项及原因
- 高风险未覆盖路径

十二、你必须输出的格式（严格遵循）
阶段 0：读取与约束提取
- 已读取文件清单（路径）
- 可改文件清单
- 禁止改动文件清单
- 需同步改动文件清单
- 冻结边界与门禁绑定点清单
- 高风险点与硬约束清单

阶段 1–6：每阶段都输出
- 阶段目标（映射差距编号）
- 改动文件清单
- 关键函数/方法变更（路径 + 名称）
- 设计说明（为何这样改、如何满足门禁）
- 测试命令与结果
- 风险与回归点
- 阶段交付摘要（可作为 commit message）

最终交付总结
- 已修复项（映射 1–8）
- 部分修复项（映射 1–8）
- 未修复项（映射 1–8）
- 冻结约束与门禁绑定点符合性说明（逐项）
- 新增字段/摘要/开关的兼容性策略
- 残余风险与下一步建议（仅技术必要项）

十三、额外限制（防止不合规实现）
1. 不允许用“注释声明完成”代替代码实现。
2. 不允许通过吞异常（`except Exception: pass`）隐藏失败。
3. 不允许为通过测试而绕过状态语义/门禁语义。
4. 不允许修改无关模块来规避接口问题。
5. 不允许未经说明改变已有字段类型或取值语义。
6. 不允许把代理路径命名为真实路径（proxy attention ≠ self-attention）。
7. 不允许在未跑测试的情况下宣称“已完成且合规”。

现在开始：
先读取 `configs/` 与 `main/` 的相关文件，输出“阶段 0：读取与约束提取”和“阶段 1：实施计划”，然后再按阶段执行代码修改、测试与阶段交付摘要。
