**A. 现有审计即可判定的不符合项**
1. `onefile` 会覆盖 `paper_full_cuda.yaml` 的关键实现ID，导致测试对象偏离你的目标配置。  
证据：[paper_full_cuda.yaml](/d:/Code/CEG-WM/configs/paper_full_cuda.yaml) 与 [run_onefile_workflow.py](/d:/Code/CEG-WM/scripts/run_onefile_workflow.py)（第269、270行）。

2. `onefile` 会自动补写 `detect_record` 的 `status/score` 用于 calibrate/evaluate，破坏真实闭环证据。  
证据：[run_onefile_workflow.py](/d:/Code/CEG-WM/scripts/run_onefile_workflow.py)（第454-457行）。

3. 论文机制断言脚本与目标机制不一致，且允许“sync-only”通过，约束偏松。  
证据：[assert_paper_mechanisms.py](/d:/Code/CEG-WM/scripts/assert_paper_mechanisms.py)（第316-317行、第429行）。

4. `T2SMark` 比较入口只“读取” `detect_record.t2smark_baseline`，项目内未形成清晰“产出该字段”的闭环。  
证据：[experiment_matrix.py](/d:/Code/CEG-WM/main/evaluation/experiment_matrix.py)（第286行）。

5. 真实 self-attention map 注入通道在仓库内未形成可见写入闭环，检测侧默认可回退 latent proxy。  
证据：[detect/orchestrator.py](/d:/Code/CEG-WM/main/watermarking/detect/orchestrator.py)（第3777行）；全仓仅发现读取键，无写入点。

**B. 必须“真实 CUDA 全流程”才能判定的不符合项**
1. 是否真正跑在 `detect_runtime_mode=real` 且 `synthetic_pipeline=false`。  
本不符合项需要真实cuda全流程workflow验证

2. 子空间证据是否达到主证据级（`evidence_level=primary`，非 surrogate）。  
本不符合项需要真实cuda全流程workflow验证

3. 几何链在攻击下是否“可用且有效”（如 `geo_available_rate>0`，并稳定）。  
本不符合项需要真实cuda全流程workflow验证

4. 对旋转/缩放/高斯噪声鲁棒性是否达到并超越 T2SMark（真实同口径对比）。  
本不符合项需要真实cuda全流程workflow验证

5. “保持高生成质量”的论文目标是否成立（图像质量与鲁棒性联合结果）。  
本不符合项需要真实cuda全流程workflow验证