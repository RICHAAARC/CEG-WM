**关键差距（按严重性）**

### 1. 几何同步 v2 运行阻断（BLOCK）
- 现状：
  - `SyncGeometryLatentSyncSD3V2.sync_with_context()` 中使用了未定义变量 `runtime_inputs`，detect 侧会落入 `sync_with_context_failed: NameError`。
- 目标状态：
  - v2 同步模块在 detect 主链路可稳定执行，不出现 `NameError`/`TypeError` 类失败。
  - `paper_full_cuda` 配置下 geometry 链可得到有效 `sync_status`（非异常失败）。

### 2. paper 主路径实现未与 T2SMark/PRC 真正对齐（BLOCK）
- 现状：
  - 配置声明 `hf_embedder_t2smark_v1` / `lf_coder_prc_v1`，但 latent per-step 主注入路径主要走 `channel_hf` / `channel_lf`。
  - 形成“配置对齐、执行未对齐”的方法学风险。
- 目标状态：
  - paper 主路径的 HF/LF 注入与检测由目标实现（或等价可证明适配实现）主导，且证据字段能明确绑定到对应 impl_id。
  - 运行记录可审计地区分“真实目标实现”与“fallback/代理实现”。

### 3. 子空间主证据仍是 surrogate（HIGH）
- 现状：
  - planner 已记录 `sample_semantics` / `surrogate_reason`，但 `_sample_from_diffusion_pipeline()` 仍非真实 UNet Jacobian/流形反演链路。
- 目标状态：
  - 子空间构建可输出真实 Jacobian/轨迹证据，或提供可验证的等价证据链并在报告中明确级别。
  - 与论文“真实流形/Jacobian 主证据”叙事一致。

### 4. 几何锚点仍是 proxy attention（HIGH）
- 现状：
  - 已明确标注 `attention_like/proxy`，但关系图来自 latent 相关性代理，非真实 Self-Attention Map。
- 目标状态：
  - 几何锚点来自真实 attention 特征（或明确的可验证等价替代），并可解释其几何不变性来源。
  - 报告中避免将代理特征描述为真实 self-attention。

### 5. 几何同步质量评分仍为 heuristic（HIGH）
- 现状：
  - `sync_quality_semantics` 与 `quality_method` 已声明 heuristic，质量分数构造仍偏启发式。
- 目标状态：
  - 同步评分机制从启发式升级为可验证的几何一致性度量（至少提供更强理论/实证绑定）。
  - 支撑旋转、缩放、高斯噪声鲁棒性主张的主证据角色。

### 6. HF/LF embed 接线闭环未完成（HIGH）
- 现状：
  - LF 侧已有 `impl_set.lf_coder` 适配和绑定语义。
  - HF 侧 embed 仍以 `embed_high_freq_pattern()` 路径为主，`impl_set.hf_embedder` 尚未形成对等主调用。
- 目标状态：
  - HF/LF 两侧都具备统一的“impl_set 主调用 + 明确 fallback 语义”闭环。
  - 消融实验可无歧义隔离 HF/LF 真实实现影响。

### 7. 消融归一化口径存在断点（MEDIUM）
- 现状：
  - detect 读取 `enable_sync/enable_anchor/enable_attention_proxy/enable_image_sidecar`；
  - 但 `normalize_ablation_flags()` 未将这些字段纳入 `ablation.normalized`。
- 目标状态：
  - 所有实验使用的关键开关统一进入 `ablation.normalized` 与 digest 口径，确保可复验。

### 8. `impl_set_capabilities_digest` 仍为历史五域口径（MEDIUM）
- 现状：
  - 当前 digest 仅绑定五个核心域，HF/LF 扩展能力变化未纳入同口径摘要。
- 目标状态：
  - 建立并行 `v2` 能力摘要口径（不破坏历史兼容），覆盖 HF/LF 变更并可审计区分版本。

### 9. v2 主链路测试覆盖不足（MEDIUM）
- 现状：
  - 现有 sync registry 回归主要覆盖 v1；paper 相关回归多为 stub 路径，未覆盖真实 v2 registry 实现。
- 目标状态：
  - 增加 v2 真实实现的单测与主链路集成测试，覆盖 `sync_with_context(runtime_inputs)`、异常语义和 relation 绑定。

### 10. 子空间语义映射仍是投影近似（MEDIUM）
- 现状：
  - 已升级为稳定哈希映射并具备可复算锚点，但仍非显式流形局部几何建模。
- 目标状态：
  - 映射机制与语义显著区域/流形局部结构形成更直接、可解释、可验证的绑定。

### 11. 仓库级 append-only 静态审计未全绿（MEDIUM）
- 现状：
  - `audit_records_fields_append_only.py` 在当前基线仍有字段未登记导致 FAIL。
- 目标状态：
  - 冻结注册表与扩展字段治理完成，仓库级 append-only 审计恢复全绿。


结论：**目前不完全符合你的研究预期**，但比之前更接近，已修复部分阻断问题。

已验证的进展：
- v2 同步链路的 `NameError` 已修复，`sync_with_context` 接口现在可运行：`main/registries/geometry_registry.py:372`。
- 新增了 latent 路径的 impl 绑定审计字段并有测试覆盖：`tests/test_latent_impl_binding_audit_fields.py:21`。
- 新增 detect 侧细粒度消融开关归一化：`main/core/config_loader.py:89`、`main/core/config_loader.py:145`。
- 新增 `impl_set_capabilities_v2_digest` 并保留 v1：`main/registries/runtime_resolver.py:399`、`main/registries/runtime_resolver.py:401`、`main/registries/runtime_resolver.py:637`。
- 我实跑了 4 组相关测试，均通过（sync/paper path/impl binding/region+anchor）。

仍与论文目标存在的关键差距：
- **子空间主证据仍是 surrogate**，不是你要的真实 Jacobian/流形主证据：`main/watermarking/content_chain/subspace/subspace_planner_impl.py:2493`、`main/watermarking/content_chain/subspace/subspace_planner_impl.py:2495`。
- **paper 主路径仍未真正落到 PRC/T2SMark 核心算子**，而是 channel 适配/回退语义；新测试本身也在验证该回退：`main/diffusion/sd3/infer_runtime.py:517`、`main/diffusion/sd3/infer_runtime.py:535`、`main/watermarking/content_chain/latent_modifier.py:148`、`main/watermarking/content_chain/latent_modifier.py:210`、`tests/test_latent_impl_binding_audit_fields.py:46`。
- **几何锚点仍是 proxy attention**，不是真实 self-attention 锚点：`main/watermarking/geometry_chain/attention_anchor_extractor.py:640`、`main/watermarking/geometry_chain/attention_anchor_extractor.py:642`。
- **同步评分仍是 heuristic**，尚非强几何反演证据：`main/watermarking/geometry_chain/sync/latent_sync_template.py:696`、`main/watermarking/geometry_chain/sync/latent_sync_template.py:845`。
- **“超越 T2SMark”缺少可复验量化证据**（旋转/缩放/高斯噪声完整对比未形成）：`doc/目标实现差距.md:59`。
- **仓库级 append-only 审计仍 FAIL**（历史未登记字段仍在），合规闭环未全绿：`scripts/audits/audit_records_fields_append_only.py` 当前输出 `result: FAIL`。

所以，按你的论文预期判断：  
- 方法骨架：部分达到。  
- 论文级“完全达成”：**未达到**。