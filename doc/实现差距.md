**A. 现有审计即可判定的不符合项**
1. `onefile` 会覆盖 `paper_full_cuda.yaml` 的关键实现ID，导致测试对象偏离你的目标配置。  
证据：[paper_full_cuda.yaml](/d:/Code/CEG-WM/configs/paper_full_cuda.yaml) 与 [run_onefile_workflow.py](/d:/Code/CEG-WM/scripts/run_onefile_workflow.py)（第269、270行）。

2. `onefile` 会自动补写 `detect_record` 的 `status/score` 用于 calibrate/evaluate，破坏真实闭环证据。  
证据：[run_onefile_workflow.py](/d:/Code/CEG-WM/scripts/run_onefile_workflow.py)（第454-457行）。

3. 论文机制断言脚本与目标机制不一致，且允许“sync-only”通过，约束偏松。  
证据：[assert_paper_mechanisms.py](/d:/Code/CEG-WM/scripts/assert_paper_mechanisms.py)（第316-317行、第429行）。

4. `T2SMark` 比较入口只“读取” `detect_record.t2smark_baseline`，项目内未形成清晰“产出该字段”的闭环。  
证据：[experiment_matrix.py](/d:/Code/CEG-WM/main/evaluation/experiment_matrix.py)（第286行）。

5. 真实 self-attention map 注入通道在仓库内未形成可见写入闭环，检测侧默认可回退 latent proxy。  
证据：[detect/orchestrator.py](/d:/Code/CEG-WM/main/watermarking/detect/orchestrator.py)（第3777行）；全仓仅发现读取键，无写入点。

**B. 必须“真实 CUDA 全流程”才能判定的不符合项**
1. 是否真正跑在 `detect_runtime_mode=real` 且 `synthetic_pipeline=false`。  
本不符合项需要真实cuda全流程workflow验证

2. 子空间证据是否达到主证据级（`evidence_level=primary`，非 surrogate）。  
本不符合项需要真实cuda全流程workflow验证

3. 几何链在攻击下是否“可用且有效”（如 `geo_available_rate>0`，并稳定）。  
本不符合项需要真实cuda全流程workflow验证

4. 对旋转/缩放/高斯噪声鲁棒性是否达到并超越 T2SMark（真实同口径对比）。  
本不符合项需要真实cuda全流程workflow验证

5. “保持高生成质量”的论文目标是否成立（图像质量与鲁棒性联合结果）。  
本不符合项需要真实cuda全流程workflow验证


任务目标：
仅修复 A1 与 A3 两项静态不符合项，且不引入与本阶段无关的改动。修复后需满足 paper_full_cuda 冻结约束，不允许“脚本覆盖配置”或“断言口径与目标机制不一致”。

先做的事（必须）：
1. 先读取真实仓库代码，不要臆测。重点读取：
- configs/paper_full_cuda.yaml（冻结约束基准）
- scripts/run_onefile_workflow.py（A1）
- scripts/assert_paper_mechanisms.py（A3）
- 相关 tests 与 scripts 下调用链文件（用于回归验证）
2. 给出“改动文件清单”，分三类：
- 需要改动
- 需要同步改动
- 禁止改动
3. 然后再实施代码修改并运行自动测试。

本阶段必须修复的两项问题：
A1（onefile 覆盖 paper_full_cuda 关键 impl ID）：
- 修复 scripts/run_onefile_workflow.py 中对 paper profile 的 impl 强制覆写逻辑。
- 对 profile=paper_full_cuda：不得覆盖 sync_module_id、geometry_extractor_id（以及同类关键 impl 绑定字段）。
- 若存在 profile 归一化逻辑，只允许补齐非关键缺省字段，不允许改写冻结约束字段。
- 若检测到配置缺失或非法，必须 fail-fast 报错，不允许静默替换为脚本默认值。

A3（论文机制断言脚本口径不一致且约束偏松）：
- 修复 scripts/assert_paper_mechanisms.py，使断言口径与 configs/paper_full_cuda.yaml 一致。
- 断言必须严格绑定目标机制，不允许“sync-only 通过”。
- 对 paper_full_cuda 至少强约束：
  - impl.sync_module_id 必须等于配置冻结值（当前为 geometry_latent_sync_sd3_v2）
  - impl.geometry_extractor_id 必须等于配置冻结值（当前为 attention_anchor_map_relation_v1）
- 保留并强化 attention relation 证据校验（anchor_digest、anchor_metrics、extraction_source 等），禁止退化为仅排除 baseline identity 的宽松规则。

整体构建思路：
1. 先抽取 paper profile 冻结约束读取入口（可在脚本内封装小函数），所有断言/编排都从同一约束来源取值，避免硬编码漂移。
2. onefile 只做流程编排，不做关键 impl 重写；assert 脚本只做合规判定，不做推断性放宽。
3. 对关键路径增加最小必要注释和错误信息，确保失败原因可审计。

实现要求（方法与函数层面）：
1. 函数/方法命名保持现有命名风格（snake_case，与仓库一致）。
2. 新增函数优先小而单一职责，例如：
- 读取/解析 paper 冻结 impl 约束
- 校验 onefile 不覆写关键 impl 字段
- 校验 assert 与配置约束一致
3. 不要改动与 A1/A3 无关的业务逻辑。

冻结边界与门禁绑定点（必须遵守）：
1. 冻结边界：
- configs/paper_full_cuda.yaml 作为冻结约束源，不得被脚本隐式覆盖其关键 impl 含义。
2. 门禁绑定：
- paper_faithfulness 机制门禁与 impl 绑定一致性门禁必须可追溯到同一配置约束。
- 断言失败必须可定位到具体字段，不允许模糊失败。
3. 约束关系：
- 编排层（run_onefile_workflow）不得破坏配置层（paper_full_cuda）；
- 审计层（assert_paper_mechanisms）必须与配置层同口径，且严于“只排除 baseline”规则。

改动范围控制：
1. 优先只改：
- scripts/run_onefile_workflow.py
- scripts/assert_paper_mechanisms.py
2. 若必须新增公共校验函数，可在 scripts 内新增小型辅助模块，但要说明必要性。
3. 禁止改动：
- configs/paper_full_cuda.yaml 的冻结语义（除非我明确要求）
- main/ 主流程中与 A1/A3 无关模块
- 与本任务无关的历史问题修复

自动测试与验收（必须执行）：
1. 运行与 A1/A3 相关的脚本/测试（优先 scripts 与 tests 目录下可用用例）。
2. 至少覆盖：
- paper_full_cuda 下 onefile 不再覆写关键 impl ID
- assert 脚本对 v2 + attention_anchor_map_relation_v1 严格通过
- 错误配置时能正确 fail-fast
3. 输出测试结果与结论：
- 通过项
- 失败项
- 风险项（若存在）
4. 若某项必须依赖真实 CUDA 全流程才可最终确认，请单独列出并原样标注：
“本不符合项需要真实cuda全流程workflow验证”

交付格式：
1. 先给“变更摘要（按文件）”
2. 再给“门禁/冻结约束符合性对照表”
3. 再给“测试执行与结果”
4. 最后给“剩余风险与下一步建议”