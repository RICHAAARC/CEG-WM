基于你当前仓库的真实结构，我的结论是：**现有 notebook 体系只覆盖了“机制闭环验证”和“单变量消融”的一部分，还不足以支撑最终论文发表所需的完整 Colab 流程。**
从论文发表视角，你还缺两类 notebook：

1. **发布收口类 notebook**：用于 repro bundle、signoff、审计与最终发布级打包。
2. **论文指标生产类 notebook**：用于大规模 clean／attack 数据生成、只读阈值校准、LPIPS／SSIM／FID、TPR@FPR 与最终表格导出。

同时，需要先指出一个基于真实仓库的前置事实。

---

## 一、基于真实代码的当前状态判定

你当前仓库中，`notebook/` 下实际存在：

1. `Paper_Full_Cuda.ipynb`
2. `Paper_Ablation.ipynb`
3. `Mini_Real_Validation.ipynb`

当前 `scripts/` 下实际存在的 live 入口主要是：

1. `scripts/run_paper_full_cuda.py`
2. `scripts/run_parallel_attestation_statistics.py`
3. `scripts/run_experiment_matrix.py`
4. `scripts/run_all_audits.py`

但是，`Paper_Ablation.ipynb` 与 `Mini_Real_Validation.ipynb` 在 notebook 内部调用的是：

* `scripts/run_paper_ablation_workflow.py`
* `scripts/run_mini_real_validation.py`

而这两个脚本当前**并不在 live `scripts/` 目录**，而是在 `archive/scripts/` 下。并且：

* `archive/scripts/run_paper_ablation_workflow.py` 还依赖 `scripts/run_onefile_workflow.py`；
* `archive/scripts/run_mini_real_validation.py` 也依赖 `scripts/run_onefile_workflow.py`；
* `archive/scripts/run_publish_workflow.py` 依赖 `run_repro_pipeline.py`；
* `run_repro_pipeline.py` 与 `run_freeze_signoff.py` 也只在 `archive/scripts/` 下。

因此，**你当前 notebook 版图在“主全机制 notebook”之外，其余 Colab 编排入口实际上尚未完成 live 化收口。**

这意味着：
从 notebook 组织的角度，第一优先级不是盲目再加 notebook，而是先把这组 Colab 编排脚本恢复到 live `scripts/`，或者统一改为 notebook 显式调用 `archive/scripts/`。
若不先处理这一步，你的 notebook 体系会在命名上完整，但在运行路径上不闭环。

---

## 二、建议的完整 notebook 组织方式

我建议将你的 notebook 体系分为三个层次：

### （一）核心机制与工程闭环层

这一层回答“项目机制是否真实成立、正式路径是否闭环”。

### （二）论文指标生产层

这一层回答“论文中的主表、攻击矩阵表、质量指标表如何稳定产出”。

### （三）发布与复现收口层

这一层回答“最终投稿或开源时，是否具备 repro bundle、审计与 signoff”。

基于你当前代码，我建议组织为下面这一套。

---

# 三、推荐的完整 notebook 版图

## A. 核心机制与工程闭环层

### 1. `Mini_Real_Validation.ipynb`

**定位**：正式路径前置结构验证 notebook。
**对应脚本**：`run_mini_real_validation.py`
**当前状态**：notebook 已存在，但依赖的 live 脚本未收口。

**作用**：

1. 验证 real negative cache 是否真实生成。
2. 验证 global calibrate 与 shared thresholds 是否真实生成。
3. 验证 forced pair fallback 是否在 formal guard 下被显式阻断。

**建议地位**：
它不是论文主结果 notebook，而是**正式大规模运行前的门禁 notebook**。
每次你修改 formal path、matrix 约束或 negative cache 逻辑后，都应先跑它。

---

### 2. `Paper_Full_Cuda.ipynb`

**定位**：项目全机制正式主链 notebook。
**对应脚本**：`scripts/run_paper_full_cuda.py`
**当前状态**：这是你当前最稳定、最可用的 live notebook。

**作用**：

1. 执行 embed → detect → calibrate → evaluate 主链。
2. 生成正式 run_root。
3. 验证项目“全机制闭环”是否成立。
4. 输出最关键的 records 与主 workflow artifacts。

**建议地位**：
它应继续保留为**唯一正式主机制 notebook**，但建议把其中两个重型分支从主 notebook 中拆出去，避免 Colab 24 小时限制下的整链重跑。

---

### 3. `Paper_Parallel_Attestation_Statistics.ipynb`（新增）

**定位**：独立事件级 attestation 统计 notebook。
**对应脚本**：`scripts/run_parallel_attestation_statistics.py`
**当前状态**：脚本已经 live，建议从 `Paper_Full_Cuda.ipynb` 中拆成独立 notebook。

**作用**：

1. 基于已完成的主 run_root；
2. 对 `event_attestation_score` 做独立 calibrate／evaluate；
3. 不影响主 workflow 的成功口径；
4. 生成专项统计结果，供论文 attestation 分支单独分析。

**为什么建议拆出**：
因为该流程在语义上就是 detached branch。若继续放在 `Paper_Full_Cuda.ipynb` 里，一旦这一支失败，你会浪费前面已经完成的主链运行时间。

---

### 4. `Paper_Experiment_Matrix.ipynb`（新增）

**定位**：正式 experiment matrix 独立 notebook。
**对应脚本**：`scripts/run_experiment_matrix.py`
**当前状态**：脚本已 live，但目前被 `run_paper_full_cuda.py` 作为 optional branch 调用。

**作用**：

1. 对攻击协议下的矩阵实验做批量运行；
2. 产出攻击条件分组汇总；
3. 形成正式论文中的 robustness matrix 基础表；
4. 与主 run_root 解耦，避免主链 notebook 被可选分支拖垮。

**为什么建议拆出**：
当前脚本语义已经很清楚：`experiment_matrix` 是 optional，不应与主机制闭环绑定成“失败即整链重来”的 notebook 结构。

---

### 5. `Paper_Ablation.ipynb`

**定位**：论文级单变量消融 notebook。
**对应脚本**：`run_paper_ablation_workflow.py`
**当前状态**：notebook 已存在，但依赖的编排脚本当前在 `archive/scripts/`，且还依赖 `run_onefile_workflow.py`。

**作用**：

1. 单次 embed；
2. 多次 detect-side variant 重跑；
3. 复用同一份 base embed；
4. 输出 compare summary；
5. 支撑论文中的“同对象、同上游产物、只改一个变量”的严格消融。

**建议地位**：
它不是日常每次都跑的 notebook，而是**机制稳定后，针对关键组件做论文消融**时使用。

---

## B. 论文指标生产层（你前面让我设计的 7 个 notebook）

这一组 notebook 不负责证明机制是否存在，而负责**大规模、可分批、可复用地产出论文表格和图**。
它们应当与 `Paper_Full_Cuda.ipynb` 分工明确：
前者是“项目闭环验证”，后者是“论文结果生产”。

---

### 6. `Eval_00_Manifest_And_Splits.ipynb`

**定位**：论文评测口径冻结 notebook。

**作用**：

1. 固定 prompt 列表、seed 列表、样本划分；
2. 划分 `calibration split` 与 `clean eval split`；
3. 固定 attack plan；
4. 生成后续所有 notebook 共享的 manifest。

**作用边界**：
它不跑模型，不做质量指标，不做 detect。
它只做一件事：**冻结评测样本口径**。

**必须性**：
很高。没有这一层，后面的 FID、LPIPS、TPR@FPR、攻击矩阵就容易口径漂移。

---

### 7. `Eval_01_Clean_Generation.ipynb`

**定位**：clean 基础产物生成 notebook。

**作用**：

1. 批量生成 baseline clean images；
2. 批量生成 watermarked clean images；
3. 保存统一记录；
4. 为后续 LPIPS、SSIM、FID 与 clean detect 提供同源输入。

**对应代码来源**：
主要复用你的生成主链与 `main.cli.run_embed`、`main.diffusion.sd3.*` 机制。

---

### 8. `Eval_02_Readonly_Calibration.ipynb`

**定位**：只读阈值校准 notebook。

**作用**：

1. 使用 `calibration split` 做 detect 打分；
2. 计算目标 FPR 下的阈值；
3. 形成只读 threshold artifact；
4. 后续 attack 评测只允许读取该阈值，不允许重估。

**对应代码来源**：
可复用 `main.cli.run_calibrate`，以及你当前 `calibration_record`／`thresholds` 的既有逻辑。

---

### 9. `Eval_03_Quality_Metrics.ipynb`

**定位**：质量指标 notebook。

**作用**：

1. 基于 `baseline clean` 与 `watermarked clean` 计算 LPIPS；
2. 计算 SSIM；
3. 计算 FID；
4. 导出逐样本与总体统计。

**对应代码来源**：
优先复用 `main/evaluation/image_quality.py`。

**为什么应独立**：
质量指标与攻击检测在依赖关系上是分离的，单独拆出最适合 Colab 多会话执行。

---

### 10. `Eval_04_Attack_Generation.ipynb`

**定位**：攻击派生产物 notebook。

**作用**：

1. 对 `watermarked clean images` 批量施加攻击；
2. 保存 attacked images；
3. 固定攻击族与攻击参数；
4. 输出 attack generation records。

**对应代码来源**：
优先复用：

* `main/evaluation/attack_plan.py`
* `main/evaluation/attack_runner.py`
* `main/evaluation/protocol_loader.py`

---

### 11. `Eval_05_Detect_And_TPR.ipynb`

**定位**：鲁棒检测与 TPR@FPR notebook。

**作用**：

1. 对 clean eval 集做 detect；
2. 对 attacked eval 集做 detect；
3. 使用 `Eval_02` 的只读阈值计算 clean TPR@FPR；
4. 计算 attack-wise TPR@FPR；
5. 导出攻击矩阵下的检测表。

**对应代码来源**：
优先复用：

* `main.cli.run_detect`
* `main.cli.run_evaluate`
* `main/evaluation/metrics.py`

---

### 12. `Eval_06_Tables_And_Figures.ipynb`

**定位**：论文总表与图输出 notebook。

**作用**：

1. 汇总 LPIPS、SSIM、FID；
2. 汇总 clean TPR@FPR；
3. 汇总 attack TPR@FPR；
4. 生成主表、攻击矩阵表、图表与 LaTeX 表；
5. 输出论文中真正要引用的最终数字。

**对应代码来源**：
优先复用：

* `main/evaluation/table_export.py`
* `main/evaluation/report_builder.py`

---

## C. 发布与复现收口层

### 13. `Paper_Publish_Repro_Signoff.ipynb`（新增）

**定位**：发布级收口 notebook。
**对应脚本**：

* `run_repro_pipeline.py`
* `run_publish_workflow.py`
* `run_freeze_signoff.py`

**当前状态**：这组脚本目前只在 `archive/scripts/` 下。

**作用**：

1. 生成 repro bundle；
2. 执行最小发布级审计；
3. 生成 signoff 决策；
4. 形成最终可归档、可复核、可随论文附带的发布包。

**为什么必须新增**：
你当前 notebook 体系没有一条 Colab 级的“发布收口链”。
只有 `Paper_Full_Cuda.ipynb`，还不足以回答“这个 run 是否已经达到最终发布与投稿所需的可复现收口状态”。

---

# 四、建议的目录组织方式

建议将 notebook 目录重组为：

```text
notebook/
├── core/
│   ├── Mini_Real_Validation.ipynb
│   ├── Paper_Full_Cuda.ipynb
│   ├── Paper_Parallel_Attestation_Statistics.ipynb
│   ├── Paper_Experiment_Matrix.ipynb
│   └── Paper_Ablation.ipynb
├── eval/
│   ├── Eval_00_Manifest_And_Splits.ipynb
│   ├── Eval_01_Clean_Generation.ipynb
│   ├── Eval_02_Readonly_Calibration.ipynb
│   ├── Eval_03_Quality_Metrics.ipynb
│   ├── Eval_04_Attack_Generation.ipynb
│   ├── Eval_05_Detect_And_TPR.ipynb
│   └── Eval_06_Tables_And_Figures.ipynb
└── release/
    └── Paper_Publish_Repro_Signoff.ipynb
```

这样分层后，notebook 语义会非常清晰：

* `core/`：证明机制存在与正式路径闭环；
* `eval/`：生产论文数字；
* `release/`：完成最终可复现发布。

---

# 五、运行顺序与并行关系

## （一）严格先后顺序

### 第 0 阶段：先做编排入口收口

这一步不是 notebook，而是你当前体系的前提条件：

1. 将 `archive/scripts/` 中 notebook 依赖的脚本恢复到 live `scripts/`；
2. 或者把 notebook 中的脚本路径全部改为 `archive/scripts/`；
3. 统一 `notebook/README.md`，因为它当前仍指向旧的 `colab_complete_workflow.ipynb`，与现状不一致。

若不先做这一步，后续 notebook 体系在 Colab 中不稳定。

---

### 第 1 阶段：机制前置验证

1. `Mini_Real_Validation.ipynb`
2. `Paper_Full_Cuda.ipynb`

逻辑是：
先用 mini-real 验证 formal guard 与 real cache 等结构前提，再跑正式全机制主链。

---

### 第 2 阶段：从主 run_root 派生的独立分支

在 `Paper_Full_Cuda.ipynb` 生成正式 run_root 之后，可以并行运行：

1. `Paper_Parallel_Attestation_Statistics.ipynb`
2. `Paper_Experiment_Matrix.ipynb`

这两个 notebook 都依赖主 run_root，但彼此之间没有强依赖，可以并行开两个 Colab 会话。

---

### 第 3 阶段：论文消融

`Paper_Ablation.ipynb`

它通常在机制稳定后执行，不必与每一次 `Paper_Full_Cuda.ipynb` 绑定。
若你支持“外部 embed_record 复用”，它甚至可以与部分评测 notebook 并行。

---

### 第 4 阶段：论文指标生产链

严格顺序建议为：

1. `Eval_00_Manifest_And_Splits.ipynb`
2. `Eval_01_Clean_Generation.ipynb`
3. `Eval_02_Readonly_Calibration.ipynb`

然后分叉：

4. `Eval_03_Quality_Metrics.ipynb`
5. `Eval_04_Attack_Generation.ipynb`

其中，`Eval_03` 与 `Eval_04` 可以并行。

最后合流：

6. `Eval_05_Detect_And_TPR.ipynb`
7. `Eval_06_Tables_And_Figures.ipynb`

因为 `Eval_05` 需要同时依赖：

* `Eval_02` 给出的只读阈值；
* `Eval_04` 生成的 attacked images。

而 `Eval_06` 必须等待：

* `Eval_03` 的质量指标；
* `Eval_05` 的鲁棒检测结果。

---

### 第 5 阶段：发布级收口

`Paper_Publish_Repro_Signoff.ipynb`

这一步建议放在最后。
因为它的意义不是帮助你调参，而是将一个你已经认可的正式 run 做最终封装、审计与签署。

---

## （二）可以并行的 notebook

你的 Colab 允许多会话，因此可以利用下面的并行结构。

### 可以并行的第一组

在 `Paper_Full_Cuda.ipynb` 完成后：

1. `Paper_Parallel_Attestation_Statistics.ipynb`
2. `Paper_Experiment_Matrix.ipynb`

---

### 可以并行的第二组

在 `Eval_01_Clean_Generation.ipynb` 与 `Eval_02_Readonly_Calibration.ipynb` 完成后：

1. `Eval_03_Quality_Metrics.ipynb`
2. `Eval_04_Attack_Generation.ipynb`

---

### 可以进一步分片并行的部分

`Eval_04_Attack_Generation.ipynb` 与 `Eval_05_Detect_And_TPR.ipynb` 还可以按攻击族分片：

* JPEG 一组会话；
* crop／resize 一组会话；
* rotate／blur 一组会话。

这样更适合 Colab 24 小时限制。

---

# 六、针对你当前项目，最关键的三点调整建议

## （一）不要再让 `Paper_Full_Cuda.ipynb` 承担所有论文任务

基于真实脚本边界，`run_paper_full_cuda.py` 的职责是：

* 主链 output-only 编排；
* 可选触发 `experiment_matrix`；
* 不负责 signoff；
* 不负责发布收口。

因此，`Paper_Full_Cuda.ipynb` 应继续是**主机制闭环 notebook**，而不是论文所有表格与发布工作的总入口。

---

## （二）把 detached branch 从主 notebook 中拆出去

你当前 live 脚本已经给出了很清楚的边界：

* `run_parallel_attestation_statistics.py` 是独立专项流程；
* `run_experiment_matrix.py` 是独立批量评测入口。

因此，notebook 结构上也应反映这一边界。
这会显著降低 Colab 中断时的重跑成本。

---

## （三）发布链必须有单独 notebook

从最终论文发表角度，你目前最缺的是：

**“把正式 run_root 收口为 repro bundle + signoff + audit package 的发布级 notebook”。**

如果没有这一层，你能证明“机制跑通”，但仍不足以证明“这一轮结果已经达到对外发布、论文附带和复现实验可交付”的状态。

---

# 七、给你的最终建议

若按优先级排序，我建议你这样实施：

### 第一优先级

先修正 notebook 与脚本的 live 绑定关系。
尤其是：

* `Paper_Ablation.ipynb`
* `Mini_Real_Validation.ipynb`
* 未来的 `Paper_Publish_Repro_Signoff.ipynb`

因为它们当前依赖的编排脚本并未 live 化。

### 第二优先级

保留并精简 `Paper_Full_Cuda.ipynb`，把：

* `parallel_attestation_statistics`
* `experiment_matrix`
  拆成独立 notebook。

### 第三优先级

补齐 `Eval_00` 到 `Eval_06` 这 7 个论文指标 notebook。
这组 notebook 不应与 `Paper_Full_Cuda.ipynb` 混成一个大 notebook，而应作为论文主表生产线独立存在。

### 第四优先级

新增 `Paper_Publish_Repro_Signoff.ipynb`，作为最终发布收口入口。

---