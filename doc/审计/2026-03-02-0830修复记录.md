# 【警告】修复方案未完全落地，以下为实际状态记录。

## 一、变更背景说明

- 关联审计报告文件名：`doc/审计/2026-03-02-0830审计报告.md`
- 关联修复方案文件名：`doc/审计/2026-03-02-0830修复方案.md`
- 触发原因（对应审计报告编号）：
  - FAIL-5（运行期 `runtime_whitelist` 门禁未执行）
  - FAIL-1（evaluate 退化统计未强制阻断）
  - P1-3/P2-1（`paper_full_cuda` 下 sidecar 关闭与校准过滤开关未开启）
- 变更类型分类：
  - P0 阻断性修复：无
  - P1 重要语义修复：`evaluate_status="degenerate"` 落盘后阻断；signoff 对退化评估阻断
  - P2 结构优化：无
  - 合规性补充：CLI 主干强制传入 whitelist；resolver 运行期白名单断言
  - 统计口径修复：开启 `exclude_formal_sidecar_disabled_marker`，并启用 `image_domain_sidecar_enabled`
- 变更是否涉及冻结面（Frozen Surface）：否（未修改 `frozen_contracts.yaml` / `runtime_whitelist.yaml`）
- 变更是否涉及门禁面（Gate Enforcement）：是（resolver + CLI + signoff）
- 是否影响 workflow 输出结构：是（仅 append-only 新增 `evaluate_record.evaluate_status`，其余输出路径不变）

- 方案未生效项（基于当前仓库代码核对）：
  - 未生效：`main/watermarking/detect/orchestrator.py` 的 P0-2/P0-3 路径兜底改造，本轮未改动。
  - 未生效：`scripts/run_onefile_workflow.py` 中 synthetic 负样本注入与 marker 清理逻辑（`-1.0` 注入、`calibration_sample_is_synthetic_fallback` 清理）仍存在。

## 二、文件级变更记录（按文件分组）

### 文件：main/registries/runtime_resolver.py

1. 关联问题编号：FAIL-5
2. 问题现象描述：`assert_impl_allowed` 存在但运行主链未调用，`impl_id` whitelist 无执行级门禁。
3. 原始行为（修复前）：`build_runtime_impl_set_from_cfg(cfg)` 仅解析/构建 impl，不执行 whitelist 断言。
4. 变更操作：
   - 函数/类修改点：`build_runtime_impl_set_from_cfg`
   - 新增/删除/替换逻辑：新增可选入参 `whitelist`，在 `parse_impl_identity_from_cfg` 后执行 `assert_impl_allowed(whitelist, identity.as_dict())`
   - 字段读写变化：无 records 字段写入变化
   - 条件分支变化：新增 `if whitelist is not None` 分支
5. 解决问题：将 whitelist 从“元数据绑定”升级为“运行前强制门禁”。
6. 实现目的：阻断未许可 impl 在主链实例化与执行。
7. 当前状态：已验证
8. 是否影响：
   - records schema：否
   - digest 计算：否
   - workflow 文件输出：否
   - 冻结字段语义：否
   - 门禁执行路径：是
9. 回归测试覆盖情况：
   - 现有测试覆盖：`tests/test_runtime_resolver_capabilities_v2_digest.py`（本次已执行）
   - 是否需要新增测试：需要（建议新增“传入受限 whitelist 时抛 `GateEnforcementError`”的 resolver 级单测）
10. 风险评估：
   - 新风险可能性：中
   - 风险等级：MED
   - 缓解措施：保持 `whitelist=None` 向后兼容分支，仅在 CLI 主干强制传 whitelist。

### 二次修复记录：
- 操作时间：
- 问题原因：
- 修复补充：
- 是否产生新风险：
- 审计结果：

### 文件：main/cli/run_embed.py

1. 关联问题编号：FAIL-5
2. 问题现象描述：CLI 已加载 whitelist，但未传给 resolver。
3. 原始行为（修复前）：`build_runtime_impl_set_from_cfg(cfg)`。
4. 变更操作：
   - 函数/类修改点：`run_embed`
   - 新增/删除/替换逻辑：替换为 `build_runtime_impl_set_from_cfg(cfg, whitelist)`
   - 字段读写变化：无
   - 条件分支变化：无
5. 解决问题：embed 主链启用运行期 whitelist 门禁。
6. 实现目的：防止 embed 阶段加载未许可 impl。
7. 当前状态：已验证
8. 是否影响：
   - records schema：否
   - digest 计算：否
   - workflow 文件输出：否
   - 冻结字段语义：否
   - 门禁执行路径：是
9. 回归测试覆盖情况：
   - 现有测试覆盖：`tests/test_run_embed_cli_input_binding.py`（本次已执行）
   - 是否需要新增测试：可选（建议增加 whitelist 拒绝路径）
10. 风险评估：
   - 新风险可能性：低
   - 风险等级：LOW
   - 缓解措施：保持参数为位置参数，兼容测试 monkeypatch stub。

### 二次修复记录：
- 操作时间：
- 问题原因：
- 修复补充：
- 是否产生新风险：
- 审计结果：

### 文件：main/cli/run_detect.py

1. 关联问题编号：FAIL-5
2. 问题现象描述：detect 主链未把 whitelist 传给 resolver。
3. 原始行为（修复前）：`build_runtime_impl_set_from_cfg(cfg)`。
4. 变更操作：
   - 函数/类修改点：`run_detect`
   - 新增/删除/替换逻辑：替换为 `build_runtime_impl_set_from_cfg(cfg, whitelist)`
   - 字段读写变化：无
   - 条件分支变化：无
5. 解决问题：detect 主链启用运行期 whitelist 门禁。
6. 实现目的：阻断 detect 运行时 impl 越权。
7. 当前状态：部分验证
8. 是否影响：
   - records schema：否
   - digest 计算：否
   - workflow 文件输出：否
   - 冻结字段语义：否
   - 门禁执行路径：是
9. 回归测试覆盖情况：
   - 现有测试覆盖：本次未单独执行 detect CLI 白名单拒绝路径
   - 是否需要新增测试：需要（detect CLI 注入非法 impl_id 的失败用例）
10. 风险评估：
   - 新风险可能性：低
   - 风险等级：LOW
   - 缓解措施：保持 resolver 兼容签名，避免影响非主链调用。

### 二次修复记录：
- 操作时间：
- 问题原因：
- 修复补充：
- 是否产生新风险：
- 审计结果：

### 文件：main/cli/run_calibrate.py

1. 关联问题编号：FAIL-5
2. 问题现象描述：calibrate 主链未把 whitelist 传给 resolver。
3. 原始行为（修复前）：`build_runtime_impl_set_from_cfg(cfg)`。
4. 变更操作：
   - 函数/类修改点：`run_calibrate`
   - 新增/删除/替换逻辑：替换为 `build_runtime_impl_set_from_cfg(cfg, whitelist)`
   - 字段读写变化：无
   - 条件分支变化：无
5. 解决问题：calibrate 主链启用运行期 whitelist 门禁。
6. 实现目的：阻断阈值阶段 impl 越权执行。
7. 当前状态：部分验证
8. 是否影响：
   - records schema：否
   - digest 计算：否
   - workflow 文件输出：否
   - 冻结字段语义：否
   - 门禁执行路径：是
9. 回归测试覆盖情况：
   - 现有测试覆盖：本次未单独执行 calibrate CLI 白名单拒绝路径
   - 是否需要新增测试：需要（calibrate CLI 非法 impl_id 失败用例）
10. 风险评估：
   - 新风险可能性：低
   - 风险等级：LOW
   - 缓解措施：只改调用参数，不改校准业务逻辑。

### 二次修复记录：
- 操作时间：
- 问题原因：
- 修复补充：
- 是否产生新风险：
- 审计结果：

### 文件：main/cli/run_evaluate.py

1. 关联问题编号：FAIL-1、FAIL-5
2. 问题现象描述：
   - evaluate 对 `n_pos/n_neg` 退化无强制阻断。
   - evaluate 主链未把 whitelist 传给 resolver。
3. 原始行为（修复前）：
   - 无 `evaluate_status` 字段写入。
   - 写盘后不因 `n_pos/n_neg` 退化而失败。
4. 变更操作：
   - 函数/类修改点：`run_evaluate`
   - 新增/删除/替换逻辑：
     - resolver 调用改为 `build_runtime_impl_set_from_cfg(cfg, whitelist)`
     - 新增退化判定：从 `record.metrics` 读取 `n_pos/n_neg`
     - 退化时 append-only 写入 `record["evaluate_status"] = "degenerate"`
     - `records_io.write_json` 成功后抛 `ValueError("Degenerate evaluation: n_pos or n_neg must be positive")`
   - 字段读写变化：新增顶层字段 `evaluate_status`（仅退化分支）
   - 条件分支变化：新增 `is_degenerate_evaluate` 分支
5. 解决问题：保留可审计产物同时阻断退化评估静默通过。
6. 实现目的：实现“可写出现场 + 下游强失败”的 fail-fast 策略。
7. 当前状态：部分验证
8. 是否影响：
   - records schema：是（append-only 新增字段）
   - digest 计算：否
   - workflow 文件输出：是（退化时写盘后进失败路径）
   - 冻结字段语义：否
   - 门禁执行路径：是
9. 回归测试覆盖情况：
   - 现有测试覆盖：本次未新增 evaluate 退化专测
   - 是否需要新增测试：需要（`n_pos=0` 或 `n_neg=0` 时断言写入 `evaluate_status` 且抛异常）
10. 风险评估：
   - 新风险可能性：中
   - 风险等级：MED
   - 缓解措施：仅在退化分支触发；非退化路径不变。

### 二次修复记录：
- 操作时间：
- 问题原因：
- 修复补充：
- 是否产生新风险：
- 审计结果：

### 文件：scripts/run_freeze_signoff.py

1. 关联问题编号：FAIL-1
2. 问题现象描述：signoff 决策未显式阻断 `evaluate_status="degenerate"`。
3. 原始行为（修复前）：`compute_signoff_decision` 仅综合 static_audit 与 run_root_evidence。
4. 变更操作：
   - 函数/类修改点：`compute_signoff_decision`、`main`
   - 新增/删除/替换逻辑：
     - `compute_signoff_decision` 新增参数 `evaluate_record`
     - 新增 `evaluate_degenerate` 判断并并入 `BLOCK_FREEZE` 条件
     - 新增 blocking reason：`audit_id=evaluate.degenerate_status_block`
     - `main` 中加载 `records/evaluate_record.json` 并传入决策函数
   - 字段读写变化：signoff_report 中 `blocking_reasons` 可能新增 evaluate 来源条目
   - 条件分支变化：新增 `evaluate_degenerate` 分支
5. 解决问题：下游冻结门禁与 evaluate 退化语义闭环。
6. 实现目的：防止退化评估被 static audit 全绿掩盖。
7. 当前状态：已验证
8. 是否影响：
   - records schema：否
   - digest 计算：否
   - workflow 文件输出：是（signoff 决策结果可能从 ALLOW 变 BLOCK）
   - 冻结字段语义：否
   - 门禁执行路径：是
9. 回归测试覆盖情况：
   - 现有测试覆盖：`tests/test_matrix_audit_requirement_semantics.py`、`tests/test_signoff_path_audit_failed_allows.py`（本次已执行）
   - 是否需要新增测试：需要（新增 evaluate_degenerate 专项断言）
10. 风险评估：
   - 新风险可能性：低
   - 风险等级：LOW
   - 缓解措施：只增加阻断条件，不变更已有 static audit 评分规则。

### 二次修复记录：
- 操作时间：
- 问题原因：
- 修复补充：
- 是否产生新风险：
- 审计结果：

### 文件：configs/paper_full_cuda.yaml

1. 关联问题编号：P1-3、P2-1
2. 问题现象描述：sidecar 关闭，且 formal sidecar disabled marker 默认不过滤。
3. 原始行为（修复前）：
   - `detect_runtime.image_domain_sidecar_enabled: false`
   - `calibration.exclude_formal_sidecar_disabled_marker` 缺省（实际等价 false）
4. 变更操作：
   - 函数/类修改点：配置项
   - 新增/删除/替换逻辑：
     - 新增 `calibration.exclude_formal_sidecar_disabled_marker: true`
     - 修改 `detect_runtime.image_domain_sidecar_enabled: true`
   - 字段读写变化：影响 detect/calibrate 运行时行为，不改 records 字段定义
   - 条件分支变化：触发 detect sidecar 分支与校准过滤分支
5. 解决问题：打开真实 sidecar 路径并启用 formal marker 过滤。
6. 实现目的：降低 synthetic fallback 样本进入正式校准的概率。
7. 当前状态：待验证（需完整 workflow 重跑）
8. 是否影响：
   - records schema：否
   - digest 计算：是（cfg 变化会改变 `cfg_digest`）
   - workflow 文件输出：是（detect/calibrate 输出值域可能变化）
   - 冻结字段语义：否
   - 门禁执行路径：间接影响
9. 回归测试覆盖情况：
   - 现有测试覆盖：本次未执行基于 `paper_full_cuda` 的完整 workflow 测试
   - 是否需要新增测试：需要（profile 级 e2e/onefile 最小回归）
10. 风险评估：
   - 新风险可能性：中
   - 风险等级：MED
   - 缓解措施：执行 `paper_full_cuda` workflow 回归，重点检查 sidecar 依赖可达与样本量。

### 二次修复记录：
- 操作时间：
- 问题原因：
- 修复补充：
- 是否产生新风险：
- 审计结果：

## 三、跨文件逻辑联动变更记录

### 联动 1：运行期 impl whitelist 强制门禁

1. 变更链路图说明（文字）：
   - `run_embed/run_detect/run_calibrate/run_evaluate` 加载 `whitelist`
   - → 传入 `runtime_resolver.build_runtime_impl_set_from_cfg(cfg, whitelist)`
   - → resolver 调用 `assert_impl_allowed(whitelist, identity.as_dict())`
   - → 未许可 impl 在构建前失败
2. 受影响字段路径：
   - 输入：`cfg.impl.*_id`
   - 门禁：`runtime_whitelist.impl_id.*`
3. 变更一致性确认：4 个主干 CLI 均已改为传 `whitelist`。
4. 是否破坏既有门禁或冻结约束：否（加强门禁，不改冻结语义）。
5. 是否影响已有历史 records 可复现性：否（历史 records 可读；新运行可能更早失败）。

### 联动 2：evaluate 退化标记与 signoff 阻断闭环

1. 变更链路图说明（文字）：
   - `run_evaluate` 根据 `metrics.n_pos/n_neg` 判退化
   - → 退化时写 `evaluate_status="degenerate"`
   - → 写盘后抛异常
   - → `run_freeze_signoff` 读取 `records/evaluate_record.json`
   - → 若 `evaluate_status=="degenerate"`，决策 `BLOCK_FREEZE`
2. 受影响字段路径：
   - `records/evaluate_record.json::evaluate_status`
   - `artifacts/signoff/signoff_report.json::blocking_reasons[*]`
3. 变更一致性确认：写标记与读标记均已落地。
4. 是否破坏既有门禁或冻结约束：否（append-only 字段；不改既有枚举语义）。
5. 是否影响已有历史 records 可复现性：否（旧 records 缺字段时不触发该分支）。

### 联动 3：paper_full_cuda 统计口径开关

1. 变更链路图说明（文字）：
   - `paper_full_cuda.yaml` 开启 sidecar + 开启 formal marker 过滤
   - → detect 侧可进入 image-domain sidecar 分支
   - → calibrate 侧可过滤 `formal_with_sidecar_disabled_marker`
2. 受影响字段路径：
   - 配置：`detect_runtime.image_domain_sidecar_enabled`
   - 配置：`calibration.exclude_formal_sidecar_disabled_marker`
   - 统计：`calibration_record.sampling_policy.*`
3. 变更一致性确认：配置已改，orchestrator 已存在对应读取逻辑。
4. 是否破坏既有门禁或冻结约束：否。
5. 是否影响已有历史 records 可复现性：是（配置变化导致新 run 指标不可与旧 run 直接同口径比较）。

## 四、Workflow 输出影响确认

### 1. Embed 阶段

- 输出文件列表（按当前代码路径）：
  - `records/embed_record.json`
  - `artifacts/run_closure.json`
  - `artifacts/records_manifest.json`（由闭包逻辑维护）
- 关键字段完整性：本轮未改 embed records schema。
- 是否存在 fallback：存在（本轮未改 embed fallback 机制）。

### 2. Detect 阶段

- 输入解析路径：本轮未改 `orchestrator._resolve_detect_image_path`。
- `content_evidence` 是否可达：本轮无新增保障逻辑，仍依赖现有路径与输入可达性。
- `plan_digest` 是否透传：现有透传逻辑在代码中存在；本轮未改该链路。

### 3. Calibrate 阶段

- 是否存在退化阈值：风险仍在（`run_onefile_workflow.py` synthetic 负样本注入逻辑未改）。
- 样本过滤是否合规：配置已开启 `exclude_formal_sidecar_disabled_marker: true`，但需 workflow 实跑验证。
- 是否存在 synthetic 替代写入：存在（本轮未改 `run_onefile_workflow.py` 相关逻辑）。

### 4. Evaluate 阶段

- 是否可计算 TPR@FPR：取决于 `n_pos/n_neg`；本轮新增退化强失败。
- `n_pos / n_neg` 是否大于 0：若不大于 0，将写入 `evaluate_status="degenerate"` 并抛异常，且 signoff 阻断。

## 五、冻结面与门禁面影响评估

- 是否修改 `frozen_contracts.yaml`：PASS（未修改）
- 是否修改 `runtime_whitelist.yaml`：PASS（未修改）
- 是否新增字段未 append-only：PASS（仅新增 `evaluate_status`，且仅退化分支写入）
- 是否改变状态枚举语义：PASS（未修改既有 `status/decision_status` 语义）
- 是否新增写盘路径：PASS（未新增旁路写盘路径）
- 是否引入旁路输出：PASS（仍走 `records_io/path_policy`）

## 六、多次操作预留记录结构

### 二次修复记录（全局）

- 操作时间：
- 问题原因：
- 修复补充：
- 是否产生新风险：
- 审计结果：

## 七、自动补充的通用合规检查

1. 是否存在 silent fallback：是，存在（潜在残留风险）
   - 证据：embed/detect 相关 fallback 路径仍在，且本轮未改。
2. 是否存在 error → ok 的语义覆盖：存在（潜在残留风险）
   - 证据：`run_onefile_workflow.py` sidecar disabled 分支会把失败语义改写为可用分数输入。
3. 是否存在 score 置 0 替代失败语义：存在（潜在残留风险）
   - 证据：`run_onefile_workflow.py` 相关 fallback 逻辑仍在。
4. 是否存在阈值占位值：存在（潜在残留风险）
   - 证据：synthetic 负样本注入 `-1.0` 逻辑仍在。
5. 是否存在 `inputs=None` 合法化：存在风险（潜在残留风险）
   - 证据：detect 输入可达性链路本轮未改，仍需结合运行路径验证。
6. 是否存在 synthetic 样本混入正式校准：存在风险（潜在残留风险）
   - 证据：注入逻辑仍在；虽开启过滤开关，但需实跑确认过滤生效与样本数量约束。
7. 是否存在未记录的默认参数：存在风险（潜在残留风险）
   - 证据：多处配置默认值依赖代码缺省分支，需在运行记录中继续固化。

- 本次定向回归执行记录：
  - 命令：`pytest tests/test_runtime_resolver_capabilities_v2_digest.py tests/test_matrix_audit_requirement_semantics.py tests/test_signoff_path_audit_failed_allows.py tests/test_run_embed_cli_input_binding.py -q`
  - 结果：`7 passed in 0.27s`

## 八、续推复核与追加修复（2026-03-02）

### 8.1 用户反馈失败复盘（outputs/fail）

1. 失败目录：`outputs/fail`
2. 关键日志：`outputs/fail/audit_diagnostics/logs/workflow_execution.log`
3. 失败阶段：`evaluate`
4. 根因定位（日志尾部）：
   - `NameError: name 'cast' is not defined`
   - 报错点：`main/cli/run_evaluate.py` 中 `metrics_payload = cast(Dict[str, Any], metrics_node) ...`
5. 判定：属于运行时导入缺失导致的确定性代码错误，不是数据偶发问题。

### 8.2 对比历史成功产物（outputs/colab_complete_workflow）

1. 对比目录：`outputs/colab_complete_workflow`
2. 关键结论：
   - `outputs/colab_complete_workflow/DIAGNOSIS.txt` 显示“所有阶段执行成功”。
   - `outputs/colab_complete_workflow/colab_run_paper_full_cuda/records/evaluate_record.json` 存在且 `n_pos=30, n_neg=30`，可计算评估指标。
3. 说明：本次失败与“评估样本退化”无关，直接原因为 `cast` 导入缺失。

### 8.3 本次实际代码修复

1. 文件：`main/cli/run_evaluate.py`
2. 修复内容：
   - 新增导入：`from typing import Any, Dict, cast`
3. 修复目的：
   - 消除 evaluate 阶段 `NameError`，恢复 `metrics` 退化判定分支可执行性。

### 8.4 与本轮 InSPyReNet 路径变更的联动校验

1. 配置侧：`configs/paper_full_cuda.yaml`
   - `mask.semantic_model_path` 已对齐为 `../models/inspyrenet/ckpt_base.pth`。
2. Notebook 侧：`notebook/Colab_Workflow.ipynb`
   - 第11单元默认下载文件改为 `ckpt_base.pth`。
   - 下载默认直链：`https://huggingface.co/plemeri/InSPyReNet/resolve/main/ckpt_base.pth`。
   - 增加仓库核对链接展示：`https://huggingface.co/plemeri/InSPyReNet/tree/main`。
   - 第14单元预检硬失败项已对齐为 `models/inspyrenet/ckpt_base.pth` 文件存在性检查。

### 8.5 再次审计结论（是否仍可能 workflow 失败）

1. 已消除的高优先级失败点：
   - `evaluate` 的 `cast` 未导入导致的确定性崩溃（已修复）。
2. 仍需关注的剩余风险（非本次新增）：
   - `run_onefile_workflow.py` 仍保留 sidecar fallback 与 synthetic 注入相关路径，可能影响统计口径与 signoff 结果。
   - 若跳过 Notebook 第11/14单元，`ckpt_base.pth` 缺失仍可能导致语义模型路径不可达。
3. 当前静态判断：
   - 在执行第11单元下载权重 + 第14单元预检通过前提下，本次已识别的 `evaluate` 崩溃不会再复现。

### 8.6 本次追加回归验证

1. 执行命令：
   - `pytest tests/test_c4_calibrate_evaluate_readonly.py tests/test_calibrate_evaluate_readonly_threshold.py tests/test_onefile_workflow_paper_full_profiles.py -q`
2. 结果：
   - `39 passed in 0.71s`
3. 结论：
   - 与 evaluate/onefile 相关的关键回归集通过，未发现由本次修复引入的新失败。

### 8.7 新一轮失败复盘与修复（workflow 第 5 步返回码 1）

1. 用户现象：
   - 第 5 步提示“workflow 执行失败，返回码=1”，但 embed/detect/calibrate/evaluate 均已生成产物。
2. 根因定位：
   - 日志末尾：`[onefile] experiment_matrix produced zero successful items; abort before audits to expose root cause`。
   - `aggregate_report.json` 中批量失败统一为：`override_field_path_missing: field_path=ablation.enable_*`。
   - 本质原因：`paper_full_cuda.yaml` 缺失 `ablation` 字段树，导致 experiment_matrix 注入 `ablation_enable_*` 覆写时被白名单规则拒绝。
3. 修复动作：
   - 文件：`configs/paper_full_cuda.yaml`
   - 新增 `ablation` 基础字段：
     - `enable_content: null`
     - `enable_geometry: null`
     - `enable_anchor: null`
     - `enable_lf: null`
     - `enable_hf: null`
     - `lf_only: false`
     - `hf_only: false`
4. 修复后验证：
   - 命令：`pytest tests/test_onefile_workflow_paper_full_profiles.py tests/test_experiment_matrix_detect_stage_input_binding.py -q`
   - 结果：`32 passed in 0.65s`
5. 结论：
   - 当前第 5 步失败不再是 `evaluate` 崩溃，而是 experiment_matrix 配置字段缺失触发的批量失败。
   - 已通过补齐 `ablation` 字段修复该阻断条件。

### 8.8 追加稳健性修复（experiment_matrix 对旧 profile 兼容）

1. 背景：
   - 用户在 Colab 侧仍出现 `experiment_matrix` 40/40 失败，通常对应远端代码/配置未同步到已补齐 `ablation` 字段的版本。
2. 根因：
   - `main/evaluation/experiment_matrix.py` 在 `_run_stage_sequence` 中无条件注入 `ablation_enable_*` 覆写。
   - 当 `cfg_snapshot` 不含 `ablation` 段时，覆写会触发 `override_field_path_missing`，导致整批失败。
3. 修复动作：
   - 文件：`main/evaluation/experiment_matrix.py`
   - 新增兼容判断：仅当 `cfg_snapshot.ablation` 为 `dict` 时，才注入 `ablation_enable_*` 覆写项。
   - 新增类型校验：`cfg_snapshot` 非 `dict` 时直接抛 `TypeError`，避免隐式降级。
4. 回归测试：
   - 文件：`tests/test_experiment_matrix_detect_stage_input_binding.py`
   - 新增用例：`test_run_stage_sequence_skips_ablation_overrides_when_cfg_snapshot_has_no_ablation`
   - 断言：`cfg_snapshot` 缺失 `ablation` 时，四阶段 overrides 均不包含 `ablation_enable_*`。
5. 验证结果：
   - 命令：`pytest tests/test_experiment_matrix_detect_stage_input_binding.py tests/test_onefile_workflow_paper_full_profiles.py tests/test_experiment_matrix_detect_stage_input_binding.py::test_run_stage_sequence_skips_ablation_overrides_when_cfg_snapshot_has_no_ablation -q`
   - 结果：`34 passed in 0.78s`
6. 结论：
   - 该修复可覆盖“profile 缺 ablation 字段”的旧版本场景，避免第 5 步因 experiment_matrix 0 成功项而提前中止。

### 8.9 新根因复盘与修复（paper mode 与 ablation 变体冲突）

1. 现象：
   - `outputs/colab_complete_workflow/.../outputs/experiment_matrix/artifacts/grid_summary.json` 显示 40/40 失败。
   - 首个失败原因为 `ValueError: latent injection evidence not ok under paper mode`，失败阶段为 `embed`。
2. 根因：
   - `experiment_matrix` 运行 ablation 变体时沿用 `paper_full_cuda` 的 `paper_faithfulness.enabled=true`。
   - 在 ablation 子实验（如禁用几何/禁用内容）下，paper-faithful 注入证据门禁不满足，导致子实验在 embed 阶段全部失败。
3. 修复动作：
   - 文件：`main/evaluation/experiment_matrix.py`
   - 规则：当 `ablation_flags` 非空时，stage overrides 统一追加 `enable_paper_faithfulness=false`。
   - 作用域：仅作用于 experiment_matrix 子实验，不影响 onefile 主链四阶段。
4. 回归测试：
   - 文件：`tests/test_experiment_matrix_detect_stage_input_binding.py`
   - 新增用例：`test_run_stage_sequence_disables_paper_faithfulness_when_ablation_present`
   - 断言：四阶段 overrides 均包含 `enable_paper_faithfulness=false`。
5. 验证结果：
   - 命令：`pytest tests/test_experiment_matrix_detect_stage_input_binding.py tests/test_onefile_workflow_paper_full_profiles.py -q`
   - 结果：`34 passed in 0.78s`

### 8.10 论文复现实验口径收敛（paper_full_cuda 默认 baseline-only）

1. 背景：
   - 用户当前目标为“论文复现实验/最终结论”，要求输出保持 paper-faithful 口径。
2. 问题：
   - `paper_full_cuda` 的 onefile 主流程此前会执行 `experiment_matrix.ablation_variants`，导致矩阵子实验混入 ablation 场景。
3. 调整：
   - 文件：`scripts/run_onefile_workflow.py`
   - 在 `_prepare_profile_cfg_path` 中，针对 `paper_full_cuda` 运行期配置强制：
     - `experiment_matrix.ablation_variants = [{}]`
   - 语义：paper 主流程仅跑 faithful baseline；ablation 需走独立实验流程。
4. 回归测试：
   - 文件：`tests/test_onefile_workflow_paper_full_profiles.py`
   - 新增用例：`test_prepare_profile_cfg_path_sets_paper_matrix_baseline_only`
   - 断言：运行期 profile 配置中的 `experiment_matrix.ablation_variants` 固定为 `[{}]`。
5. 验证结果：
   - 命令：`pytest tests/test_onefile_workflow_paper_full_profiles.py::test_prepare_profile_cfg_path_sets_paper_matrix_baseline_only tests/test_onefile_workflow_paper_full_profiles.py tests/test_experiment_matrix_detect_stage_input_binding.py -q`
   - 结果：`36 passed in 0.88s`

### 8.11 配置职责拆分与消融配置落位（paper vs ablation）

1. 背景：
   - 用户明确要求：`paper_full_cuda.yaml` 仅用于论文级机制生成验证；消融实验需独立配置。
2. 调整：
   - 文件：`configs/paper_full_cuda.yaml`
   - 变更：删除 `experiment_matrix.ablation_variants` 默认消融定义，仅保留 paper 主流程矩阵配置语义。
   - 目的：防止主配置承载消融逻辑，避免实验口径混用。
3. 新增文件：
   - 文件：`configs/ablation/paper_ablation_cuda.yaml`
   - 变更：新增 ablation 专用配置，包含 `experiment_matrix.ablation_variants`（content/geometry/anchor/LF/HF 五类模块消融）。
   - 目的：将消融研究从 paper 主配置中解耦，形成独立入口。
4. 回归验证：
   - 命令：`pytest tests/test_onefile_workflow_paper_full_profiles.py tests/test_experiment_matrix_detect_stage_input_binding.py -q`
   - 结果：`35 passed in 0.76s`

### 8.12 消融配置可操作性注释补充

1. 背景：
   - 用户要求明确“哪里改可实现不同模块消融”，并在配置内解释各开关语义。
2. 调整：
   - 文件：`configs/ablation/paper_ablation_cuda.yaml`
   - 在 `ablation` 段新增说明：
     - 各开关语义（`enable_content/enable_geometry/enable_anchor/enable_lf/enable_hf`）
     - `lf_only/hf_only` 互斥说明
     - `null/true/false` 三种取值的作用边界
   - 在 `experiment_matrix.ablation_variants` 段新增说明：
     - 单模块消融、联合消融、基线恢复（`{}`）的添加方式
     - 变体键必须先在 `ablation` 段声明的约束
3. 结果：
   - 配置文件内已具备自解释能力，可直接按注释扩展消融矩阵，无需额外脚本改动。
