# 端到端完整性审计报告

> 审计版本号：2026-03-03-1100
> 审计方式：静态代码审计 + 真实产物字段复核 + 测试套件运行 + 双报告交叉校验
> 审计方法：从目标字段反向追溯至代码入口，禁止在"第一个报错点"截断
> 产物区分声明：所有引用字段均来自 `outputs/colab_complete_workflow/colab_run_paper_full_cuda/records/`，**未混用** `audit_diagnostics/` 下的本地诊断产物

---

## 一、审计输入与差异声明

### 1.1 审计来源

本报告综合以下两份审计报告，对全部争议点进行代码级逐一核验，最终给出去重后的权威结论：

| 来源 | 版本号 |
|------|--------|
| 端到端完整性审计报告（链路追踪版） | 2026-03-03-1400 |
| 双报告综合审计（前序版） | 2026-03-03-0242 |

### 1.2 两份报告的关键分歧与核验结论

| 争议点 | 0242 报告主张 | 1400 报告主张 | 代码核验结论 |
|--------|--------------|--------------|------------|
| `enable_mask` 键位 | 嵌套于 `mask.enable_mask`，与代码不匹配，掩码路径完全不可达 | 根键 `enable_mask: true`，掩码路径可达（但 proxy fallback）| ✅ **1400 正确**：`paper_full_cuda.yaml:81` 有根键 `enable_mask: true`；`semantic_mask_provider.py:191` 读取 `cfg.get("enable_mask", False)` 正确命中，掩码IS 计算（但因 inspyrenet 权重缺失退化为 proxy） |
| `embed_record.content_evidence.status` | "absent" | "ok" | ✅ **1400 正确**：mask 已计算（proxy），content status="ok"，mask_digest 非 null |
| `detect_record.content_evidence_payload.status` | "absent" | "ok" | ✅ **1400 正确**：HF 通道成功，整体 status="ok"，score=0.628 |
| Score recovery 机制 | 存在，将 absent→ok，污染统计 | 未提及 | ✅ **0242 正确**：`run_onefile_workflow.py:1133-1135`：`status != "failed"` 且有有限 score 候选 → 强制写 `status="ok"` |
| Ground truth 注入 | 存在，从单一 detect 记录复制+注入标签 | 未提及 | ✅ **0242 正确**：`run_onefile_workflow.py:981-987`：深拷贝同一 detect_record 后注入 label/ground_truth/is_watermarked |
| `decision_status` 取值域 | 缺少 `rejected_insufficient_evidence` | 未提及 | ✅ **0242 正确**：`fusion/decision.py:149` 明确限定 `{"decided", "abstain", "error"}` |

**已丢弃（错误）的 0242 主张**：
- ~~enable_mask 键位不匹配导致掩码路径完全不可达~~（实为可达，但 inspyrenet fallback）
- ~~content_evidence.status="absent"~~（实为 "ok"）

---

### 1.3 真实产物关键字段（已代码追踪验证）

**embed_record.json**（`colab_run_paper_full_cuda/records/`）：
```json
{
  "content_evidence": {
    "status": "ok",
    "mask_digest": "5db9e7df5957d66c4b73951952af148536171c9edb9163aef7feb91481132ba3",
    "plan_digest": null,
    "audit": {
      "fallback_used": true,
      "fallback_reason": "semantic_model_unavailable: RuntimeError",
      "mask_impl_id": "semantic_saliency_v1"
    },
    "injection_site_spec": {
      "injection_rule_summary": {
        "injection_mode": "latent_direct_fallback",
        "plan_digest": "1373d24083cae9b9b0b7d29eb1761a5dd26a7efb9adeec114d222dd11efdde4b"
      }
    }
  }
}
```

> **注意**：`content_evidence.plan_digest = null` 但 `injection_site_spec.injection_rule_summary.plan_digest` 非 null，两者不一致（GAP-A3）。

**detect_record.json**：
```json
{
  "detect_runtime_mode": "fallback_identity_v0",
  "content_evidence_payload": {
    "status": "ok",
    "score": 0.62811587,
    "mask_digest": null,
    "plan_digest": "1373d24083cae9b9b0b7d29eb1761a5dd26a7efb9adeec114d222dd11efdde4b",
    "score_parts": {
      "lf_status": "absent",
      "lf_absent_reason": "lf_prc_missing_latents_or_plan_digest",
      "lf_score": 0.47042095,
      "hf_status": "ok",
      "hf_score": 0.9960707
    }
  },
  "geometry_evidence": { "status": "mismatch" },
  "fusion_result": {
    "decision_status": "error",
    "is_watermarked": null
  }
}
```

**calibration_record.json**：
```json
{
  "calibration_samples": 30,
  "threshold_value": -1.0,
  "order_stat_index_0based": 29,
  "target_fpr": 0.01
}
```

**evaluate_record.json**：
```json
{
  "metrics": {
    "tpr_at_fpr_primary": 1.0,
    "fpr_empirical": 0.03333333333333333,
    "n_pos": 30,
    "n_neg": 30
  },
  "reject_rate_by_reason": {
    "geometry_mismatch": 1.0
  }
}
```

### 1.4 测试套件运行结果
```
FAILED tests/test_paper_path_closure_regressions.py::test_image_domain_sidecar_disabled_in_paper_mode
433 passed, 2 skipped, 1 failed (88.87s)
```

---

## 二、端到端路径完整性审计（四条主链路反向追踪）

### 链路 A：语义掩码 → 自适应子空间注入

**目标字段**：
- `embed_record.content_evidence.injection_site_spec.injection_rule_summary.injection_mode == "subspace_projection"`
- `embed_record.content_evidence.plan_digest != null`
- `embed_record.content_evidence.audit.fallback_used == false`

**当前实际状态**：injection_mode=`"latent_direct_fallback"`，plan_digest=`null`，fallback_used=`true`

**反向追踪**：

```
目标字段未达成
  ↑
run_embed.py:~341-380
  plan_digest is None → fallback 分支
  injection_site_spec.injection_rule_summary.injection_mode = "latent_direct_fallback"
  （injection_site_spec 在 inference 前锁定，之后不可更改）
  ↑
subspace_planner_impl.py:plan()
  paper_faithfulness.enabled=True，isinstance(mask_summary, dict) is False
  → raise ValueError("mask_summary required for paper_faithfulness region index spec")
  → except ValueError: plan_digest=None, reason="invalid_subspace_params"
  ↑
embed/orchestrator.py:_build_planner_inputs_for_runtime(cfg, content_evidence_payload=None)
  content_evidence_payload=None → planner_inputs = {"trace_signature": {...}}
  （无 mask_summary 字段；mask_summary 只在 content_evidence_payload 是含 mask_stats 的 dict 时传入）
  ↑
run_embed.py PRE-COMPUTATION 阶段（inference 之前）
  _build_planner_inputs_for_runtime(cfg, trajectory_evidence=None)
  content_evidence_payload 硬编码为 None
  ↑
scripts/run_onefile_workflow.py:_ensure_default_embed_input_image(run_root)
  生成 512×512 渐变 RGB 合成图作为 embed 输入（非真实语义图像）
```

**A-2 并联阻断点（inspyrenet 权重缺失）**：

```
semantic_mask_provider.py:_probe_model_v2_availability()
  mask.semantic_model_path = "../models/inspyrenet/ckpt_base.pth" 在 Colab 不存在
  → RuntimeError → fallback_used=True
  → mask_impl_id = "semantic_saliency_v1"（texture gradient proxy）
  → mask_digest 仍计算成功，但为 proxy 来源（非真实语义显著性）
```

**A-3 并联问题（plan_digest 字段口径不一致）**：

- `embed_record.content_evidence.plan_digest = null`
- `embed_record.content_evidence.injection_site_spec.injection_rule_summary.plan_digest = "1373d240..."` （非 null）
- 根因：`run_embed.py:540-544` 在 orchestrator 写入 record 后，将 PRE-COMPUTATION 阶段构造的 `injection_site_spec` 覆盖写入 `content_evidence`，但未同步写入 `content_evidence.plan_digest`
- 影响：detect 侧 `_resolve_expected_plan_digest()` 从 `injection_site_spec.injection_rule_summary.plan_digest` 找到"1373d..."作为期望 plan_digest（非 null），导致 forced_absent=False，但 LF decoder 仍无法基于 fallback plan_digest 重构真实子空间 basis

**阻断点汇总（链路 A）**：

| 序号 | 阻断点 | 位置 | 类型 |
|------|--------|------|------|
| A-1 | PRE-COMPUTATION 时 content_evidence_payload=None → planner 无 mask_summary → paper_faithfulness ValueError → plan_digest=null | `run_embed.py:~277-380` | 代码时序缺陷 |
| A-2 | inspyrenet 权重不存在 → mask 退化为 texture gradient proxy | `semantic_mask_provider.py:~295-310` | 环境缺失 |
| A-3 | plan_digest 字段口径不一致（content_evidence.plan_digest=null vs injection_site_spec 中非 null）| `run_embed.py:540-544` | 写入逻辑缺陷 |

---

### 链路 B：detect 端内容证据产出

**目标字段**：
- `detect_record.content_evidence_payload.score_parts.lf_status == "ok"`
- `detect_record.detect_runtime_mode == "real"`

**当前实际状态**：lf_status=`"absent"`，detect_runtime_mode=`"fallback_identity_v0"`

**反向追踪**：

```
目标字段未达成
  ↑
detect/orchestrator.py:~709（real 模式门控，4 个条件全部必须满足）
  (1) detect_lf_status == "ok"         ← 当前 FAIL（"absent"）
  (2) subspace_consistency_status != "inconsistent"  ← "absent"（通过）
  (3) runtime_built == True            ← 需真实 SD3 pipeline
  (4) not synthetic_pipeline_runtime   ← 需真实 pipeline
  → 条件 1 失败 → detect_runtime_mode = "fallback_identity_v0"
  ↑
LF PRC decoder 无法重构 basis
  embed_record.content_evidence.plan_digest = null（链路 A-1 失败的级联）
  → _resolve_expected_plan_digest() 找到 fallback plan_digest="1373d..."（来自 A-3 的错误字段）
  → LF decoder 期望真实子空间 plan_digest + final_latents，fallback plan_digest 无对应 basis
  → lf_absent_reason = "lf_prc_missing_latents_or_plan_digest"
```

**阻断点汇总（链路 B）**：

| 序号 | 阻断点 | 说明 |
|------|--------|------|
| B-1 | embed plan_digest=null（A-1 的级联）→ LF basis 不存在 → lf_status="absent" | 依赖 A-1 修复 |
| B-2 | detect_runtime_mode 无法进入 real（条件 1 由 B-1 阻断；条件 3/4 需真实 SD3 pipeline） | 需先解决 A-1 |

---

### 链路 C：detect 最终判决可达性

**目标字段**：
- `detect_record.fusion_result.decision_status ∈ {"decided", "abstain"}`
- `detect_record.fusion_result.is_watermarked != null`

**当前实际状态**：decision_status=`"error"`，is_watermarked=`null`

**反向追踪**：

```
目标字段未达成
  ↑
fusion/decision.py:NeumanPearsonFusionRule.fuse()
  validate_fusion_inputs(content_evidence, geometry_evidence)
  → geometry_evidence.status = "mismatch"
  → 返回 (False, "geometry_mismatch")
  → 早退 → FusionDecision(is_watermarked=None, decision_status="error")
  ↑
geometry_evidence.status = "mismatch"（100% 样本：reject_rate_by_reason.geometry_mismatch=1.0）
  geometry 链在 Colab 环境下无真实 SD3 pipeline → sync 无法正常执行
  → 执行了但结果不一致（"mismatch" 非 "absent"）
```

**C-2 并联问题（decision_status 取值域缺失）**：
- `fusion/decision.py:149` 明确限定取值域为 `{"decided", "abstain", "error"}`
- 研究目标可能需要 `"rejected_insufficient_evidence"` 来语义区分"不足够"与"错误"
- 当前即使 geometry 修复，content_score 不足时也只能返回 `"abstain"`（非目标语义）

**C-3 并联问题（NP threshold artifact 缺失）**：
- 即使 geometry 修复，`allow_threshold_fallback_for_tests=false`，fusion 内 `get_np_threshold()` 需从 `__thresholds_artifact__` 读取阈值
- 当前无有效 threshold artifact → 抛 ValueError → decision 失败

**阻断点汇总（链路 C）**：

| 序号 | 阻断点 | 说明 |
|------|--------|------|
| C-1 | geometry_evidence.status="mismatch" → decision_status="error" | 100% 样本受影响 |
| C-2 | decision_status 取值域缺失语义区分 | 代码设计缺陷 |
| C-3 | NP threshold artifact 缺失 | 依赖上游 calibrate 产出有效阈值 |

---

### 链路 D：TPR@FPR 统计指标可计算性

**目标字段**：
- `evaluate_record.fpr_empirical <= target_fpr (0.01)`
- 指标具有真实统计意义

**当前实际状态**：tpr=1.0，fpr=0.033（> target 0.01），指标**不具真实统计意义**

**反向追踪**：

```
evaluate_record.fpr_empirical=0.033 > 0.01（超标）
  ↑
calibration_record.threshold_value = -1.0（sentinel 值）
  order_stat_index_0based=29 → 30 个负样本的 max = -1.0
  ↑
30 个负样本 detect 均在 fallback 模式，content_score 退化（null 映射 -1.0）
  ↑
scripts/run_onefile_workflow.py:_prepare_detect_records_with_minimal_ground_truth()
  深拷贝同一份 detect_record → 注入 is_watermarked=True（正样本）/ False（负样本）
  30 对 = 60 条记录，正负样本内容完全相同，仅 label 不同（line 981-987）
  ↑
正负样本分数完全相同（0.628）
  → 因 threshold=-1.0，ALL 样本 score=0.628 >> -1.0 → 全判为"有水印"
  → TP=30, FP=1（负样本中 score 恰好低于 -1.0 的只有 1 个）
    实际：fpr=1/30=0.033，tpr=1.0
  
另一缺陷：score recovery（run_onefile_workflow.py:1133-1135）
  若负样本 content_evidence_payload.status != "ok"，
  但存在有限 score 候选（如 hf_score=0.996）
  → 强制改写 status="ok"、score=recovered_score
  → calibrate 基于被改写的值计算阈值，统计意义不成立
```

**阻断点汇总（链路 D）**：

| 序号 | 阻断点 | 说明 |
|------|--------|------|
| D-1 | ground truth 由复制单一 detect_record + 注入标签构造 | 正负样本无实质区别，统计意义不成立 |
| D-2 | score recovery 强制将 non-ok status 改写为 "ok" | 失败语义被虚假打通，污染 NP 校准 |
| D-3 | threshold=-1.0（sentinel 值） | 30 个负样本 score 均 ≤ -1.0（退化值），无判别力 |

---

## 三、静态代码审计即可确认差距项

### GAP-SIDECAR【HIGH】：paper_full_cuda.yaml 显式启用 image_domain_sidecar（测试失败）

**差距项说明**：
- `paper_full_cuda.yaml` 显式 `detect_runtime.image_domain_sidecar_enabled: true`
- `detect/orchestrator.py:_is_image_domain_sidecar_enabled()`：若 `detect_runtime.image_domain_sidecar_enabled` 为显式 bool → 直接返回，**绕过** paper_faithfulness 检查
- 设计意图：paper faithfulness 模式应禁用 image-domain sidecar，改用 latent 域检测

**测试失败证据**：
```
FAILED tests/test_paper_path_closure_regressions.py::test_image_domain_sidecar_disabled_in_paper_mode
# 失败断言：assert sidecar_enabled(cfg) is False（实际返回 True）
```

**代码位置**：
- `configs/paper_full_cuda.yaml`（`detect_runtime.image_domain_sidecar_enabled: true`）
- `main/watermarking/detect/orchestrator.py:3726-3748`（`_is_image_domain_sidecar_enabled`）

**修复**：将 YAML 中该字段删除或改为 `false`，使 paper_faithfulness 逻辑生效

**修复后下一阻断点**：HF detection 切换为 latent 域方式，需确认 `detect_final_latents` 可用性

---

### GAP-A1【HIGH-CRITICAL】：injection_mode 恒为 latent_direct_fallback（PRE-COMPUTATION 时序缺陷）

**差距项说明**：
- `run_embed.py` PRE-COMPUTATION 阶段在 inference 前调用 SubspacePlanner，`content_evidence_payload=None`
- planner_inputs 无 mask_summary → paper_faithfulness 门控抛 ValueError → plan_digest=null
- injection_site_spec 在 inference 前即锁定（一旦创建无法更改）→ injection_mode 永久为 `latent_direct_fallback`

**证据位置**：
- `main/cli/run_embed.py:~277-380`（PRE-COMPUTATION 到 fallback 分支）
- `main/watermarking/content_chain/subspace/subspace_planner_impl.py:plan()`（paper_faithfulness 门控）
- `main/watermarking/embed/orchestrator.py:_build_planner_inputs_for_runtime()`

**目标字段差异**：期望 `injection_mode="subspace_projection"`，实际 `"latent_direct_fallback"`

**修复**：将 injection_site_spec 创建移至 inference 之后，使用 orchestrator 计算的有效 plan_digest

**修复后下一阻断点**：GAP-A2（inspyrenet 权重缺失导致 mask 仍为 proxy，plan 语义正确性存疑）

---

### GAP-A2【HIGH-CRITICAL】：inspyrenet 权重缺失 → 语义掩码退化为 texture gradient proxy

**差距项说明**：
- `mask.semantic_model_path = "../models/inspyrenet/ckpt_base.pth"` 在 Colab 不存在
- `_probe_model_v2_availability()` 返回 False → RuntimeError → silent fallback 到 texture gradient proxy
- `saliency_source="model_v2"` 要求强制使用真实模型，但代码允许 silent fallback

**产物证据**：
```json
"audit": { "fallback_used": true, "fallback_reason": "semantic_model_unavailable: RuntimeError", "mask_impl_id": "semantic_saliency_v1" }
```

**修复**：下载 inspyrenet 权重到正确路径；并建议修改判断逻辑，使权重缺失时 fail-fast（而非 silent fallback）

**修复后下一阻断点**：GAP-A1（时序问题导致 plan_digest 仍为 null）

---

### GAP-A3【HIGH】：plan_digest 字段口径不一致（content_evidence.plan_digest=null vs injection_site_spec 中非 null）

**差距项说明**：
- `embed_record.content_evidence.plan_digest = null`
- `embed_record.content_evidence.injection_site_spec.injection_rule_summary.plan_digest = "1373d24..."` （非 null）
- 根因：`run_embed.py:540-544` 在 orchestrator 写入记录后覆盖写入 `injection_site_spec`，未同步写入 `content_evidence.plan_digest`
- 影响：detect 侧从错误字段找到非 null plan_digest，干扰了 forced_absent 判断，但 LF 仍无法重构（D-3 的一部分原因）

**代码位置**：`main/cli/run_embed.py:540-544`

**修复**：在覆盖写入 injection_site_spec 时，同步写入 `content_evidence.plan_digest`，或禁止在 CLI 层覆盖核心证据字段

**修复后下一阻断点**：GAP-A1（根本时序问题未解决）

---

### GAP-B1【HIGH-CRITICAL】：LF 检测 absent → detect_runtime_mode 无法进入 real

**差距项说明**：
- embed plan_digest=null → LF basis 不可重构
- lf_absent_reason=`"lf_prc_missing_latents_or_plan_digest"`
- detect_runtime_mode 4 个门控条件：条件 1 (`detect_lf_status == "ok"`) 持续 FAIL

**产物证据**：`detect_record.score_parts.lf_status="absent"` → `detect_runtime_mode="fallback_identity_v0"`

**修复**：依赖 GAP-A1 修复，embed 侧产出真实 plan_digest 后，detect 侧才能重构 LF basis

**修复后下一阻断点**：real 模式条件 3/4（需真实 SD3 pipeline 在 detect 端可用）

---

### GAP-C1【HIGH-CRITICAL】：geometry_mismatch → fusion decision_status="error"

**差距项说明**：
- `validate_fusion_inputs()` 检测 geometry_evidence.status="mismatch" → 返回 `(False, "geometry_mismatch")`
- 早退返回 `FusionDecision(is_watermarked=None, decision_status="error")`
- 100% 样本有 geometry_mismatch（`reject_rate_by_reason.geometry_mismatch=1.0`）

**代码位置**：`main/watermarking/fusion/decision.py:NeumanPearsonFusionRule.fuse()`

**修复后下一阻断点**：GAP-C3（NP threshold artifact 缺失，`get_np_threshold()` 抛 ValueError）

---

### GAP-C2【MEDIUM】：decision_status 取值域缺失 rejected_insufficient_evidence

**差距项说明**：
- `fusion/decision.py:149` 限定 `decision_status ∈ {"decided", "abstain", "error"}`
- `"error"` 语义为"流程异常"，`"abstain"` 为"内容证据缺失"；两者无法区分"证据不足但流程健康"
- 若研究目标需要此语义区分，当前实现不可达

**修复**：在 decision.py 添加 `"rejected_insufficient_evidence"` 状态，并同步更新 frozen_contracts.yaml / policy_path_semantics.yaml

**修复后下一阻断点**：policy_path 的冻结语义需明确该状态下的 NP 校准口径

---

### GAP-D1【HIGH】：calibration threshold=-1.0（NP 校准污染）

**差距项说明**：
- 30 个负样本 detect 均在 fallback 模式，content_score 退化（null 映射 -1.0）
- threshold_value = 30 个负样本 99th 百分位 = max = -1.0（order_stat_index_0based=29）
- fpr_empirical = 0.033 > target 0.01
- tpr=1.0 在 threshold=-1.0 下意义存疑（所有正样本 score=0.628 >> -1.0）

**修复**：依赖链路 A/B/C 修复，使正负样本在真实模式下产出有效 score，再重新校准

---

### GAP-D2【HIGH-CRITICAL】：ground truth 由复制单一 detect_record + 注入标签构造，统计意义不成立

**差距项说明**：
- `run_onefile_workflow.py:_prepare_detect_records_with_minimal_ground_truth()`（lines 927-1000+）
- 将同一份 `detect_record.json` 深拷贝 `pair_count` 次，注入 `is_watermarked=True`（正样本）/ `False`（负样本）
- 正负样本内容完全相同，score 相同，仅 label 不同
- TPR / FPR 的统计假设（正负样本独立性、正样本为真实带水印图像）完全不成立

**代码位置**：`scripts/run_onefile_workflow.py:981-987`

**修复**：实现真实双流 embed/detect workflow（watermarked branch + clean branch），在 detect 记录中写入可审计的 `ground_truth_source` 字段

**修复后下一阻断点**：依赖链路 A/B 修复，watermarked branch 的 embed 必须成功进入 subspace_projection 模式

---

### GAP-D3【HIGH】：score recovery 强制将 non-ok status 改写为 "ok"，污染 NP 校准

**差距项说明**：
- `run_onefile_workflow.py:1133-1135`：
  ```python
  if isinstance(recovered_score, float) and status_value != "failed":
      content_payload["score"] = recovered_score
      content_payload["status"] = "ok"
  ```
- 当 content_evidence_payload.status 为 `"absent"` 且存在有限 score 候选（如 `hf_score`）时，直接改写为 `status="ok"`
- 使 calibrate/evaluate 在失败证据上建立 NP 阈值，语义不成立

**代码位置**：`scripts/run_onefile_workflow.py:1133-1135`

**修复**：paper_full_cuda 下若 status != "ok"，应阻断 calibrate/evaluate 并明确报告证据链缺失；若需保持连通，应以独立的 `synthetic_fallback` 口径进行，不覆盖原始 status

**修复后下一阻断点**：calibrate/evaluate 将因缺少有效 status="ok" 的真实 score 而失败，需先修复链路 A/B

---

## 四、需要真实 SD workflow 才能判定的差距项

### 需运行：SubspacePlanner 在 trace_signature Path B 下能否成功 decomposition

**已静态确认**：当前 plan_digest=null 的路径为 `invalid_subspace_params`（paper_faithfulness 门控）
**需运行确认**：若 GAP-A1 修复（时序修复），且 GAP-A2 修复（inspyrenet 权重到位），SubspacePlanner 能否在 `trace_signature` Path B（无真实 SD3 pipeline）下成功完成 SVD decomposition，还是仍需 Path A（真实 pipeline 采样）

### 需运行：geometry 链 "mismatch" vs "absent" 的根本来源

**静态已知**：`geometry_evidence.status="mismatch"`（而非 "absent"），说明 geometry 链有执行但结果不一致
**需运行确认**：具体是 sync_strength 不达标还是 attention anchor 位置不匹配，需真实运行日志确认

---

## 五、消融实验可行性评估

| 消融维度 | 当前基线状态 | 消融可行性 |
|---------|------------|----------|
| enable_content | LF absent，HF ok（via sidecar，但 GAP-SIDECAR 配置错误） | ❌ sidecar 错配导致消融结论受污染 |
| enable_geometry | 100% mismatch | ❌ 当前基线本身失真，消融无意义 |
| enable_hf | HF 通过 image-domain sidecar ok | ❌ 依赖错配的 sidecar，应先修复 GAP-SIDECAR |
| enable_lf | LF absent | ❌ 无信号，无法有意义消融 |
| enable_anchor | attention anchor 作辅锚点 | ❌ 主 sync 未通过，消融无意义 |

---

## 六、差距项优先级汇总与修复路径

| 差距项 | 优先级 | 可立即修复 | 依赖项 |
|--------|--------|-----------|--------|
| GAP-SIDECAR | HIGH | ✅ YAML 1 行修改 | 无 |
| GAP-A1 | HIGH-CRITICAL | ✅ 代码重构 | 无 |
| GAP-A2 | HIGH-CRITICAL | ✅ 环境准备 | 无 |
| GAP-A3 | HIGH | ✅ 写入逻辑修复 | 无 |
| GAP-B1 | HIGH-CRITICAL | ⛔ 依赖 A1 | GAP-A1 |
| GAP-C1 | HIGH-CRITICAL | ⛔ 需真实 SD3 pipeline | SD3 环境 |
| GAP-C2 | MEDIUM | ✅ 代码扩展 | 无（但需同步冻结合同） |
| GAP-D1 | HIGH | ⛔ 依赖上游全链路 | GAP-A1/B1/C1 |
| GAP-D2 | HIGH-CRITICAL | ✅ workflow 重构 | 无（重构本身） |
| GAP-D3 | HIGH | ✅ 删除 score recovery 改写逻辑 | 无 |

**推荐修复顺序**：

1. **立即可执行**（无外部依赖）：
   - GAP-SIDECAR：YAML 改 `image_domain_sidecar_enabled: false` 或删除该行
   - GAP-A3：修复 plan_digest 字段写入口径一致性
   - GAP-D3：删除或硬阻断 score recovery 改写 status 逻辑（`run_onefile_workflow.py:1133-1135`）
   - GAP-C2：扩展 decision_status 取值域（可选，需同步冻结合同）

2. **代码重构**（可并行进行）：
   - GAP-A1：injection_site_spec 创建移至 inference 之后
   - GAP-D2：实现真实双流 workflow（watermarked + clean）

3. **环境准备**（并行）：
   - GAP-A2：下载 inspyrenet 权重到正确路径

4. **级联修复**（依赖上游）：
   - GAP-B1：A1 修复后自动修复
   - GAP-D1：全链路修复后重新运行 calibrate + evaluate

5. **需真实 SD3 runtime 验证**：
   - GAP-C1：geometry sync 链路（需 Colab CUDA 环境专项验证）

---

## 历史修复操作因果溯源分析

### A. 由历史修复操作明确引入（回归型）

| 差距项 | 引入周期 | 引入证据 |
|--------|----------|---------|
| **GAP-SIDECAR** | **0830 修复** | 2026-03-02-0830修复记录.md：配置变更节明确记录"原始行为（修复前）：`detect_runtime.image_domain_sidecar_enabled: false`；变更操作：修改为 `true`"。0830 主动开启了 sidecar，但测试未同步更新，导致当前测试失败 |
| **GAP-D2** | **0530 修复** | 2026-03-02-0530修复记录.md：5.2 节新增 `_prepare_detect_records_with_minimal_ground_truth`，明确写出"由 source detect record 生成 positive 与 negative 两份记录；写入 label/ground_truth/is_watermarked"——正是当前 GT 伪造的代码来源 |
| **GAP-D1** | **0530 修复**（D2 的二阶效应） | 同一节："负样本在 `content_evidence_payload.status == "ok"` 时将 score 设为 `-1.0`"；0830 审计报告将此命名为 P1-2，定位在 `run_onefile_workflow.py:999`。-1.0 分数进入 calibrate → 阈值退化为 -1.0（GAP-D1） |
| **GAP-D3** | **0300 修复**（0530 沿用） | 0300修复记录.md 第 4 节（0300-2 追加修复）：为恢复 calibrate 可跑通而改写 run_onefile_workflow.py，引入了 score recovery。0830 修复记录七.2-3 节明确承认其为"潜在残留风险：sidecar disabled 分支会把失败语义改写为可用分数输入" |

---

### B. 历史修复计划中有记录但未执行（挂单型）

| 差距项 | 计划周期 | 未执行证据 |
|--------|----------|-----------|
| **GAP-A1**（injection_mode 永远 fallback）| 0830 计划未执行 | 0830修复记录.md：冒头即声明"**未生效**：`orchestrator.py` 的 P0-2/P0-3 路径兜底改造，本轮未改动"；0830 修复方案 2.1 节曾计划修复，但实际代码变更清单无 `orchestrator.py` |
| **GAP-B1**（LF absent，detect_runtime_mode 无法进入 real）| 0830 计划未执行 | A1 的级联效应；P0-2/P0-3 未执行，LF 链自然仍断 |
| **GAP-C2**（decision_status 取值域缺少 rejected_insufficient_evidence）| 0530 延期 | 0530 修复方案提及该项为后续扩展，实际 6 个修复周期均未实施 |

---

### C. 预存架构差距项（非任何修复操作引入）

| 差距项 | 性质 | 说明 |
|--------|------|------|
| **GAP-A2**（inspyrenet 权重缺失）| 环境依赖缺失 | 6 个修复周期均未解决模型权重自动化获取；0830 仅更新了 Notebook 下载单元，需手动执行 |
| **GAP-A3**（content_evidence.plan_digest=null vs injection_site_spec 非空）| 预存写入口径不一致 | 所有修复记录均未提及 plan_digest 字段一致性；属于 embed→detect 跨阶段拼接时的设计遗留 |
| **GAP-C1**（geometry_mismatch → fusion 返回 error）| A1/B1 的级联效应 | geometry sync 逻辑从未被任何修复周期触及；属于真实注入链路不通导致的必然结果 |

---

### 总结

| 类型 | 差距项 | 数量 |
|------|--------|------|
| **由历史修复操作引入（回归）** | GAP-SIDECAR、GAP-D1、GAP-D2、GAP-D3 | **4 项** |
| **历史修复计划有记录但未执行（挂单）** | GAP-A1、GAP-B1、GAP-C2 | **3 项** |
| **预存架构差距（非修复引入）** | GAP-A2、GAP-A3、GAP-C1 | **3 项** |

**关键结论**：本轮 10 个差距项中，**4 项属于修复操作引入的回归**，其中 GAP-D1/D2/D3 均来自同一根因链——0530 为解决 calibrate 样本闭环而引入 GT 伪造逻辑（D2），其 -1.0 score 注入导致阈值退化（D1），0300 的 score recovery 补丁进一步掩盖了失败语义（D3）。GAP-SIDECAR 是 0830 的单一配置变更未同步测试预期所致。3 项挂单差距（A1/B1/C2）是在修复记录中被明确标注"未执行"或"延期"的条目。