审查我现在项目是否还存在风险。审计我的项目和我的目标还有什么差距。审计项目是否有利于后期消融实验。
如果静态代码审查不能确定，如果需要经过真实sd模型进行全流程workflow，你需要告知我“此项风险需要真实sd模型workflow才能判断”。
按照“静态代码审计即可确认差距项”，“需要真实sd模型workflow确认差距项”分别给我当前项目距离我的研究目标的差距。如果你需要进行审计测试，scripts/和tests/有测试相关脚本。如果能够通过scripts/和tests/测试判断的风险或差距项目，列入“静态代码审计即可确认差距项”。
Colab_Workflow.ipynb是我将在colab上使用的完整工作流执行notebook，运行期配置采用configs/paper_full_cuda.yaml（profile=paper_full_cuda），paper_faithfulness_spec.yaml仅作为规范文件；执行流程脚本采用scripts/run_onefile_workflow.py。所以你同时检查配置和脚本是否能够生成符合我的研究要求的最终输出。输出能否满足判断“需要真实sd模型workflow确认差距项”
以下是我的研究目的，如果有任何不清晰的地方，请随时提问以获取更多细节。
你可以自助调用.github\skills中的技能来审计我的项目。
---
基于语义显著性的自适应流形水印
论文创新点：不再对所有图像使用统一的嵌入强度或固定的低维子空间，而是根据每张图像的语义显著性动态调整水印嵌入的“流形区域”。
具体方法：自适应子空间选择：在 Shallow Diffuse的基础上，不单纯依赖Jacobian矩阵的低秩性，而是结合图像的语义掩码。在纹理复杂区域（高频）利用 T2SMark 的尾部截断策略增强鲁棒性；在平滑区域（低频）利用 PRC-Watermark 的编码策略保证隐蔽性。抗几何攻击机制：引入一个轻量级的空间同步模块（类似于传统水印中的同步模板），但在Latent空间中实现。利用扩散模型的Self-Attention图作为具有几何不变性的特征锚点，将水印信息绑定在Attention Map的相对关系上，而非绝对像素位置上。
预期结果：在保持高生成质量的同时，显著提升对抗几何变换（旋转、缩放）和高斯噪声的鲁棒性，超越 T2SMark 的表现。
---
本文方法属于**基于扩散模型 latent 空间的初始噪声调制型水印**。与传统初始噪声调制方法直接检测噪声相关性不同，本文将噪声扰动视为**诱导结构化证据的载体**，通过后验可观测特征构建**可审计、可拒绝的证据体系**，而非依赖单一连续分数完成判决。
系统采用**内容域证据链与几何结构证据链并列的多证据链设计**。两条证据链在实现、语义与失败机制上均保持独立，仅通过统一的 EvidenceBundle 结构在策略层汇合。任一证据链均允许失败，失败被视为合法且有信息量的系统状态，而非异常。
内容域引入**内容自适应子空间选择机制**，显式区分不同频率区域在鲁棒性与隐蔽性上的角色分工：
1. **高频区域**：采用类似 T2SMark 的尾部截断或相关增强策略，其目标是构造**对攻击强度单调敏感、可自然失败的证据子空间**，在强攻击下明确转入失败状态，避免伪鲁棒残留。
2. **低频区域**：采用类似 PRC-Watermark 的编码约束型策略，以保证视觉隐蔽性，并在无攻击或弱攻击条件下提供稳定证据。
内容域明确引入 `content.status = failed` 的失败语义，避免在证据已不可信时输出低判别力分数。
几何证据链通过**显式空间同步与低阶几何反演**应对 crop、resize、rotate 等几何攻击。检测端仅在同步信号显著、几何参数可稳定估计时输出几何证据；否则明确返回失败状态。
扩散模型的 Self-Attention Map 不作为主几何锚点，仅在主同步路径成功后作为辅锚点参与一致性评估，避免产生“稳定但不可信”的伪几何证据。
所有证据仅以显式字段（状态、质量分数、失败原因）进入策略层。最终判决由**唯一、冻结的策略与决策层**完成，显式选择 `policy_path`，禁止证据链内部的隐式降级、跨链调用或自由组合，从系统层面消除“隐式几何”与“隐式多链”风险。
方法在统计意义上严格遵循**Neyman–Pearson 范式**进行阈值校准，系统性压低假阳性率（FPR），允许在强攻击场景下提高假阴性率（FNR）。该取舍与“可拒绝证据链”设计一致，优先保证判决可信性与审计一致性，而非追求表面鲁棒。
---

**静态代码审计即可确认差距项**

1. 多协议结果门禁未闭环：`onefile`允许失败继续，后续只检查 `compare_summary.json` 存在，不校验“全部成功”。证据：[run_onefile_workflow.py](/d:/Code/CEG-WM/scripts/run_onefile_workflow.py:718), [run_onefile_workflow.py](/d:/Code/CEG-WM/scripts/run_onefile_workflow.py:730), [assert_paper_mechanisms.py](/d:/Code/CEG-WM/scripts/assert_paper_mechanisms.py:506)  
2. `paper`级 signoff 未覆盖 `protocol_compare` schema 门禁。证据：[run_freeze_signoff.py](/d:/Code/CEG-WM/scripts/run_freeze_signoff.py:45), [run_freeze_signoff.py](/d:/Code/CEG-WM/scripts/run_freeze_signoff.py:62)  
3. 审计存在“非当前 run_root 强绑定”风险：多数审计只收 `repo_root`，协议覆盖审计扫描 `outputs/**` 且支持 union，受历史产物影响。证据：[run_freeze_signoff.py](/d:/Code/CEG-WM/scripts/run_freeze_signoff.py:781), [audit_attack_protocol_report_coverage.py](/d:/Code/CEG-WM/scripts/audits/audit_attack_protocol_report_coverage.py:45), [audit_attack_protocol_report_coverage.py](/d:/Code/CEG-WM/scripts/audits/audit_attack_protocol_report_coverage.py:350)  
4. 状态语义不一致：冻结语义强调 `failed`，但 `execution_report` 校验用 `fail`；且 detect 中 `execution_report` 含硬编码状态，不是由链路真实状态严格推导。证据：[policy_path_semantics.yaml](/d:/Code/CEG-WM/configs/policy_path_semantics.yaml:111), [frozen_contracts.yaml](/d:/Code/CEG-WM/configs/frozen_contracts.yaml:639), [freeze_gate.py](/d:/Code/CEG-WM/main/policy/freeze_gate.py:1788), [orchestrator.py](/d:/Code/CEG-WM/main/watermarking/detect/orchestrator.py:669)  
5. NP 校准“null 来源”与实现未闭合：校准收分仅按 `status=ok`，未按标签/错钥来源过滤；但元数据固定写 `null_source=wrong_key`。证据：[orchestrator.py](/d:/Code/CEG-WM/main/watermarking/detect/orchestrator.py:2540), [orchestrator.py](/d:/Code/CEG-WM/main/watermarking/detect/orchestrator.py:2075)  
6. 实验矩阵缺失标签默认当正样本（`True`），会污染 FPR 解释。证据：[experiment_matrix.py](/d:/Code/CEG-WM/main/evaluation/experiment_matrix.py:857)  
7. `run_onefile_workflow.py` 默认 `signoff-profile=baseline`，脱离 Notebook 运行会降级门禁。证据：[run_onefile_workflow.py](/d:/Code/CEG-WM/scripts/run_onefile_workflow.py:1082)  
8. `paper_full_cuda` 下 embed/detect 内容模式冲突：配置默认 `detect.content.enabled=true`，而 embed 只传 `image_path`，会触发 detector 的 `detector_no_plan_expected` 路径。证据：[paper_full_cuda.yaml](/d:/Code/CEG-WM/configs/paper_full_cuda.yaml:60), [unified_content_extractor.py](/d:/Code/CEG-WM/main/watermarking/content_chain/unified_content_extractor.py:101), [orchestrator.py](/d:/Code/CEG-WM/main/watermarking/embed/orchestrator.py:998), [content_detector.py](/d:/Code/CEG-WM/main/watermarking/content_chain/content_detector.py:145), [run_onefile_workflow.py](/d:/Code/CEG-WM/scripts/run_onefile_workflow.py:286)  
9. 几何链主辅关系与研究目标不一致：当前是 anchor 先执行，sync 后执行；sync v2 还依赖 anchor `relation_digest`，并非“sync 成功后 attention 仅作辅锚”。证据：[orchestrator.py](/d:/Code/CEG-WM/main/watermarking/detect/orchestrator.py:3923), [orchestrator.py](/d:/Code/CEG-WM/main/watermarking/detect/orchestrator.py:3941), [latent_sync_template.py](/d:/Code/CEG-WM/main/watermarking/geometry_chain/sync/latent_sync_template.py:689)  
10. `experiment_matrix` 严格门禁与测试夹具不一致，当前仍有可复现失败，影响批量消融可回归性。证据：[test_batch_runner_independent_runs.py](/d:/Code/CEG-WM/tests/test_batch_runner_independent_runs.py:94), [experiment_matrix.py](/d:/Code/CEG-WM/main/evaluation/experiment_matrix.py:724)  


**需要真实SD模型workflow确认差距项**
1. 此项风险需要真实SD模型workflow才能判断：运行时是否稳定拿到“真实self-attention map”（而非缺失/代理），并形成可用几何证据。相关实现点：[infer_runtime.py](/d:/Code/CEG-WM/main/diffusion/sd3/infer_runtime.py:349)、[attention_anchor_extractor.py](/d:/Code/CEG-WM/main/watermarking/geometry_chain/attention_anchor_extractor.py:669)。
2. 此项风险需要真实SD模型workflow才能判断：对`rotate/resize/crop/noise`的鲁棒性是否实测优于T2SMark（不是结构上“可比较”，而是统计显著优于）。
3. 此项风险需要真实SD模型workflow才能判断：NP目标FPR是否在真实负样本/错钥样本上达标，同时FNR上升是否符合你预期取舍。
4. 此项风险需要真实SD模型workflow才能判断：语义自适应LF/HF机制在真实图像上的“隐蔽性-鲁棒性”权衡是否成立（视觉质量与检测稳定性）。
5. 此项风险需要真实SD模型workflow才能判断：Colab完整流程在真实GPU资源约束下的稳定性（失败率、重试后一致性、耗时）。

消融实验方面：基础设施是有利的（ablation开关归一化+digest绑定+matrix分组都在），但默认paper配置并不会自动产出有判别力的ablation实验设计与负样本统计，需要你显式补实验矩阵轴与样本策略。
