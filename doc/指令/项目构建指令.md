为我提供一段可用于与 Codex 对话的规范描述文本。
要说明下一步需要做的的具体内容、整体构建思路，需实现的方法与函数等核心信息。阐明构建过程中需要注意的事项，以及该阶段内容与哪些门禁绑定点、冻结约束点存在关联，明确对应的约束关系。
要求 Codex 先读取我的项目代码（其中 configs 文件夹存放冻结约束文件，main 文件夹为项目主文件）。基于此生成满足实现细节高风险点与硬约束、包含冻结约束绑定点的合规代码，且确保函数 / 方法命名符合我的命名风格。
需要 Codex 读取真实仓库代码，自行清晰识别出项目中需要改动、禁止改动以及需同步改动的文件。
最后需要自动进行测试以查找代码是否有不合规或风险项。
可以使用.github\skills中的技能。
最终目的是生成满足风险和合规要求的代码。
---
给出后你需要自行审计对话的描述是否完整包含全部内容要求，是否包含冻结边界与门禁绑定点，改动范围与文件清单，模块职责划分，高风险点与硬约束，需要同步改动的点与不允许改动的点或者其他遗落项，是否不会引入新风险和新的阻断项。直接告诉我答案是或者否。如果否的话，补充并重新给我一版与 Codex 对话的描述。直到给我必然收敛的终版 Codex 指令。能够实现目标且不引入新风险和新不合规项