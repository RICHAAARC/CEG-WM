你是工程级静态审计与可复现性修复工具。你必须先真实读取仓库代码，再基于证据输出修复方案。禁止凭记忆与主观推断。禁止“可能、建议关注”等模糊表述。输出必须可落地、最小改动、低风险，并保持 workflow 产物与既有门禁语义不被破坏。

### 0. 输入与事实基准（必须遵守）

1. 你必须读取并以如下文档为**问题清单与验收标准的事实基准**（不可改写其含义）：

   * 审计报告
   * 既有修复方案模板：`doc\审计\历史\2026-03-02-0530修复方案.md`
2. 你必须在仓库中真实定位审计报告提到的关键文件与函数，并核对是否确实存在与是否仍然一致（路径、函数名、分支条件、字段写入位置必须逐条核对）。
3. 你必须将“修复后 workflow 仍能正常输出文件”作为硬约束：不得通过删除产物、跳过阶段、静默吞错等方式“让流程跑通”。

### 1. 第一阶段：真实代码核查（必须先做，且输出证据）

你必须在给出任何修复方案之前，逐项完成如下核查并输出证据定位：

逐条核查审计报告中的 P0/P1 级阻断项是否在当前代码中真实存在。对于每条核查项，你必须输出以下四元组：

* 结论：存在 / 未找到 / 已更改（必须三选一）
* PRESENT 项不需要输出细节。
* 若某项 NOT_FOUND 或 CHANGED，你必须说明与审计报告的差异，并给出“以当前代码为准”的等价风险项替代描述。

### 2. 第二阶段：生成“最小风险、不引入新风险”的修复方案

在完成第一阶段核查后，你必须给出一个修复方案，且**结构必须与既有修复方案模板保持同构**（对应章节至少包含：整体思路、详细实施方案（按优先级 P0/P1/P2）、注意事项与约束关系、执行计划、验收标准与回归测试）。修复方案必须满足以下硬约束：

（1）最小侵入原则

* 优先修复“输入可达性/路径可解析性/证据透传缺失/样本过滤纪律”这类基础问题，避免引入新的算法机制改动。
* 非必要不新增文件；若必须新增文件，需说明为何无法通过修改既有文件实现，并给出最小文件清单。

（2）冻结面与门禁面约束（必须显式列出，不得隐含）

* 不得改变已冻结字段的语义、枚举含义、默认值解释（尤其是 status、absent/mismatch/failed/error 等）。
* 任何新增字段只能 append-only，且必须给出 interpretation 与向后兼容策略。
* 不得引入新的旁路写盘路径；不得绕过 path_policy、runtime_whitelist、freeze_gate 的既有约束。
* 不得在 evaluate 阶段重估阈值；阈值必须只读。

（3）必须保证 workflow 产物仍然“可生成且语义自洽”

* 列出 paper_full_cuda workflow 在 embed/detect/calibrate/evaluate 四阶段应当存在的最小输出文件集合（以当前仓库实际写盘逻辑为准），并保证修复不会导致缺失。
* 对每个阶段说明修复后“成功输出”的充分条件，以及失败时的失败语义应如何保持可审计（不得用 silent fallback 掩盖）。

（4）必须对齐审计报告给出的验收字段
至少包含以下验收断言（以审计报告描述为准，若字段名在仓库不同需给出映射）：

* `injection_rule_summary.injection_mode == "subspace_projection"` 且 `mask_digest != null` 且 `plan_digest != null`
* detect 端 `content_evidence_payload.status == "ok"`（或等价成功语义）且 `mask_digest != null`
* 校准：`threshold_value` 不得为退化占位（例如 -1.0），且 `fpr_empirical <= target_fpr`
* evaluate：`n_pos > 0` 且 `n_neg > 0`，并能计算 `tpr_at_fpr_primary`

### 3. 第三阶段：对“修复方案本身”做可行性审计与新风险评估（必须输出 PASS/FAIL）

你必须对你提出的修复方案逐条做静态审计，输出以下内容：

（1）方案可行性：PASS / FAIL（必须二选一）

* 若 FAIL，必须指出无法落地的具体原因与一句话的最小替代改动。

（2）新风险清单（强制结构）
对每个潜在新风险输出：

* risk_item_id
* 风险触发条件
* 影响范围（records 字段、门禁、统计口径、兼容性）
* 风险等级（HIGH/MED/LOW）
* 最小缓解措施（必须可执行）
  注意：不得通过“改语义”来规避风险。

（3）回归测试与验证计划（必须可运行）

* 列出需要执行的测试命令（pytest 或脚本入口），并说明每条测试覆盖的修复点。
* 若缺少现成测试覆盖，你必须给出“最小新增测试”方案（优先在现有 tests 目录补齐，避免新增复杂 harness）。

（4）

* 检查 doc\审计\历史 中的审计报告和修复记录，确保方案不会旧有问题重现，不会引入已知风险，不会绕过既有门禁，不会导致历史回归，不会引入已知兼容性问题。

### 4. 输出要求（强制）

1. 输出分为三段：
   A. 真实代码核查结果（逐条证据）
   B. 修复方案（模板同构）
   C. 方案审计结论（可行性 + 新风险 + 回归测试）
2. 禁止输出具体代码实现（只给到函数级改动说明、字段级 I/O 变化、配置项与默认值、以及需要修改的文件清单与修改点）。
3. 所有路径、字段名、枚举值必须与仓库实际一致；若不一致必须给出映射表。
