本指令用于指导对本项目进行**端到端完整性审计**。历次审计复审的根本原因是：审计停在"第一个报错点"而不是追踪到"研究目标对应输出字段是否真实可达"。本次审计必须遵守以下约束，否则审计结论无效。

---

## 第一步：真实读取代码（必须逐文件读取，禁止依赖记忆）

在开始任何分析之前，必须真实读取以下文件的实际内容（使用 read_file 工具，不得依赖对话历史中的摘要）：

**配置文件（必读）**
- `configs/paper_full_cuda.yaml`（全文）
- `configs/frozen_contracts.yaml`
- `configs/policy_path_semantics.yaml`
- `configs/runtime_whitelist.yaml`

**核心流程文件（必读关键段落）**
- `scripts/run_onefile_workflow.py`（重点：embed/detect/calibrate/evaluate 各阶段的输入构造与输出写入逻辑，仅作为流程编排，项目发布不包含）
- `main/cli/run_embed.py`（重点：content_inputs 构造、plan_digest 分支、fallback 分支）
- `main/cli/run_detect.py`（重点：preplan 调用、主链路 content_inputs 构造、real/fallback 模式门控）
- `main/watermarking/embed/orchestrator.py`（`_build_content_inputs_for_embed`、`_resolve_embed_input_image_path`）
- `main/watermarking/detect/orchestrator.py`（`_build_content_inputs_for_detect`、`_resolve_detect_image_path`、real 模式门控条件、`_build_mismatch_fusion_decision`）
- `main/watermarking/content_chain/semantic_mask_provider.py`（`extract()`、`_probe_model_v2_availability()`）
- `main/watermarking/content_chain/content_detector.py`（`extract()` 的期望输入字段）
- `main/watermarking/fusion/decision.py`（`decision_status` 所有可能值）
- `main/watermarking/geometry_chain/sync/latent_sync_template.py`（uncertainty 门控、sync_strength 默认值）

**真实产物（必读，字段级对比）**
- `outputs/GPU Outputs/colab_run_paper_full_cuda/records/embed_record.json`
- `outputs/GPU Outputs/colab_run_paper_full_cuda/records/detect_record.json`
- `outputs/GPU Outputs/colab_run_paper_full_cuda/records/calibration_record.json`
- `outputs/GPU Outputs/colab_run_paper_full_cuda/records/evaluate_record.json`

**注意**：`outputs/GPU Outputs/audit_diagnostics` 下的产物来自**本地无图像输入的诊断运行**，与 `colab_run_paper_full_cuda/` 是**不同场景的不同产物**，禁止混用字段作为同一根因的证据。

**特别说明**
- 项目核心发布仅有 `configs/` 和 `main/`，不包含 `scripts/` 和 `tests/`，所有项目核心流程和输出结果必须能够通过 `configs/paper_full_cuda.yaml` 配置和 `main/` 目录下的代码实现，禁止依赖 `scripts/` 中的任何代码逻辑。
- `scripts/` 中的代码仅作为审计结构辅助，禁止将其作为产物字段追踪链路的一部分，否则视为链路截断。
- `run_onefile_workflow.py` 仅作为 Colab notebook 的辅助脚本，不作为核心流程的一部分，其目的仅为全流程运行项目以生成结果。
- 如果 `run_onefile_workflow.py` 中某段代码涉及核心流程的输入构造或输出写入逻辑，必须回到 `main/` 目录下对应的代码文件中确认该逻辑是否也存在，并以 `main/` 目录下的代码为准进行追踪分析。
---

## 第二步：端到端路径完整性审计（核心方法论，禁止截断）

### 2.1 强制追踪方法

对每个研究目标对应的输出字段，**必须从目标字段反向追溯到每一个前置依赖，直到代码入口**。追踪链不得在"第一个报错点"截断。

**必须追踪的四条主链路（每条都要追踪到底）：**

**链路 A：语义掩码 → 自适应子空间注入**
```
目标字段：embed_record.injection_rule_summary.injection_mode == "subspace_projection"
          embed_record.content_evidence.mask_digest != null
          embed_record.content_evidence.plan_digest != null

追踪路径（必须逐层验证每个条件是否在当前代码+配置下可满足）：
1. paper_full_cuda.yaml 是否配置 embed.input_image_path 或等价参数？
2. run_embed.py/_build_content_inputs_for_embed() 是否可以返回非 None？
3. SemanticMaskProvider._probe_model_v2_availability() 在 Colab 环境中是否返回 True？
4. 即使 mask 成功，SubspacePlanner 是否能产出有效 plan_digest？
5. run_embed.py 的 plan_digest 非 None 分支是否进入 subspace_projection 而非 fallback？
```

**链路 B：detect 端内容证据产出**
```
目标字段：detect_record.content_evidence_payload.status == "ok"
          detect_record.score_parts.lf_status == "ok"
          detect_record.score_parts.hf_status == "ok"

追踪路径：
1. _resolve_detect_image_path() 在当前 workflow 中能否获得有效图像路径？
2. SemanticMaskProvider.extract(inputs={"image_path": ...}) 是否成功产出 mask_digest？
3. ContentDetector.extract() 期望的输入字段是什么（必须读 content_detector.py:extract() 签名）？
4. _build_content_inputs_for_detect() 提供的输入字段是否与 ContentDetector 期望的字段匹配？
5. 若不匹配，lf_score/hf_score 是否有其他产出路径？
6. image_domain_sidecar_enabled: false 时，HF/LF 信号是否有替代产出路径？
```

**链路 C：detect 最终判决可达性**
```
目标字段：detect_record.detect_runtime_mode == "real"
          detect_record.final_decision != null
          detect_record.fusion_result.decision_status ∈ {"decided", "rejected_insufficient_evidence"}

追踪路径：
1. detect/orchestrator.py 中 real 模式门控的所有条件是什么（逐条列出）？
2. 在当前 paper_full_cuda 配置下，每个条件是否可满足？哪个是最终阻断条件？
3. fusion/decision.py 中 decision_status 的所有可能取值是什么？
4. 当某链路禁用/absent 时，decision_status 输出什么？是否符合 NP 框架语义？
```

**链路 D：TPR@FPR 统计指标可计算性**
```
目标字段：evaluate_record.tpr_at_fpr_primary != null
          evaluate_record.fpr_empirical != null

追踪路径：
1. calibration_record 的 calibration_samples 数量是否满足 NP 统计要求（≥30）？
2. detect_record 是否包含 is_watermarked 字段（ground truth 标签）？
3. 当前 workflow 是否有生成正负样本对的机制？
4. calibration 样本中是否有 calibration_sample_is_synthetic_fallback=true 的污染？
```

### 2.2 前瞻性阻断点扫描（假设修复后的下一个阻断点）

对每个已知缺陷，在描述修复方案时，**必须同时描述"假设该修复完成后，链路中的下一个阻断点是什么"**。

例如：不可以只写"下载 inspyrenet 权重即可修复"——必须继续追问：
- 下载权重后，`_probe_model_v2_availability()` 通过，那么 `SubspacePlanner` 能正常产出 plan_digest 吗？
- plan_digest 产出后，`_build_content_inputs_for_detect()` 是否能为 `ContentDetector` 提供正确的结构化输入？
- 即使 ContentDetector 收到正确输入，`lf_status=ok` 是否还依赖 sidecar_enabled=true？

---

## 第三步：审计内容要求

按以下三类分别输出结论：

### 3.1 静态代码审计即可确认差距项

**格式要求（每项必须包含以下全部字段）：**

```
### [优先级]：[标题]

**目标字段**（本项修复后，以下字段应达到的状态）：
- 字段路径: 期望值

**当前实际状态**（从产物字段级证据引用）：
- embed_record/detect_record/calibration_record/evaluate_record 中的实际字段值

**代码级根因**（必须精确到文件名+行号+代码片段）：
- 文件:行号 → 具体代码逻辑描述

**调用链追踪**（从入口到目标字段的完整路径，禁止截断）：
- 入口 → 中间层1 → 中间层2 → ... → 目标字段
- 每层必须说明：输入是什么、为什么失败/成功、产出是什么

**修复方案**：
- 具体到哪个文件哪个函数做什么改动

**修复后的下一个阻断点**：
- 假设本项修复完成，链路中的下一个卡点是什么
```

### 3.2 需要真实 SD workflow 才能判定差距项

对每项说明：
- 静态代码已确认的前提条件（如"代码路径存在"）
- 为什么无法静态判断（具体说明哪个变量的值依赖真实运行）
- 对应的验收字段（真实运行后需检查哪个字段判断是否通过）

### 3.3 消融实验可行性评估

必须覆盖以下五个消融维度，每个维度给出"当前是否可产出有意义的消融结论"及原因：
- `enable_content` 消融（禁用内容链）vs baseline
- `enable_geometry` 消融（禁用几何链）vs baseline
- `enable_hf` 消融（禁用高频通道）vs baseline
- `enable_lf` 消融（禁用低频通道）vs baseline
- `enable_anchor` 消融（禁用 attention anchor）vs baseline

---

## 第四步：测试套件验证

运行测试并将结果纳入静态审计证据：
```
python -m pytest tests/ -q --tb=short
```
对失败的测试，**必须读取测试文件代码确认失败原因**，不得仅凭错误信息猜测。

可调用 `.github/skills/` 中的技能框架作为审计结构辅助，但技能框架不能替代逐行代码阅读。

---

## 第五步：审计结论完整性自检

在输出审计报告前，必须逐项回答以下自检问题：

1. **四条主链路是否每条都追踪到了目标字段？** 若有链路被截断，必须标注"链路追踪不完整，截断于：[位置]"。
2. **产物引用是否区分了两套产物？** `colab_run_paper_full_cuda` 与 `audit_diagnostics` 不得混用。
3. **每个修复方案是否包含了下一个阻断点？** 若没有，该修复方案无效。
4. **是否存在"代码路径存在"但"数据流不通"的情况？** 即某个函数被调用了，但其输入字段与函数期望的字段类型/结构不匹配。
5. **消融实验五个维度是否全部覆盖？**

---

## 审计报告格式

- 版本号格式：`YYYY-MM-DD-HHMM审计报告`
- 存放路径：`doc/审计/`
- 模板参考：`doc/审计/历史/2026-03-02-0300审计报告.md`
- 不符合项按 HIGH-CRITICAL > HIGH > MEDIUM > LOW 排序
- 功能缺失或真实方法缺失等根本性影响论文研究的部分，在标题前加【根本性缺失】前缀

---

以下是我的研究目的，如果有任何不清晰的地方，请随时提问以获取更多细节。

---

基于语义显著性的自适应流形水印
论文创新点：不再对所有图像使用统一的嵌入强度或固定的低维子空间，而是根据每张图像的语义显著性动态调整水印嵌入的“流形区域”。
具体方法：自适应子空间选择：在 Shallow Diffuse的基础上，不单纯依赖Jacobian矩阵的低秩性，而是结合图像的语义掩码。在纹理复杂区域（高频）利用 T2SMark 的尾部截断策略增强鲁棒性；在平滑区域（低频）利用 PRC-Watermark 的编码策略保证隐蔽性。抗几何攻击机制：引入一个轻量级的空间同步模块（类似于传统水印中的同步模板），但在Latent空间中实现。利用扩散模型的Self-Attention图作为具有几何不变性的特征锚点，将水印信息绑定在Attention Map的相对关系上，而非绝对像素位置上。
预期结果：在保持高生成质量的同时，显著提升对抗几何变换（旋转、缩放）和高斯噪声的鲁棒性，超越 T2SMark 的表现。

---

本文方法属于**基于扩散模型 latent 空间的初始噪声调制型水印**。与传统初始噪声调制方法直接检测噪声相关性不同，本文将噪声扰动视为**诱导结构化证据的载体**，通过后验可观测特征构建**可审计、可拒绝的证据体系**，而非依赖单一连续分数完成判决。
系统采用**内容域证据链与几何结构证据链并列的多证据链设计**。两条证据链在实现、语义与失败机制上均保持独立，仅通过统一的 EvidenceBundle 结构在策略层汇合。任一证据链均允许失败，失败被视为合法且有信息量的系统状态，而非异常。
内容域引入**内容自适应子空间选择机制**，显式区分不同频率区域在鲁棒性与隐蔽性上的角色分工：
1. **高频区域**：采用类似 T2SMark 的尾部截断或相关增强策略，其目标是构造**对攻击强度单调敏感、可自然失败的证据子空间**，在强攻击下明确转入失败状态，避免伪鲁棒残留。
2. **低频区域**：采用类似 PRC-Watermark 的编码约束型策略，以保证视觉隐蔽性，并在无攻击或弱攻击条件下提供稳定证据。
内容域明确引入 `content.status = failed` 的失败语义，避免在证据已不可信时输出低判别力分数。
几何证据链通过**显式空间同步与低阶几何反演**应对 crop、resize、rotate 等几何攻击。检测端仅在同步信号显著、几何参数可稳定估计时输出几何证据；否则明确返回失败状态。
扩散模型的 Self-Attention Map 不作为主几何锚点，仅在主同步路径成功后作为辅锚点参与一致性评估，避免产生“稳定但不可信”的伪几何证据。
所有证据仅以显式字段（状态、质量分数、失败原因）进入策略层。最终判决由**唯一、冻结的策略与决策层**完成，显式选择 `policy_path`，禁止证据链内部的隐式降级、跨链调用或自由组合，从系统层面消除“隐式几何”与“隐式多链”风险。
方法在统计意义上严格遵循**Neyman–Pearson 范式**进行阈值校准，系统性压低假阳性率（FPR），允许在强攻击场景下提高假阴性率（FNR）。该取舍与“可拒绝证据链”设计一致，优先保证判决可信性与审计一致性，而非追求表面鲁棒。

---