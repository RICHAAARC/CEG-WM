请继续在当前仓库完成“静态代码审计差距”收敛修复，不要先讲方案，直接改代码并跑测试。

当前已知状态：
1. 已做过部分修复：`main/core/records_io.py`、`tests/test_records_io_atomic_replace_fallback.py`、`pyproject.toml`。
2. 仍未闭环的问题：
   - `tests/conftest.py` 的 Windows+Py3.13 tmpdir 补丁在 `--basetemp=.pytest/tmp/...` 下会因父目录不存在报 `FileNotFoundError`。
   - 集成测试中仍有 `.writing` 临时文件残留断言失败（experiment_matrix / multi_protocol 写盘相关）。
3. 要求所有 pytest 临时目录都在 `.pytest/` 下，不允许再出现根目录 `.pytest_tmp_*` 新目录。

硬约束：
1. 不得改动冻结边界语义与门禁等级（BLOCK/NON_BLOCK 不得降级）。
2. 不得通过跳过测试、放宽断言、绕开审计获得“假通过”。
3. 仅做最小必要改动，函数命名保持现有风格。

请执行的修复任务：
1. 修复 `tests/conftest.py`：
   - 给 `given_basetemp` 创建逻辑补上父目录创建（`parents=True`），保证 `.pytest/tmp/...` 可稳定创建。
2. 修复 `main/core/records_io.py`：
   - 确保 `_atomic_replace_write_bytes` 在主路径成功/WinError5回退/异常分支下都不会遗留 `.writing` 文件。
   - 保持异常语义：非目标异常必须抛出，禁止吞异常。
3. 必要时调整相关测试（仅限反映真实语义，不得弱化要求）。

测试与验收（按顺序执行）：
1. 先建目录：`mkdir .pytest\\tmp`（若不存在）。
2. 快测：
   - `python -m pytest -q tests/test_records_io_atomic_replace_fallback.py --basetemp .pytest/tmp/fast1 -p no:cacheprovider`
3. 关键集成：
   - `python -m pytest -q tests/test_experiment_matrix_write_path_safety.py tests/test_multi_protocol_write_path_safety.py --basetemp .pytest/tmp/fast2 -p no:cacheprovider`
4. 扩展回归：
   - `python -m pytest -q tests/test_experiment_matrix_writes_outputs_under_run_root.py tests/test_experiment_matrix_runner_writes_summary_under_run_root.py tests/test_multi_protocol_runner_and_compare.py --basetemp .pytest/tmp/fast3 -p no:cacheprovider`
5. 门禁：
   - `python scripts/run_all_audits.py --repo-root . --strict`

最终输出格式：
1. 先给结论：是否已完全修复静态差距（是/否）。
2. 列出改动文件和每个文件修复点。
3. 给出每条测试命令的结果（通过/失败）。
4. 若仍有未闭环项，按两栏输出：
   - 静态代码审计即可确认差距项
   - 需要真实sd模型workflow确认差距项（并逐条写“此项风险需要真实sd模型workflow才能判断”）。
