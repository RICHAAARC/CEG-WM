"""Minimal real-runtime adapters for Stage-A method execution.

Imports remain explicit at submodule level so the method scorer can use the
ordinary-image observation boundary without creating a method/runtime cycle.
"""
