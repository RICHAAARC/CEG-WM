# 2026-03-02-0300修复记录
> 记录版本号：2026-03-02-0300
---

# 本次实际执行记录

## 已实施代码改动

A. `scripts/run_onefile_workflow.py`
- 新增 `_resolve_paper_semantic_model_path`：
  - 支持 `/content/...` 到仓内相对路径的受控映射。
  - 对不可解析路径输出显式告警日志。
- `paper_full_cuda` profile 配置生成：
  - 保持 `mask.impl_id=semantic_saliency_v2`。
  - 写入解析后的 `mask.semantic_model_path`。
  - 将 `embed.geometry.sync_strength` 从 `0.1` 调整为 `0.2`。

B. `main/watermarking/detect/orchestrator.py`
- `_build_content_inputs_for_detect`：
  - 显式输入优先级：`image` > `latent` > `image_path`。
  - 返回结构加入 `input_source`。
- `run_detect_orchestrator`：
  - 将 `input_source` 注入 `content_evidence_payload` 供审计。
- `load_scores_for_calibration`：
  - 新增 synthetic fallback 过滤。
  - 新增统计字段 `n_rejected_synthetic_fallback`。
- 新增 `_is_synthetic_fallback_calibration_sample` 判定函数。

C. 测试改动
- `tests/test_onefile_workflow.py`
- `tests/test_calibrate_evaluate_readonly_threshold.py`
- `tests/test_c4_calibrate_evaluate_readonly.py`

D. `tests/test_onefile_workflow_paper_full_profiles.py`
- 修改 `scripts/run_onefile_workflow.py`：在 formal 校准样本写入前执行 `pop("calibration_sample_is_synthetic_fallback", None)`。
- 修改 `tests/test_onefile_workflow_paper_full_profiles.py`：断言调整为 formal 样本不包含 `calibration_sample_is_synthetic_fallback`。
- 修改 `doc/审计/2026-03-02-0300修复记录.md`：补充 commit 对照与本次操作记录。

## 失败事件复盘

### 现象
- `outputs/fail/audit_diagnostics/logs/workflow_execution.log` 显示 embed/detect 已成功执行，失败发生在 calibrate 阶段。
- 错误信息为：`calibration requires at least one valid content_score sample`。

### 根因
- 根因属于本次 0300 变更引入：
  - `load_scores_for_calibration` 新增了 synthetic fallback 硬过滤。
  - sidecar-disabled 分支生成的 formal 校准输入仍继承 `calibration_sample_is_synthetic_fallback=true`。
  - 导致唯一候选样本在 calibrate 被过滤，最终无有效分数样本而报错。

### 已落地修正
- 将 sidecar-disabled 分支中的“diagnostic 样本标记”与“formal 校准输入”解耦：
  - `detect_record_for_calibration_diagnostic.json` 继续保留 `calibration_sample_is_synthetic_fallback=true` 作为审计证据。
  - `detect_record_for_calibration.json` 不再携带 `calibration_sample_is_synthetic_fallback` 字段，仅保留 `calibration_sample_usage=formal_with_sidecar_disabled_marker`。
- 该修复保证 calibrate 在当前单样本链路下可继续执行，同时保留诊断可追溯性。

## 追加修复

### 触发背景
- 用户反馈 `outputs/fail` 仍在 calibrate 阶段失败，需恢复 workflow 可跑通。

### 修复动作
- 文件：`scripts/run_onefile_workflow.py`
  - 在 sidecar-disabled fallback 的 formal 校准样本写入前，移除 `calibration_sample_is_synthetic_fallback` 字段，避免被 calibrate 入口硬过滤。
- 文件：`tests/test_onefile_workflow_paper_full_profiles.py`
  - 同步更新断言：formal 样本不再携带 synthetic 标记；diagnostic 样本仍要求带 synthetic 标记。

