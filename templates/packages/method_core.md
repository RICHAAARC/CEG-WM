# CEG-WM Method Core Package

This package contains the importable CEG-WM research core and lightweight unit tests. Governance, notebooks, experiments, models, outputs, and historical repositories are intentionally excluded.

`extraction_manifest.json` distinguishes structural validity from release-candidate readiness. Neither field is scientific evidence.
